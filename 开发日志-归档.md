# 开发日志（归档）

> v0.x 历史版本日志归档，按日期倒序记录。
> 当前版本（v1.x）日志见「开发日志.md」。
> 与「重要开发经验.md」（踩坑与解决方案）和「重要设计.md」（架构与设计决策）配合阅读。

## 目录

- [2026-08-06](#2026-08-06)
  - v0.9.11 — 首页新增「上一轮角色扮演」可视化入口
  - v0.9.10 — 档案更新引导词微调：点明「人物档案」+ 显式维护指令
  - v0.9.9 — 档案更新输入段引导自解释：说明 character / current_profile / turn_index
  - v0.9.8 — 档案预筛 0 入选排查：日常互动也算新记忆
  - v0.9.7 — 确认弹层自绘：修复 plugin:dialog|confirm not allowed by ACL
  - v0.9.6 — Gate 预筛 0 入选排查：原文日志 + 预筛提示词强化
  - v0.9.5 — 四份默认提示词优化：输出契约前置 + JSON 格式强化
  - v0.9.4 — 档案预筛 Gate 请求体分段化：名单块 + 剧情块 + 输出契约
  - v0.9.3 — Gate 预筛请求体分段化：名单块 + 剧情块 + 输出契约
  - v0.9.2 — 角色推演请求体分段化：档案块 + 剧情块 + 输出契约
  - v0.9.1 — 角色扮演默认提示词重写 + 注入包裹强化 + TauriTavern 宿主兼容修复
  - v0.9.0 — 剧情前置 NPC 推演（发送前 Gate 预筛 + 并发角色推演 + 注入后生成）
  - v0.8.12 — 日志系统：跟随按钮修复（重复监听器 + 钉底语义）
  - v0.8.11 — 日志系统：导出按钮 toast 反馈
  - v0.8.10 — 自动档案维护修复：宿主事件订阅（event_types 解析 + 事件源看门狗）
  - v0.8.9 — 日志系统：噪音过滤扩展（真实日志量化驱动）
  - v0.8.8 — 自动档案维护：Gate 预筛 + 并发分析（generationEnded 驱动）
  - v0.8.7 — 档案分析：普适世界设定写入世界观（世界书三类处理）
  - v0.8.6 — 档案分析：世界书本人条目直接补全档案（世界观/家庭背景为空修复）
  - v0.8.5 — 日志系统：暂停/跟随修复（暂停快照 + 热重载共享状态）
  - v0.8.4 — 档案分析请求体重构：剧情前置 + 分节引导消息
  - v0.8.3 — 日志系统：噪音治理（脱敏 / 噪音过滤 / 重复折叠）
  - v0.8.2 — 日志系统：完整请求体捕获与导出（DEBUG 用）
  - v0.8.1 — 日志系统：网络请求捕获（API 请求/响应可视化 DEBUG）
  - v0.8.0 — 世界书系统（触发完全跟随 SillyTavern）
  - v0.8.0 热修复 — TauriTavern 下世界书崩溃
  - v0.8.0 功能追加 — 世界书视图：每本书搜索 + 条目排序
  - v0.8.1 热修复 — 世界书未注入排查 + 旧提示词自动迁移
  - v0.8.1 诊断增强 — 世界书注入日志带条目名
  - v0.8.1 修复 — AN/深度/示例/出口位置的触发条目现在也会注入
  - v0.7.1 — 分析取消与按钮联动
  - v0.7.0 — 注册系统 + 档案系统（首个核心功能）

- [2026-08-05](#2026-08-05)
  - v0.6.0 — 预设系统：四个子系统提示词管理
  - v0.5.0 — 日志系统：后台运行观察与 DEBUG
  - v0.4.0 — 总前端：首页启动器（多视图面板）
  - v0.3.0 — API 连接（OpenAI 兼容）
  - v0.2.0 — UI 骨架：菜单图标 + 常驻悬浮球 + 占位面板

## 2026-08-06

### v0.9.11 — 首页新增「上一轮角色扮演」可视化入口

**背景**
- 发送前角色推演的结果（预筛入选、各角色内心独白、最终注入 SillyTavern 的 `<NPC_Deduction>` 提示词原文）此前只能看 toast 和后台日志，用户想回看上一轮到底推演了什么、注入了什么，没有入口。

**改动**
- **首页**：「嘿，欢迎回来！」便签右侧新增小图标（🎭 面具），带状态圆点（绿=已注入 / 黄=超时 / 灰=本轮无角色 / 红=失败），悬停 tooltip 显示时间、预筛、成败摘要。
- **新视图「上一轮角色扮演」**：点击图标进入，展示最近一轮快照：
  - 概要：结果徽章（已注入 N 个角色 / 超时 / 未注入原因）+ 时间、耗时、预筛入选、成功/失败/跳过计数 + 角色名单；
  - 角色内心独白：每个成功角色一张卡片，独白原文保留换行，可滚动；
  - 注入提示词原文：`<NPC_Deduction>` 块完整展示，带「⧉ 复制」按钮（clipboard API + execCommand 兜底）。
- **数据**：`runNpcDeductionPipeline` 的 `finish()` 统一把 `record` + 成功角色独白 + 注入原文写入 `npcDeductionLastRound` 快照（超时/失败/跳过也留痕）；推演进行中若面板正停留该视图会实时刷新。
- 版本号升至 0.9.11（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；DOM 桩冒烟测试覆盖空记录 / 已注入 / 跳过 / 超时四态，断言全部通过。

### v0.9.10 — 档案更新引导词微调：点明「人物档案」+ 显式维护指令

**背景**
- v0.9.9 的输入段引导仍不够直接：第一句「以下是本轮的输入 JSON」没有点明这是人物档案，也没有一句明确的「请维护档案」指令。

**改动**
- 首句改为「以下是本轮的输入 JSON，其中 current_profile 就是当前角色的人物档案：」；
- 末尾新增显式维护指令：「请按【输出契约】约定的格式对本档案进行维护：fields 覆盖本轮需要改写的标量字段；各分节按 add / remove / update 增量更新——add 的新条目避免与 current_profile 已有内容重复，remove / update 的 id 必须来自 current_profile 中已有的 id。」
- 版本号升至 0.9.10（index.js / manifest.json）。

**验证**
- `node --check` 语法通过。

### v0.9.9 — 档案更新输入段引导自解释：说明 character / current_profile / turn_index

**背景**
- 抓包完整请求体（11:56）发现档案更新请求的最后一段 user 消息只有一句「请依据约定输出 JSON，输入如下」，直接把 JSON 丢给模型，没有像剧情段（`<Recent_Messages>`）和世界书段那样说明输入是什么、该怎么用。
- 风险：模型可能不清楚 `current_profile` 就是该角色当前已记录的完整档案，导致增量维护（add 去重、remove/update 引用真实 id）执行不稳定。

**改动**
- **代码 · 输入段引导词**：`buildArchiveAnalysisMessages` 最后一段改为自解释：
  - `character`：本轮要维护档案的角色名；
  - `current_profile`：该角色当前已记录的完整档案（标量字段 + 各分节条目，每条带 id），是唯一权威现状——add 的新条目要避免与其中已有内容重复，remove / update 的 id 必须来自其中已有的 id；
  - `turn_index`：当前对话的消息索引，仅供参考。
- 请求体结构不变（提示词 → 剧情 → 世界书 → 输入 JSON），只增强输入段的引导说明。

**验证**
- `node --check` 语法通过；版本号升至 0.9.9（index.js / manifest.json）。

### v0.9.8 — 档案预筛 0 入选排查：日常互动也算新记忆

**背景**
- 实测日志（11:38）出现「自动档案维护：预筛完成 入选 0/3 个角色 []」，原文 `{"characters":[]}`——模型返回的是合法 JSON，不是格式错误，是**真实误判**。
- 该轮剧情是早餐场景：纱雾被主人点名坐身边、因平胸被拉姆取笑并搬出「主人说纱雾这样最好看」、诗涵给她喂红枣；拉姆喂诗涵吃饭、取笑纱雾；蕾姆盛粥、心里失落、鼓起勇气请求把忆梦抱来一起吃。三个已注册角色全部有实际出场和新的互动/情绪，却一个都没入选。
- 根因在提示词：判断要点里「只是单纯登场或说话，不要列入」+「拿不准时倾向不列入，宁少勿多」+ 示例全是「亲历事件/被告知新事实/关系变化」这类大事件，模型把日常早餐场景归为「平淡日常、无新信息」，被「宁少勿多」推向空数组。

**改动**
- **提示词 · 档案预筛判断要点**：明确「有实际出场并参与互动（说话、行动、情绪反应、被直接点名或作为动作对象）时，通常就有值得记录的新记忆；日常互动（一起吃饭、闲聊、小事件、情绪变化）同样算数，不要因为场景平淡就认为没有变化」；「宁少勿多」改为「对有实际出场并参与互动的角色倾向列入，对只是被提及的角色不列入」。
- **代码 · Gate 引导消息**：`buildAutoArchiveGateMessages` 的剧情段引导同步改为「有实际出场并参与互动的角色通常就有新记忆可记录，只有完全没有出场或确实无新信息的角色才不列入」。
- **迁移 · 新增 V4 旧默认**：`LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V4`（v0.9.6/7 默认文本），未自定义的旧默认自动升级，自定义文本不动。

**顺带修复：旧默认常量反引号转义 bug**
- 排查迁移时发现 v0.9.5/v0.9.6 新增的旧默认常量（`LEGACY_DEFAULT_ARCHIVE_SYSTEM`、`ARCHIVE_PRESCREEN`、`_V2`、`_V3`、`ROLEPLAY_PRESCREEN_V2/_V3`、`ROLEPLAY_SYSTEM_V3`）把「```json」写成了「\`\`\`json」：字符串字面量求值后是「反斜杠+反引号」，而真实默认（模板字面量）是普通反引号，导致迁移条件永远匹配不上真实保存的旧默认。
- 已全部改为普通反引号；迁移模拟测试 6 个场景全部通过（V1/V2/V3/V4 旧默认→升级、新默认→不动、自定义→不动）。

**验证**
- `node --check` 语法通过；迁移模拟测试通过；V4 与新默认仅差判断要点三行。

### v0.9.7 — 确认弹层自绘：修复 plugin:dialog|confirm not allowed by ACL

**背景**
- 日志出现两条 `error promise Command plugin:dialog|confirm not allowed by ACL`（间隔约 3 秒，对应「恢复默认」与「注销角色」两次确认操作）。
- 根因：TauriTavern 的 WebView 会把 `window.confirm` 拦截为 `plugin:dialog|confirm` 宿主命令，但宿主 ACL 未放行该命令，调用即 Promise reject 并打印该报错；`globalThis.confirm?.()` 的 `?.` 只防 undefined，不防调用时被拒绝。

**改动**
- **代码 · 自绘确认弹层**：新增 `showConfirm(message)`（返回 Promise<boolean>），在面板内渲染 `.soullink-confirm` 遮罩 + 手绘风格确认卡片（取消 / 确定 / Esc 取消），替换 `resetPreset` 与 `unregisterCharacter` 两处 `globalThis.confirm` 调用；两函数改为 async。
- 不再依赖宿主 confirm 能力，报错消除；样式复用现有 `--sl-paper` / `--sl-ink` 变量与涂鸦滤镜；关闭面板会取消未决的确认（防 Promise 悬挂）。

**验证**
- `node --check` 语法通过；`globalThis.confirm` 已无残留调用。

### v0.9.6 — Gate 预筛 0 入选排查：原文日志 + 预筛提示词强化

**背景**
- 实测日志出现「角色推演：Gate 预筛完成 入选 0/3 个角色 []」：用户消息直接点名「纱雾」（「我把纱雾放下来，决定抱一会女儿」），Gate 却一个角色都没选，整轮推演被跳过。
- 日志只能确认 Gate 返回了可解析的 JSON（无解析错误日志），无法区分两种可能：① 模型真的判断无人有戏份（误判）；② 模型按对话里的简称输出「纱雾」而非名单全名「和泉纱雾」，被精确匹配过滤成 0。

**改动**
- **代码 · 原文日志**：角色扮演 Gate 0 入选时打 warn 并附模型返回原文（前 400 字），档案预筛 0 入选时打 debug——下次可直接区分「空回」还是「名字变体」。
- **提示词 · 两份预筛**：输出契约补充「即使对话里用简称或昵称（如「纱雾」），也必须输出名单中的全名（如「和泉纱雾」）」；角色扮演预筛判断要点补充「被直接点名包括作为动作对象（被抱起、放下、触碰、呼唤、喂食等），必须列入」。

**设计取舍：不做代码级简称容错**
- 曾尝试在 `parseGateCharacterNames` 里按「全名包含简称」回退匹配（「纱雾」→「和泉纱雾」），随后删除：简称/昵称的映射依赖具体游戏的称呼习惯，代码无法可靠泛化（另一场游戏的角色可能不叫「纱雾」，且包含匹配可能把未注册的同名角色误映射到已注册角色）。
- 名字匹配保持精确（去空白后逐字一致）；「模型输出名单全名」由提示词约束，0 入选时日志附原文兜底排查。

**迁移**
- 新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V3` / `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V3`（v0.9.5 默认文本）；未自定义的旧默认自动升级，自定义文本不动。
- 版本号升至 0.9.6（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；迁移模拟测试通过（v0.9.5 旧默认→升级、新默认→不动、自定义→不动）。

### v0.9.5 — 四份默认提示词优化：输出契约前置 + JSON 格式强化

**背景**
- 实测（deepseek-v4-flash:0731）角色扮演推演频繁「全部失败」：模型返回的是直接书写的独白正文，而不是约定的 JSON 对象（日志：`AI 返回内容无法解析为 JSON：Unexpected token '主'...`）。
- 排查发现两个诱因：①「角色扮演」默认提示词的风格示例是**纯散文**，模型会模仿示例格式直接输出正文；② 输出契约放在提示词末尾，且「尽快返回 / 精简步骤」的措辞容易诱导模型省略 JSON 包装。

**改动（四份默认提示词）**
- **输出契约前置**：四份提示词统一把「输出契约」提到开头并标注最高优先级，措辞绝对化——「你的回复必须且只能是一个 JSON 对象，禁止输出任何其他内容：不要 Markdown、不要 ```json 代码块标记、不要解释、不要前后缀文字」。
- **示例即格式**：「角色扮演」的风格示例改为 JSON 包裹（`{ "character": ..., "monologue": ... }`），格式与口吻示范二合一；示例内 ASCII 引号改为「」避免破坏 JSON。
- **名字精确匹配**：两份预筛提示词明确要求角色名与名单逐字一致（不得改名 / 缩写 / 加修饰），并说明「程序按名字精确匹配，写错名字的角色会被丢弃」。
- **角色名契约**：「角色扮演」输出契约补充 character 必须与输入指定角色名逐字一致。
- **id 有效性**：「档案系统」输出契约补充 remove / update 的 id 必须来自 current_profile 已有 id，凭空编造的 id 无法应用。
- **措辞修正**：四份提示词把「必须**尽快**返回结果，因此要**精简步骤、控制篇幅**」改为「请直接给出结论，不要输出思考过程或多余说明」，避免诱导省略格式。

**迁移**
- 新增 `LEGACY_DEFAULT_ARCHIVE_SYSTEM` / `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V2` / `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V3` / `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V2` 四个旧默认常量（与 v0.9.4 及更早默认逐字一致）；`getSettings` 仅在保存文本与旧默认逐字一致（未自定义）时自动升级为新默认并写日志，自定义过的文本不动。
- 版本号升至 0.9.5（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；迁移逻辑模拟测试 5 个场景全部通过（旧默认→升级、新默认→不动、自定义→不动、混合→只升该升的、示例 JSON 可解析）。

### v0.9.4 — 档案预筛 Gate 请求体分段化：名单块 + 剧情块 + 输出契约

**背景**

- 自动档案维护的 Gate 请求此前是「system 提示词 + JSON 输入（registered_characters + recent_messages）」，与 v0.9.3 角色扮演预筛的分段式结构不对称。
- 用户要求按「档案预筛系统提示词 → 已注册角色名单 → 当前场景（最新四条消息）」的固定顺序组织，名单与剧情分别用 XML 标签包裹，并在各自上方配引导消息——与角色扮演预筛同款处理。

**完成内容**

- **请求体重构**（`buildAutoArchiveGateMessages`，v0.9.4 起按四段式组织）：
  1. system 档案预筛提示词；
  2. 名单段：引导消息 + `<Registered_Characters>` 块（已注册名单 JSON 数组，XML 包裹）；
  3. 剧情段：引导消息 + `<Recent_Messages>` 块（最近 4 条消息，XML 包裹）；
  4. 输出契约段：约定 JSON 模板（`{ "characters": [] }`）。
- **引导消息措辞**：名单段说明「这是当前全部已注册角色名单，预筛只能从这份名单中挑选角色，名单之外一律视为未注册、不参与本轮预筛」（与角色扮演预筛一致）；剧情段说明「据此判断哪些角色本轮确实获得了新信息、新经历，或关系、背景发生了值得记录的变动；只是单纯登场或说话、没有新信息可记录的角色不要列入」——与「档案预筛」提示词的判断要点呼应。
- **默认提示词输入说明同步**：v0.9.4 新默认把「输入说明」改为描述 `<Registered_Characters>` / `<Recent_Messages>` 两个标签块，输出契约里的「registered_characters 中角色名的子集」改为「`<Registered_Characters>` 名单中角色名的子集」；判断要点 / 输出契约其余内容原样保留。
- **旧默认自动迁移**：新增 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN`（v0.9.4 及更早默认文本）——只有保存文本与旧默认逐字一致（未自定义）才自动升级为新默认并写日志。
- 版本号更新至 v0.9.4（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 12 个场景全部通过：新增「档案预筛 Gate 四段式 + 旧默认自动迁移」场景，断言 Gate 请求为 system → 名单引导 + 块 → 剧情引导 + 块 → 输出契约、请求体不含 registered_characters 字段、旧默认自动升级为新默认并持久化。

### v0.9.3 — Gate 预筛请求体分段化：名单块 + 剧情块 + 输出契约

**背景**

- 预筛（Gate）请求此前是「system 提示词 + JSON 输入（registered_characters + recent_messages）」，与角色推演的四段式结构不对称，模型对「名单」与「剧情」的边界感弱。
- 用户要求按「角色扮演预筛系统提示词 → 已注册角色名单 → 当前场景（最新四条消息）」的固定顺序组织，名单与剧情分别用 XML 标签包裹，并在各自上方配引导消息。

**完成内容**

- **请求体重构**（`buildNpcDeductionGateMessages`，v0.9.3 起按四段式组织）：
  1. system 角色扮演预筛提示词；
  2. 名单段：引导消息 + `<Registered_Characters>` 块（已注册名单 JSON 数组，XML 包裹）；
  3. 剧情段：引导消息 + `<Recent_Messages>` 块（最近 4 条消息，XML 包裹）；
  4. 输出契约段：约定 JSON 模板（`{ "characters": [] }`）。
- **引导消息措辞**：名单段说明「这是当前全部已注册角色名单，预筛只能从这份名单中挑选角色，名单之外一律视为未注册、不参与本轮预筛」；剧情段说明「据此判断哪些角色本轮会开口、被直接点名或明显有戏份；只是被提及但不需要开口或参与的角色不要列入」。
- **默认提示词输入说明同步**：v0.9.3 新默认把「输入说明」改为描述 `<Registered_Characters>` / `<Recent_Messages>` 两个标签块，输出契约里的「registered_characters 中角色名的子集」改为「`<Registered_Characters>` 名单中角色名的子集」；判断要点 / 输出契约其余内容原样保留。
- **旧默认自动迁移**：新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN`（v0.9.2 及更早默认文本）——只有保存文本与旧默认逐字一致（未自定义）才自动升级为新默认并写日志。
- 版本号更新至 v0.9.3（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 11 个场景全部通过：场景 1 的 Gate 断言改为四段式（system → 名单引导 + 块 → 剧情引导 + 块 → 输出契约，名单段先于剧情段，请求体不含 registered_characters 字段）；新增「旧默认预筛提示词自动迁移 → v0.9.3 新默认」场景。

### v0.9.2 — 角色推演请求体分段化：档案块 + 剧情块 + 输出契约

**背景**

- 角色推演请求此前是「system 提示词 + 剧情段（<Recent_Messages> 块）+ JSON 输入（character / current_profile）」：档案混在 JSON 输入里，结构不直观，模型对「档案」与「剧情」的边界感弱。
- 用户要求按「角色扮演系统提示词 → 角色档案 → 当前场景（最新四条消息）」的固定顺序组织，档案与剧情分别用 XML 标签包裹，并在各自上方配引导消息。

**完成内容**

- **请求体重构**（`buildNpcDeductionCharacterMessages`，v0.9.2 起按四段式组织）：
  1. system 角色扮演提示词；
  2. 档案段：引导消息 + `<Character_Profile>` 块（`serializeArchiveForPrompt` 序列化的完整档案，XML 包裹）；
  3. 剧情段：引导消息 + `<Recent_Messages>` 块（最近 4 条消息，XML 包裹）；
  4. 输出契约段：角色名 + 约定 JSON 模板（`{ "character", "monologue" }`）。
- **引导消息措辞**：档案段说明「这是你扮演该角色的唯一身份依据，请据此维持设定一致；档案里的世界观是 TA 已知或相信的规则，超出档案与剧情获知范围的信息对 TA 一律不可知」；剧情段说明「先判断该角色是否在场、听到了什么、看到了什么，只有 TA 亲历 / 被告知 / 当场目击的内容才是 TA 此刻知道的，不在场的对话对 TA 不可知」——与提示词里的信息盲区 / 认知框架要求互相呼应。
- **默认提示词输入说明同步**：v0.9.2 新默认把「输入说明」改为描述 `<Character_Profile>` / `<Recent_Messages>` 两个标签块（原 JSON 字段描述已过时），其余内容（口吻要求 / 三要素 / 认知状态 / 风格示例 / `<npc_behavior>` / 输出契约）原样保留。
- **旧默认自动迁移**：新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V2`（v0.9.1 默认文本），与 v0.9.0 的 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM` 一并纳入迁移条件——只有保存文本与任一旧版默认逐字一致（未自定义）才自动升级。
- 版本号更新至 v0.9.2（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 10 个场景全部通过：场景 1 增加四段式顺序断言（提示词 → 档案引导 + 块 → 剧情引导 + 块 → 输出契约，档案块先于剧情块，块内无其他角色名单）；新增「v0.9.1 默认提示词自动迁移 → v0.9.2 新默认」场景。

### v0.9.1 — 角色扮演默认提示词重写 + 注入包裹强化 + TauriTavern 宿主兼容修复

**背景**

- 真实 TauriTavern 2.2.0 实测：点击发送后工作流完全没启动（无 toast、请求直接发出）。逐行核对宿主源码后定位根因：TauriTavern 的 `getContext()` 不提供 `extension_prompt_types` / `extension_prompt_roles`（标准 SillyTavern 提供），`getExtensionPromptApi()` 把它们当必需项 → 返回 null → messageSent 监听器静默放行。
- 推演质量反馈：注入的角色提示词「不太行」，默认「角色扮演」提示词对内心独白的口吻与结构要求不够硬，模型容易写成分析报告或要点清单。

**完成内容**

- **宿主兼容（TauriTavern 2.x）**：`getExtensionPromptApi()` 只把 `setExtensionPrompt` 当必需项；`extension_prompt_types` / `extension_prompt_roles` 缺失时按已知数值常量兜底（IN_CHAT=1、SYSTEM=0，与宿主 `extension-prompts.js` 一致）。
- **「角色扮演」默认提示词重写**（新默认）：
  - 「口吻要求」升级为最高优先级：第一人称、像 TA 在心里自言自语，禁止第三人称分析报告 / 要点列表 / 书面总结腔，独白必须是一段连贯心流；
  - 三要素硬性要求：心情、想法、下一步行动（缺一不可，自然融入独白，不分开列点）；
  - 两种认知状态：信息盲区（明确表现出不知道什么、疑点在哪、在猜什么；只用档案 + 亲历/被告知/目击的信息，宁可误判不可全知）与认知框架（身份/立场/经历决定解读方式）；
  - 内置风格示例（松本/天晓案例，示范口吻与结构，要求不得照抄内容或人名）；`<npc_behavior>` 行为逻辑块原样保留。
- **旧默认自动迁移**：新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM` 常量（与 v0.9.0 及更早的默认文本逐字一致）；`getSettings` 仅在保存文本与旧默认完全相同（用户未自定义）时自动升级为新默认并写日志，自定义过的文本不动。
- **注入包裹强化**：`<NPC_Deduction>` 块首行改为「# **扮演指令**：以下是各角色在下一轮剧情中的心理状态与行为倾向（由子 agent 推演）。请把这些状态当作各角色当下的真实内心，内化为它们的行为依据并自然融入接下来的剧情与对白——务必遵循，但不要在正文中复述或引用本段原文。」——指令性更强、更醒目，同时保留「不要复述原文」护栏。
- 版本号更新至 v0.9.1（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 9 个场景全部通过：新增「TauriTavern 无枚举上下文 → 推演启动并注入」与「旧默认提示词自动迁移 → 推演使用新默认」，其余 7 个场景回归通过。

### v0.9.0 — 剧情前置 NPC 推演（发送前 Gate 预筛 + 并发角色推演 + 注入后生成）

**背景**

- 角色扮演 / 角色扮演预筛两份提示词自 v0.6.0 起就绪，但一直缺少核心调度（「重要设计.md」第 14 节待定项）。
- 目标：用户点击发送后，先推演当前剧情中活跃 NPC 的即时心理与行为倾向，注入提示词后再让主模型生成，提升角色一致性、主动性与「活人感」。

**完成内容**

- **接管发送流程**：订阅宿主 `messageSent`，监听器**返回 Promise**（宿主 EventEmitter 的 `emit` 会 `await` 每个监听器，`sendMessageAsUser` 又 `await` emit，因此主模型请求被阻塞到推演完成；已验证 TauriTavern 与 SillyTavern 的 eventemitter 均为该语义）。
- **Gate 预筛**：只送「已注册名单 + 最近 4 条消息」（复用「角色扮演预筛」提示词），返回 `{ "characters": [...] }`；与注册名单求交集，未知名字丢弃。
- **并发角色推演**：Gate 入选角色各自独立 API 调用（复用「角色扮演」提示词），每个角色只获得**自己的档案 + 最近 4 条消息**，不共享上下文、不带世界书、不带完整聊天记录；输出 `{ "character", "monologue" }`，角色名不匹配 / 空独白视为失败。
- **注入**：`setExtensionPrompt('SoulLink_NPC_Deduction', text, IN_CHAT, depth=0, role=SYSTEM)` —— IN_CHAT + depth 0 在 OpenAI 与文本补全两条路径里都落在**最后一条用户消息正下方**（OpenAI：`populationInjectionPrompts` 在最新消息前插入后整体 reverse；文本路径：`doChatInject` 同语义）；内容为 `<NPC_Deduction>` 统一格式块，并注明「仅作角色扮演参考，不是剧情正文」。
- **注入清理**：`generationEnded` / `generationStopped` 后置空注入（`setExtensionPrompt(key, '', ...)`），下一轮开始前也先清旧注入——防止 swipes / 重生成 / 后续轮次复用过期推演块。
- **稳定性**（延续自动档案维护的设计纪律）：运行锁 + 签名去重（并发发送直接放行）；Gate 失败 / 无角色入选 / 全部推演失败 → 不注入直接放行；单角色失败不影响其余；45s 总硬超时，到点 abort 在途请求并放行发送，绝不卡死用户发送。
- **UI**：首页新增「角色推演」卡片 + 子视图（开关按钮、最近一轮汇总：耗时 / Gate 入选 / 成功 / 失败 / 注入角色）。
- 版本号更新至 v0.9.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 8 个场景全部通过：完整成功流程（阻塞 + Gate 交集过滤 + 并发推演 + 注入位置/角色）、generationEnded 清理、开关关闭直接放行、Gate 空名单跳过、单角色失败其余继续、运行锁跳过并发发送、硬超时放行（测试档 1.5s）、面板视图与开关。
- 待手动验证：真实 Tavern 中发送一条消息，观察「角色推演中… → 完成」toast、日志「已注入提示词」、主模型回复是否体现推演内容；再验证 swipe / 重生成不会复用上一轮推演块。

### v0.8.12 — 日志系统：跟随按钮修复（重复监听器 + 钉底语义）

**背景**

- 用户反馈：点「⏬ 跟随」按钮不会常亮，也不会一直让日志界面钉在底部显示最新日志，只像一次性「回到底部」。
- 排查发现两个问题：① 跟随按钮被绑定了**两个 click 监听器**（v0.8.5 改造时与并行编辑冲突残留），一次点击被连续切换两次，状态与按钮外观完全不变、点击也不滚动；② 跟随语义是「仅当列表已在底部附近时才自动滚动」，用户上翻后跟随静默失效，观感上就是「只回到底部一次」。

**完成内容**

- **删除重复监听器**：跟随按钮只保留一个 click 处理，点击一次 = 切换一次（常亮 ⇄ 熄灭），开启时立即滚到底部。
- **跟随改为「钉底」语义**：跟随开启时，无论当前滚动位置，新日志一律自动滚到底部显示最新；关闭后新日志照常追加但不再滚动（可自由上翻阅读历史，「回到最新」按钮兜底）。
- 按钮 title 同步说明：「跟随：钉在底部，新日志自动滚到底部（点一下关闭）」。
- 版本号更新至 v0.8.12（manifest.json / index.js）。

**验证**

- 
ode --check 通过；jsdom 测试通过：默认常亮、上翻后新日志仍钉底、点击一次熄灭且不再滚动、再点击常亮并立即回底。
- 待手动验证：真实 Tavern 中开启跟随后上翻列表，确认新日志仍自动滚到底部。

### v0.8.11 — 日志系统：导出按钮 toast 反馈

**背景**

- 用户反馈点「💾 导出」「📦 完整请求体」没有任何提示，不知道是否成功、导出了多少。

**完成内容**

- 「💾 导出」：导出后 toast 提示「已导出 N 条日志（JSON 文件）」；日志为空时 warning「暂无日志可导出」。
- 「📦 完整请求体」：导出后 toast 提示「已导出最近 N 次完整请求体（JSON 文件）」；无捕获时 warning「暂无完整请求体（触发一次对话请求后自动捕获）」。
- 版本号更新至 v0.8.11（manifest.json / index.js）。

**验证**

- 
ode --check 通过；jsdom 测试通过（空日志/空捕获 warning、有数据 success）。

### v0.8.10 — 自动档案维护修复：宿主事件订阅（event_types 解析 + 事件源看门狗）

**背景**

- 用户实测：AI 回复生成结束后没有任何预筛/分析 toast，档案未更新，整个工作流未启动。排查发现两个根因：
  1. **事件名错误**：SoulLink 用裸字符串 `'generationEnded'` 订阅 `ctx.eventSource`，但 TauriTavern 的 eventSource 需要 `ctx.event_types.GENERATION_ENDED` 的值（st-chatu8 / NPC Tracker 均按此订阅）；裸字符串订阅从未触发，导致自动维护（以及 chatChanged / groupSelected 等宿主事件订阅）全部静默失效。
  2. **事件源重建**：TauriTavern 在主生成后可能重建 `ctx.eventSource`，bootstrap 时绑定的订阅成为孤儿（NPC Tracker 为此专门写了看门狗）。

**完成内容**

- **事件类型解析**：新增 `HOST_EVENT_TYPE_KEYS` 映射 + `resolveHostEventType()`，`onHostEvent` 与 `initHostEventLogging` 统一经 `ctx.event_types` 解析事件名（取不到时回退原字符串），兼容 TauriTavern 与标准 SillyTavern。
- **事件源看门狗**：`startHostEventWatchdog()` 每 4s 对比当前绑定事件源与宿主现时事件源，被换后自动重挂 chatChanged / groupSelected / generationEnded 订阅（热重载时先清旧 interval）。
- **诊断日志**：bootstrap 完成时记录实际解析出的 `generationEnded` 事件名，便于在日志系统确认订阅生效。
- **运行提示补全**：预筛开始时 toast 提示「档案预筛中…」，与「预筛完成」「逐角色结果」「汇总」形成完整流程提示链。
- 版本号更新至 v0.8.10（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试新增 2 个场景：事件名解析（订阅挂在 `event_types` 解析值上、不再挂裸字符串）、事件源重建后看门狗重挂且新事件源上能跑通 Gate。全部 9 个场景通过。
- 待手动验证：重启 Tavern 后发送一轮对话，确认日志出现「扩展就绪 … generationEnded 事件: …」与「预筛开始/完成」，toast 正常弹出，档案更新。

### v0.8.9 — 日志系统：噪音过滤扩展（真实日志量化驱动）

**背景**

- 用户反馈「噪音还是相当多，一堆一堆的」。用桌面真实日志（131 条 / 一次生成）量化：getRegexedString: Skipping script（正则脚本跳过）34 条、Event emitted:（事件总线）约 20 条、WI entry N does not use probability 8 条、元数据保存类 5 条、生成流程内部 6 条，合计占 91%。
- 发现 v0.8.3 过滤的漏洞：[WI] / [Prompt Template] 在真实环境里常以 **info 级别**输出（如 [WI] Hypothetically adding 9 entries、[Prompt Template] processing #8 messages），而旧过滤只对 debug 级别生效，全部漏网。

**完成内容**

- **过滤级别扩展**：console 噪音过滤从「仅 debug」扩展到「debug + info」（warn/error 永不误伤），[WI] / [Prompt Template] 的 info 输出不再漏网。
- **名单扩充**（LOG_NOISE_PREFIXES）：新增 getRegexedString: Skipping script、Event emitted: （宿主事件已由 event 来源记录，console 版冗余）、WI entry 、Chat Completions: saving token cache、Saving/Saved metadata、Debounced metadata save cancelled、---calling setPromptString、calling runGenerate、generating prompt、Auto-continue is disabled by user.、Skipping extension interceptors for dry run、Core/all messages:、skipWIAN not active。
- **网络噪音**（NETWORK_NOISE_PATTERNS）：新增按 URL 模式过滤 Tavern 内部非模型调用——ipc.localhost/*（token 计数 / 聊天提交 IPC）与 /api/chats/save；模型 API 调用（/api/backends/chat-completions/*）与 SoulLink 自身请求永不误伤。
- 效果：真实日志 131 条 → 保留 8 条（模型请求/响应/完整请求体、Stream stats、Generate entered、第三方扩展 [QR2]），噪音减少 94%。

**验证**

- 
ode --check 通过；jsdom 集成测试通过（16 类噪音全灭、关键日志保留）；用真实日志静态回放验证覆盖率。
- 待手动验证：真实 Tavern 中触发一次生成，确认日志视图只剩模型调用与关键事件。

### v0.8.8 — 自动档案维护：Gate 预筛 + 并发分析（generationEnded 驱动）

**背景**

- 档案系统此前只能手动点「分析本角色 / 分析全部角色」；用户希望每轮 AI 回复生成结束后自动维护档案，先由轻量预筛（Gate）判断「本轮哪些已注册角色的信息/记忆会变」，再只对入选角色并发跑完整档案分析，避免对全员无谓调 API。

**完成内容**

- **自动触发**：订阅宿主 `generationEnded` 事件，每轮生成结束且确实产出新的 AI 回复后自动启动 Gate 流程；生成被中断（`streamingProcessor.abortController.signal.aborted`）、末条不是 AI 回复、无已注册角色、API 未配置时静默跳过。
- **Gate 输入最小化**：只送「已注册角色名单 + 最近 4 条消息」（`registered_characters` + `recent_messages`，严格 `ARCHIVE_RECENT_MESSAGE_COUNT=4`），不携带档案、世界书等任何其他上下文；system 提示词复用「预设」里的「档案预筛」（`archivePreScreen`），输出契约 `{ "characters": [...] }`。
- **稳定性设计**（参考 NPC Tracker 流程，规避其已知缺陷）：
  - 末条签名去重：同一末条被宿主反复触发（自动续写/事件重放）只处理一次；
  - 运行锁：上一轮 Gate/分析还在飞时新事件直接跳过，杜绝并发 API 风暴；
  - 名单交集：Gate 返回的名字必须与已注册名单求交集，未知名字一律丢弃；
  - 失败即跳过本轮：Gate 调用失败不降级、不重试轰炸，本轮视为已处理。
- **复用现有分析**：入选角色直接并发调用现有 `analyzeCharacter`（含世界书注入与增量更新），不重新开发；结果按 ok/error/cancelled/skipped/busy 汇总日志与 toast。
- **运行提示**：预筛完成时 toast 提示「预筛完成：入选 X/Y 个角色，开始更新档案」（无入选时提示「本轮无角色需要更新档案」），分析结束 toast 汇总成功/失败；API 未配置时 toast 警告「自动档案维护已开启，但 API 尚未配置」；逐角色结果沿用「分析本角色」的既有 toast（如「「木之本樱」更新 3 处」）。
- **开关**：档案系统工具栏新增「⚡ 自动维护」按钮（`autoArchiveEnabled` 设置，默认开启），点击即时持久化。
- 版本号更新至 v0.8.8（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 7 个场景全部通过：正常 Gate 流程（1 次 gate + 2 次并发分析、gate 输入严格为名单+4 条消息且无其他上下文）、同一末条去重、gate 空名单不分析、未注册名交集过滤、生成中断跳过、开关关闭跳过、末条非 AI 回复跳过。
- 待手动验证：真实 Tavern 中开启自动维护，发送一轮对话后确认日志出现「预筛开始/完成」与逐角色分析，档案按剧情增量更新。

### v0.8.7 — 档案分析：普适世界设定写入世界观（世界书三类处理）

**背景**

- v0.8.6 只放行了「本人设定卡」，其余世界书内容仍被挡在档案外；但用户指出世界设定很多是**对所有人生效**的（社会秩序、认主体系、超自然规则等）——它们是该角色作为世界一员默认知晓的常识，理应写进世界观，而不是等对话里「亲历/被告知」才记录。

**完成内容**

- **世界书三类处理**（默认「档案系统」提示词输入说明 + 判断要点同步重写）：
  - ① 与 `character` 同名的条目 = 本人设定卡，权威来源，直接补全档案；
  - ② 普适世界设定（对所有人生效的规则）= 该角色默认知晓并相信的常识，直接写入世界观；
  - ③ 其他角色的个人条目与私密信息（秘密、私事、内心、不在场经历）= 仅作背景参考，只有该角色确实获知才写入。
- 世界观判断要点改为「三来源」：本人设定卡中的世界运转规则、世界书中的普适世界设定、对话中确实获知的新设定——前两者直接进入，后者按获知情况写入；其他角色私密信息不得写入；仍记录「该角色眼中的版本」。
- 世界书段引导消息同步为三类说明；旧默认文案定点迁移（v0.8.6 文案 → v0.8.7 文案，与 v0.8.5→v0.8.6 迁移链式衔接）；版本号更新至 v0.8.7（manifest.json / index.js）。

**验证**

- `node --check` 语法通过；迁移链断言：v0.8.5 保存文案经两次迁移后 == v0.8.7 新默认、幂等、不误伤其他提示词。
- 待手动验证：真实 Tavern 中重跑「分析本角色」，确认世界观补全普适设定（日裔/华裔秩序、认主体系等）与本人设定卡内容。

### v0.8.6 — 档案分析：世界书本人条目直接补全档案（世界观/家庭背景为空修复）

**背景**

- 用户导出 v0.8.5 完整请求体排查「世界观与家庭背景 AI 一点没填」：请求体里其实完整注入了 `<大道寺知世>` 本人条目（人物背景、性格、额外背景、sexual_profile 一应俱全），但默认提示词把**所有**世界书内容定性为「不代表该角色亲历或已知，仅供推断」，且判断要点规定「只有该角色确实获知（亲历/被告知/目击）的设定才应进入其世界观」「家庭背景仅在对话给出明确新信息时才新增」——最近 4 条剧情没有明说家庭与世界观，模型按规则如实留空，等于把角色自己的设定卡挡在档案之外。

**完成内容**

- **本人设定卡概念**：世界书条目通常以「<角色名>…</角色名>」分节；其中与 `character` 同名的条目是该角色本人的设定卡（固有身份、家庭背景、性格、人际关系、世界观），是权威设定来源，可直接用于补全该角色档案；其余条目仍是背景信息，只有该角色确实获知的内容才可写入。
- **默认「档案系统」提示词重新措辞**（5 处）：引言与标量/MBTI 判断改为「能从对话或其本人设定推断」；世界书输入说明区分本人条目与背景条目；世界观判断要点改为「本人设定卡中的世界运转规则可直接进入其世界观」；家庭/关系判断要点改为「以本人设定卡为准可直接补全，对话新信息也新增，记忆仅记对话亲历」。
- **世界书段引导消息同步**：`buildArchiveAnalysisMessages` 的引导消息明确「与 character 同名的条目是本人设定卡，可直接补全档案；其余条目仅作背景参考」。
- 旧默认文案定点迁移（5 处替换，保留自定义内容）；版本号更新至 v0.8.6（manifest.json / index.js）。

**验证**

- `node --check` 语法通过；迁移断言：旧默认文案逐条升级、幂等、不误伤其他提示词。
- 待手动验证：真实 Tavern 中重跑「分析本角色（知世）」，确认世界观（日裔/华裔秩序、认主体系等）与家庭背景（大道寺财团、母亲服装设计师等）被补全。

### v0.8.5 — 日志系统：暂停/跟随修复（暂停快照 + 热重载共享状态）

**背景**

- 用户反馈「点了暂停，新日志还是会出现」。复现确认两个真因：① 暂停只拦截了实时追加，但暂停期间任何过滤 / 搜索 / 开关噪音 / 重开日志视图都会触发 
enderLogList 整表重建，把暂停期间缓冲的新日志泄漏进列表；② 暂停状态是模块级 let，扩展热重载后按钮（旧实例闭包）切换的状态与新版捕获管道（新实例 pushLogEntry）各执一份，暂停完全失联。

**完成内容**

- **暂停快照**：暂停时记录可见边界 pausedAtId（暂停时刻最后一条日志的 id），
enderLogList / getVisibleLogEntries 在暂停状态下只渲染 id <= pausedAtId 的快照；过滤、搜索、噪音开关、重开视图都不会泄漏暂停期间的新日志；点「继续」一次性整表重建显示缓冲日志。
- **热重载共享状态**：缓冲与暂停/序列状态迁到全局共享对象 __soullinkLogState__（ntries / sequence / paused / pausedCount / pausedAtId），新旧脚本实例共用同一份数据；热重载后历史不分裂、暂停状态不丢失、「继续」按钮与捕获管道永远指向同一份状态。清空（按钮与 SoulLinkLog.clear）同步重置暂停计数与快照边界。
- **语义与交互**：暂停按钮 title 明确「新日志先缓存（+N），不追加到列表；点继续一次性显示」；跟随按钮 title 明确「仅列表在底部时自动滚动，上翻不打断阅读」，开启跟随时立即滚到底部。

**验证**

- 
ode --check 通过；jsdom 冒烟全部通过（含新增：暂停中过滤不泄漏、热重载后暂停仍生效、继续一次性显示、清空后暂停计数重置）。
- 待手动验证：真实 Tavern 中暂停后触发一次「分析本角色」，确认列表不再增长、徽标 +N、过滤不泄漏，点「继续」后一次性显示。

### v0.8.4 — 档案分析请求体重构：剧情前置 + 分节引导消息

**背景**

- 用户基于「📦 完整请求体」导出复盘档案分析请求：原请求体顺序为 system 提示词 → 世界书注入 → JSON 输入（剧情藏在 `recent_messages` 字段里），剧情与世界书没有分节、没有引导说明，模型只能靠字段名猜测「哪段是剧情、哪段是世界书」。

**完成内容**

- 请求体改为四段式消息：① system 档案提示词 → ② 剧情段（引导消息 + `<Recent_Messages>` 块，最近 4 条消息 XML 包裹）→ ③ 世界书段（引导消息 + `<World_Info_Before>` / `<World_Info_Extra>` / `<World_Info_After>` 块，保留原 XML 包裹，无注入时不发该消息）→ ④ 输入段（JSON：character / current_profile / turn_index）。
- 剧情段引导措辞：「以下是最新的剧情…请先判断该角色本轮是否在场、真实获知或亲历了什么，再据此更新人物档案；该角色不在场的内容只作背景，不得写入其档案」；世界书段引导措辞：「以下是世界书注入内容，包含人物档案与世界背景信息，仅供了解世界观设定，不代表该角色亲历或已知；只有该角色确实获知的内容才能写入其档案」。
- `recent_messages` 从 JSON 输入移除（已独立成 `<Recent_Messages>` 块，不再重复烧 token）；默认「档案系统」提示词输入说明同步改为描述 `<Recent_Messages>` 块，并对旧默认文案做定点迁移（只替换那一行描述，保留其余自定义内容）。
- 版本号同步更新至 v0.8.4（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- 待手动验证：真实 Tavern 中触发「分析本角色」，导出完整请求体确认四段消息顺序与 XML 包裹、模型按约定 JSON 正常返回。

### v0.8.3 — 日志系统：噪音治理（脱敏 / 噪音过滤 / 重复折叠）

**背景**

- 用户提供 8.4 秒的真实日志（556 条）：93%（518 条）是 Tavern 世界书扫描 `[WI]` 的逐条目 dump（每次扫描把 109 个条目全文打一遍，一次分析重复扫 3 次），另有 `[Prompt Template]` 宏变量全量 dump（**明文泄露了 ZapiKey 等 API 密钥**）——日志缓冲区被噪音占满，真正有用的 soulink/network 条目被淹没。

**完成内容**

- **全局密钥脱敏**：`pushLogEntry` 对所有来源（含 console）的 message 与 detail 统一脱敏——`sk-` 开头密钥、JSON 字段 `apiKey/ZapiKey/password/proxy_password/authorization/token`、`Authorization: Bearer`、`tauri-invoke-key` 全部替换为 `***`；网络请求头敏感字段正则补充 `invoke`（覆盖 Tauri IPC 密钥）。
- **控制台噪音过滤**：默认过滤已知噪音前缀 `[WI]`、`[Prompt Template]`（仅限 console 来源的 debug 级别）；日志视图新增「🔇 过滤噪音」开关按钮，偏好持久化（`logConsoleNoise`，默认开）。
- **连续重复折叠**：同一级别/来源/内容（含 detail）紧挨着出现时自动折叠为一条，行尾显示 `×N` 徽标、时间更新为最后一次出现，导出文本带 `(×N)` 后缀；缓冲与导出不再被「同一内容 ×3」刷屏。
- 版本号同步更新至 v0.8.3（manifest.json / index.js）。

**效果（对照上述 556 条日志）**

- 噪音过滤后约剩 40 条（soulink 7 + network 14 + 其余 console 事件），缓冲从「被 WI 占 93%」恢复到「有效内容为主」。
- 若仍需排查世界书扫描细节，可关闭「🔇 过滤噪音」临时查看。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试新增用例全部通过：console 噪音前缀被过滤、关闭过滤后恢复捕获、`sk-` 密钥与 `ZapiKey` 字段脱敏、连续相同条目折叠 ×N（缓冲与导出）。
- 待手动验证：真实 Tavern 中触发一次分析，确认日志不再被 `[WI]` 刷屏、导出文件无明文密钥。

### v0.8.2 — 日志系统：完整请求体捕获与导出（DEBUG 用）

**完成内容**

- 日志视图新增「📦 完整请求体」按钮：导出最近 5 次对话请求（chat-completions / generate_chat_completion）的**未截断**完整请求体与响应体 JSON 文件。
- 网络捕获层对对话请求额外读取完整请求/响应（不经过 6000/20000 字符截断），内存环形缓冲保留最近 5 份，密钥等敏感字段照常脱敏。
- 捕获成功时记一条 debug 日志「完整请求体已捕获」，便于确认。
- 日志视图与导出文件保持原有截断展示不变，完整内容只在「📦 完整请求体」导出中提供。
- 版本号同步更新至 v0.8.2（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- 待手动验证：触发一次「分析本角色」→ 日志出现「完整请求体已捕获」→ 点击「📦 完整请求体」导出，检查 messages 中 <World_Info_Before> 等世界书内容完整无截断。

### v0.8.1 — 日志系统：网络请求捕获（API 请求/响应可视化 DEBUG）

**完成内容**

- 全局 `fetch` 捕获层：包装后透传原始请求（返回结果与行为完全不变），记录每次网络请求与响应——方法、URL、请求头、请求体（JSON 自动美化）、响应状态、耗时、响应头、响应体。
- 请求体即「提示词与参数」：chat/completions 的 `messages`（完整提示词）、`temperature` 等参数、模型名全部可见；响应体即返回内容（含 `choices[0].message.content` 与 usage）。
- 流式安全：响应体通过 `response.clone()` 异步读取，不阻塞调用方、不消费原始流；读取到 20000 字符上限即取消克隆分支，长流（SSE）不会无限缓冲。
- 脱敏：`Authorization` / `api_key` / `key` / `password` / `proxy_password` / `token` / `cookie` 请求头与请求体字段一律显示 `***`，日志可安全复制导出。
- 日志条目新增 `detail` 字段（上限 20000 字符）：点击行展开后显示完整请求头/请求体/响应头/响应体；复制与导出文本也包含 detail（缩进展示）。
- 新增「来源筛选」下拉：全部 / 网络请求 / SoulLink / 控制台 / 宿主事件 / 外部扩展 / 页面错误 / Promise 拒绝；搜索范围同步覆盖 detail 内容。
- 级别映射：请求与 2xx/3xx 响应记 `debug`，4xx 记 `warn`，5xx 与网络失败记 `error`。
- 热重载兼容：fetch 包装「一次安装、动态派发」，扩展脚本重新执行时只替换捕获目标，不重复包装。
- 版本号同步更新至 v0.8.1（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试新增网络用例全部通过：请求/响应条目生成、请求体含提示词与参数、密钥脱敏、响应体与响应头捕获、来源筛选、点击展开显示 detail、热重载后捕获不中断。
- 待手动验证：真实 Tavern 中触发一次 AI 分析（或任意 API 调用）→ 日志系统筛选「网络请求」→ 展开查看完整请求/响应。

**过程中发现并修复的 bug**

1. fetch 包装器最初只派发了 response/error 事件，漏派 request 事件，导致「请求」条目不落日志 → 在发起原始请求前补派 `{ kind: 'request' }`。

### v0.8.0 — 世界书系统（触发完全跟随 SillyTavern）

**完成内容**

- 首页新增「世界书」入口卡片：实时显示引擎可用性与已排除条数（「跟随酒馆规则」/「已排除 N 条」/「引擎不可用」）。
- 世界书视图：展示当前聊天激活的世界书（按书分组）与全部条目；每条目显示显示名（comment 优先，其次首个关键词，兜底 uid）、位置徽章（注入前/注入后/AN 顶部/深度注入/示例顶部/出口等）、常驻徽章、以及「本次触发」标记（当前扫描里被酒馆实际激活的条目）。
- 触发规则**完全交给 SillyTavern**：直接调用宿主 `ctx.getWorldInfoPrompt(chat, maxContext, true)`（dry run，无副作用事件），扫描 chat 的构造与酒馆生成时一致（过滤 system 消息、按设置带角色名前缀、最新消息在前）。扫描深度 / 递归 / 概率 / 预算 / 粘性冷却 / 分组评分等全部由酒馆引擎决定，本程序不实现任何自己的触发规则。
- 注入位置与酒馆一致：TauriTavern 的 `getWorldInfoPrompt` 会返回 `worldInfoActivation.entries`（每个激活条目的 world+uid+内容+位置），程序把「注入前」条目组装到分析提示词开头（`<World_Info_Before>` 块）、「注入后」条目组装到结尾（`<World_Info_After>` 块），中间是分析指令与 JSON 输入；AN/深度/示例/出口位置的条目在档案分析提示词中没有对应槽位，与酒馆一致不注入。
- 条目排除：勾选即把该条目加入排除名单，存储键为「书名 + 条目 uid」（酒馆自己的稳定身份，不用显示名，避免 NPC Tracker 那种按名字匹配需要 sanitize 的脆弱设计）；被排除条目即使被酒馆触发也绝不会进入档案分析提示词。「清除排除」一键还原。
- 宿主降级：未提供 `worldInfoActivation` 的宿主（如原版 ST）降级为「原样注入酒馆拼好的 worldInfoBefore/After 文本」（触发仍 100% 跟随酒馆），视图显示横幅提示「无法按条目排除」，条目复选框禁用；未提供 `getWorldInfoPrompt` 的宿主不注入世界书并明确提示。
- 档案分析接入：`buildArchiveAnalysisMessages` 改为异步，payload 移除 `world_info_background` 字段（避免与位置注入重复烧 token），默认「档案系统」提示词同步改为描述 `<World_Info_Before>` / `<World_Info_After>` 标记块；每次分析在 SoulLink 日志记录注入模式与条目统计（included / excluded / skipped）。
- 并发防护：世界书视图渲染带序号守卫，排除勾选 / 刷新 / 切聊天触发的并发渲染只允许最新一次写 DOM，杜绝交错渲染导致列表重复。
- 版本号同步更新至 v0.8.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 31 项全部通过：扫描 chat 构造（过滤 system / name 前缀 / 最新在前）/ dry run 无副作用 / before 块在首、after 块在尾、depth 跳过 / 排除后不再注入 / 取消排除恢复 / 排除名单按 书名+uid 存储 / 视图渲染（书分组、条目数、本次触发标记、排除标记、清除排除）/ 并发重渲染不重复 / strings 降级（横幅 + 复选框禁用 + 原文按位置注入）/ none 降级（首页状态 + 不注入）/ 设置默认值与旧存档迁移。
- 待手动验证：真实 Tavern 中打开世界书视图核对「本次触发」与酒馆实际触发一致 → 排除一条后分析角色，确认请求内容不含该条目 → 对比酒馆生成时的 worldInfoBefore/After 与注入文本。

**与 NPC Tracker 的差异（有意为之，避免其已知问题）**

1. **不实现自己的触发规则**：NPC Tracker 在 ST 引擎不可用（实际是其读取的 `activatedEntries` 字段在当前 ST/TT 中不存在）时会降级为「按关键词自己过滤」——绕过概率/预算/递归等语义。本程序没有降级触发路径，只有「引擎可用 / 引擎不可用」两种状态。
2. **读取正确的返回字段**：当前 ST/TT 的 `getWorldInfoPrompt` 返回 `worldInfoActivation.entries`（TauriTavern fork 新增）或根本不返回条目；NPC Tracker 读 `result.activatedEntries` 恒为空 → 永远走降级路径。
3. **扫描 chat 顺序正确**：酒馆引擎要求「最新在前」，NPC Tracker 传正序 chat，触发会作用在旧消息上。
4. **排除用稳定身份**：书名+uid，而不是「书名 :: 条目名」显示名匹配。
5. **不 import 酒馆内部模块**：NPC Tracker 尝试 `import('../../../../world-info.js')`（路径错误必然失败）再抓全局变量 / iframe 父窗口；本程序只用宿主公开 ctx API。
6. **注入保留位置**：NPC Tracker 把所有条目拼成一个 `<World_Info>` 块丢失位置；本程序按 before/after 分块注入。

### v0.8.0 热修复（2026-08-06）— TauriTavern 下世界书崩溃

- 症状：真实 TauriTavern 中日志报 `[SoulLink] getWorldInfoPrompt failed TypeError: Cannot read properties of undefined (reading 'trigger')`，世界书内容不注入、世界书视图条目复选框全部禁用（鼠标变 🚫）。
- 根因：TauriTavern fork 的 `getWorldInfoPrompt(chat, maxContext, isDryRun, globalScanData)` 内部无条件读取 `globalScanData.trigger` 组装 `worldInfoActivation`；原版 ST 的该函数不读此字段，所以只传 3 个参数在 TauriTavern 上必然崩溃。崩溃被捕获后返回 null → 模式降级为 none → 不注入 + 复选框禁用，两个症状同根。
- 修复：调用时补上第 4 参 `globalScanData`，构造方式与酒馆 `generateRawData` 完全一致——`ctx.getCharacterCardFields()` 取描述/性格/persona/深度提示/场景/创作备注，`trigger` 固定为 `normal`（对应普通生成）；宿主不提供 `getCharacterCardFields` 时安全回退为空字符串 + `normal`。
- 验证：`node --check` 通过；jsdom 冒烟测试更新至 38 项全部通过（新增第 4 参存在性、trigger=normal、六个角色字段正确映射断言）。

### v0.8.0 功能追加（2026-08-06）— 世界书视图：每本书搜索 + 条目排序

- 每本书新增名称搜索框：按条目显示名（comment 优先、其次关键词、兜底 uid）不区分大小写实时过滤，只隐藏 DOM 行、不重跑酒馆引擎、不丢输入焦点；计数徽章同步变为「匹配 / 总数 条」，搜索无结果时显示「0 / N 条」。
- 搜索词按书单独保存：排除勾选 / 刷新 / 切聊天引发的整体重渲染会恢复各书搜索词；书不再出现在激活列表时自动清理其搜索词。
- 条目排序（与酒馆编辑器 priority 排序一致，普通档内再细分）：常驻 → 本次触发 → 未触发（可触发）→ 禁用（disable=true，根本不会被激活）。禁用条目即使标了常驻也排最后——酒馆扫描时 disable 检查先于 constant，禁用条目永远不会激活。新增「禁用」徽章标识最后一档。
- 排序与搜索在所有模式下生效（entries / strings / none），不依赖激活数据。
- 验证：`node --check` 通过；jsdom 冒烟测试更新至 53 项全部通过（新增第二本书、禁用条目、排序断言、每本书搜索框隔离、计数更新、无结果、清空恢复、重渲染后搜索词保留等）。

### v0.8.1 热修复（2026-08-06）— 世界书未注入排查 + 旧提示词自动迁移

- 用户抓包发现档案分析的 API 请求里完全没有 `<World_Info_Before>` / `<World_Info_After>` 块，且 system 提示词仍是 v0.7.0 旧文本（引用 `world_info_background` / `<World_Info></World_Info>`，该字段在 v0.8.0 已从请求体移除）。
- 根因 1（确定）：`getSettings` 的提示词迁移只补「缺失键」，从不升级「旧值」——v0.7.0 时代保存的 archiveSystem 旧默认文本会永久残留，让模型期待一个已不存在的 `world_info_background` 输入字段。修复：提示词若仍引用 `world_info_background` / `<World_Info></World_Info>`（旧默认特征），自动替换为 v0.8.0 新默认（描述 `<World_Info_Before>` / `<World_Info_After>` 位置块）并持久化、console.warn 提示；只执行一次。
- 根因 2（待确认）：请求体已是 v0.8.0 新结构（无 world_info_background 字段）但无任何世界书块，最可能是抓包时仍运行着「4 参崩溃」的旧 v0.8.0（getWorldInfoPrompt 不传 globalScanData → 内部读 undefined.trigger 抛错 → 模式 none → 不注入）；该崩溃已在上一轮修复。也可能是该聊天世界书确实没有条目触发。
- 诊断增强：`buildArchiveAnalysisMessages` 每次分析把注入结果升级为 info 级日志（模式 + included/excluded/skipped 计数）；当模式为 entries/strings 但前后块均为空时记 warn「世界书未注入内容」，便于在 SoulLink 日志面板直接判断是引擎失败、书未激活还是未触发。
- 验证：`node --check` 通过；jsdom 冒烟测试更新至 57 项全部通过（新增旧提示词自动升级、升级后为新默认、只迁移一次等断言）。

### v0.8.1 诊断增强（2026-08-06）— 世界书注入日志带条目名

- 用户导出 network 日志排查「知世条目未注入」：日志里两次 `count_openai_token_prefixes` 是宿主 UI（世界书/角色卡编辑器）的本地 token 计数 IPC，不是 LLM 请求；档案分析实际只发了一次 `/api/backends/chat-completions/generate`（内部 `generate_chat_completion` 7209ms），随后一次 settings patch 写档案。
- 该次分析请求其实已成功注入世界书（`<World_Info_Before>` 含帝国设定条目），system 提示词也已是 v0.8.0 新文本（旧提示词迁移生效）；日志体被 800 字符截断（…(截断)），character/current_profile/recent_messages 部分不可见，但模型响应证明其存在。
- 为直接回答「某条目为什么没注入」，`resolveWorldInfoForAnalysis` 返回值新增 `names`（included/excluded/skipped 的条目显示名），`世界书注入` / `世界书未注入内容` 日志行现在带条目名单，可区分：未触发（名单里没有）、被排除（excluded）、位置无槽位（skipped，AN/深度/出口）、引擎失败（mode none）。
- 验证：`node --check` 通过；jsdom 冒烟测试更新至 60 项全部通过（新增 included/skipped 条目名断言）。

### v0.8.1 修复（2026-08-06）— AN/深度/示例/出口位置的触发条目现在也会注入

- 用户确认「知世」是世界书条目（Femdom学院.json uid 109，关键词 大道寺/知世），但分析请求里没有它。查实根因：该条目 position=2（AN 顶部），而旧逻辑只注入 before/after 位置，AN/深度/示例/出口一律跳过——用户世界书 95 条里 85 条是 AN 位置（角色描述都放 AN），等于绝大多数条目永远不会进分析提示词。
- 修复：`buildPositionedWorldInfoBlocks` 不再跳过非 before/after 位置，改为收集进 `<World_Info_Extra>` 补充块（AN 顶部/底部、深度、示例、出口），与 before（开头块）、after（结尾块）一起注入；只跳过内容为空的条目。排除名单对 Extra 块同样生效。
- 默认「档案系统」提示词同步更新：标记块描述改为 `<World_Info_Before>`、`<World_Info_Extra>` 与 `<World_Info_After>`；`migratePromptText` 对 v0.8.0 默认提示词里的两处旧文案做定点替换升级（保留用户其余自定义内容），世界书视图说明同步更新。
- 验证：`node --check` 通过；jsdom 冒烟测试更新至 66 项全部通过（新增 AN 条目进 Extra 块、Extra 在指令前、排除对 Extra 生效、v0.8.0 文案定点升级等断言）。

### v0.7.1 — 分析取消与按钮联动（2026-08-06）

**完成内容**

- 「分析本角色」点击后按钮变为「⏹ 取消分析角色」，再次点击立即中断该角色的 API 调用（AbortController 贯穿代理与直连两条传输路径），请求中断后不写入任何档案改动，状态显示「已取消」。
- 「分析全部角色」同理：分析进行中按钮变为「⏹ 取消分析全部角色」，点击中断全部在途分析。
- 联动规则：任一角色分析进行中，「分析全部角色」按钮即显示为「取消分析全部角色」；「分析全部」运行时每个角色卡片按钮都变为「取消分析角色」，可单独中断某一个；当全部角色都结束（完成/失败/取消）后，「取消分析全部角色」自动还原为「分析全部角色」。
- 中断实现：`fetchText` 支持外部取消信号，与超时信号合并（优先 `AbortSignal.any`，不支持时把外部取消转发到超时控制器）；用户取消抛 `SoulLinkCancelError`，与超时、真实失败区分开。
- 结果汇总升级：`analyzeCharacter` 返回 ok / error / cancelled / skipped / busy 结果，`Promise.allSettled` 后按结果计数 toast（成功/失败/取消/进行中）。
- 注销角色前自动中断其在途分析，避免删除档案后请求还在跑。
- 版本号仍为 v0.7.0 主线，功能追加不升版本。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 106 项全部通过，新增覆盖：单角色分析按钮变为「取消分析角色」并联动全部按钮 / 取消后不写入改动且状态为已取消 / 按钮与全部按钮复原 / 分析全部进行中全部按钮为「取消」/ 点击「取消分析全部角色」全部中断并复原 / 分析全部过程中单独取消一个、其余保持进行中 / 取消全部提示 toast。

### v0.7.0 — 注册系统 + 档案系统（首个核心功能）

**完成内容**

- 首页新增「角色名单」「档案系统」两个入口卡片：名单实时显示角色数，档案卡片显示「已分析/总数」。
- 注册系统：输入名字点击「注册当前角色」（或回车）即可加入名单；名单列表支持一键注销（带确认框），注销同时删除该角色档案数据。
- 档案数据模型：标量字段（姓名/年龄/性别/职业）+ 五个列表分节（性格/世界观/家庭背景/人际关系/记忆，条目含 id 与 content），随设置持久化到宿主 `ctx.extensionSettings.SoulLink.archives`。
- **按聊天绑定**：档案存储按 chatId 分区（`archives[chatId][角色名]`），切换聊天看到各自独立的名单与档案；订阅 `chatChanged` / `groupSelected` 事件自动刷新当前视图与首页状态。
- 档案系统视图：每张角色卡片展示全部字段与分节；「✏️ 编辑」进入编辑态（标量输入框 + 分节逐行 textarea），保存时逐行重建条目并保留未变化条目的 id，方便后续 AI 增量更新引用；「分析本角色」按钮用最近 4 条聊天消息 + 当前档案 + 世界书背景（常驻条目与关键词命中的选择性条目）+「档案系统」提示词调用 AI，按约定 JSON 增量更新档案。
- 并发分析：多个「分析本角色」互不阻塞、并发执行；「分析全部角色」用 `Promise.allSettled` 并发分析全部角色并汇总成功/失败。
- AI 输出解析：自动剥离 ```json 代码围栏与前后杂文再 `JSON.parse`；diff 应用支持标量覆盖与分节 add / remove / update（update 按 id 改写、remove 按 id 删除、add 去重并沿用 AI 提供的唯一 id，冲突时自动续号）。
- AI 对话传输层：复用既有 API 配置，`POST {base}/chat/completions`；跨域优先走宿主代理（`/api/backends/chat-completions`，携带模型/消息/温度），代理 401/403/404/405 或无路由时自动回退直连；兼容嵌套 `response` 字段与 `choices[0].text`；60s 超时。
- 每张卡片带状态胶囊（待分析 / 分析中… / 更新 N 处 / 无变化 / 分析失败），分析中的角色按钮禁用；首页状态、toastr、SoulLink 日志全程埋点。
- 版本号同步更新至 v0.7.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试 75 项全部通过：首页五卡片 / 注册（点击与回车）/ 重复注册拦截 / 注销（确认与取消路径）/ 档案展示与加宽 / 手动编辑保存与取消 / 单角色分析（直连，含 diff 应用与 id 沿用）/ 双角色并发（fetch 并发数 ≥2）/ 分析全部（3 路并发）/ 按聊天绑定（切换聊天名单独立）/ 围栏 JSON 解析 / diff 边界（去重、remove、update、续号）/ world info 过滤（常驻 + 关键词命中 + 禁用排除）/ 宿主代理 404 回退直连 / 未配置 Base URL 报错。
- 待手动验证：真实 Tavern 中注册角色 → 编辑档案 → 点击分析观察真实 API 返回与档案更新 → 重启 Tavern 确认档案仍保留。

**过程中发现并修复的 bug**

1. `applySectionOps` 的 add 循环最初用 `forEach`，内部 `continue` 是非法语句（SyntaxError）→ 改为 `for...of`。
2. AI 返回的 add 条目若带 `id`，最初被丢弃并强制续号 → 改为「沿用 AI 提供的唯一 id，冲突时自动续号」，与 remove/update 引用闭环。
3. jsdom 冒烟测试中 `ctx.worldInfo` 用 Node 原生 `Map` 构造，扩展代码里的 `instanceof Map` 跨 realm 判定失败 → 测试改用 `window.Map` 构造（真实 Tavern 中 worldInfo 是页面 realm 的 Map，不受影响）。

**v0.7.0 热修复（2026-08-06）**

- 档案分析在 TauriTavern 中报「对话响应不是 JSON（host-proxy）」：宿主代理的对话接口不是 `/api/backends/chat-completions`，而是 `/api/backends/chat-completions/generate`（参考同环境的 st-chatu8 扩展）；请求体格式也要同步——`custom_include_headers` 的值需带引号（`Authorization: "Bearer xxx"`）。
- 增加代理响应兜底：代理返回非 JSON（例如 TauriTavern 对未知路由返回 SPA 首页 HTML，状态码 200）时不再硬解析，自动回退直连；代理返回 `{error}` 时直接透传报错，避免把 HTML 当 JSON 解析的误导性报错。
- 修复后 jsdom 冒烟测试 80 项全部通过（新增 generate 接口路径、HTML 兜底回退直连、带引号 Bearer 头、请求体含 model/messages/stream、error 透传等断言）。

## 2026-08-05

### v0.6.0 — 预设系统：四个子系统提示词管理

**完成内容**

- 首页新增「预设」入口卡片（占整行，粉色主题），实时显示「默认配置 / 已自定义 N 份」。
- 预设视图：四个标签页（档案系统 / 档案预筛 / 角色扮演 / 角色扮演预筛），每页一个编辑器；面板进入预设视图自动加宽到 600px（`.is-preset-mode`）。
- 默认提示词：四份提示词全文内嵌为 `DEFAULT_PROMPTS`（含 \`\`\`json 反引号的转义），作为出厂默认值；设置中 `prompts` 字段随 Tavern 设置持久化，缺字段自动补齐（`getSettings` 深合并，兼容旧存档 / 部分自定义）。
- 编辑体验：字数 / 行数实时统计；状态胶囊区分「默认内容 / 已保存的自定义内容 / 未保存的更改」；「保存」按钮仅在存在未保存修改时可用；未保存内容在标签页间切换不丢失（内存暂存，保存或恢复后清除）。
- 「恢复默认」：一键还原出厂提示词并立即保存；存在自定义或未保存内容时先弹确认框，避免误覆盖；内容已为默认时按钮置灰。
- 交互闭环：打开面板刷新首页预设状态；ESC 从预设视图返回首页再关闭面板；保存 / 恢复均有 toastr 与 SoulLink 日志埋点。

**验证**

- `node --check` 语法通过。
- 提取 `DEFAULT_PROMPTS` 与四份源提示词逐字比对：全部 MATCH（含反引号转义还原）。
- jsdom 冒烟测试 37 项全部通过：面板 / 卡片创建、视图切换与加宽、四个标签渲染、默认内容填充、输入置脏（含清空文本仍视为未保存）、保存写设置、恢复默认（含确认取消路径）、标签切换保留未保存内容、首页状态联动、`getSettings` 深合并（缺字段 / 部分自定义 / null 兜底）、ESC 返回首页。
- 待手动验证：真实 Tavern 中编辑、保存后重启 Tavern 确认提示词仍保留。

**过程中发现并修复的 bug**

1. 恢复默认确认框最初只拦截「未保存的修改」：已保存过自定义内容时点击恢复会直接覆盖，缺少防护 → 改为只要有自定义内容（已保存或未保存）就弹确认，文案区分「已保存的自定义内容 / 未保存的修改」。
2. 面板 HTML 插入时误删了日志视图的收尾 `</section>`，导致日志视图与预设视图结构嵌套错误 → 补回 `</section>`，并用 jsdom 断言视图结构与激活状态。
3. 空文本编辑（全选删除）曾被视为「未保存」以外的干净状态：`presetUnsaved[key]` 空字符串为假值 → dirty 判断改为 `!== undefined`，清空文本依然置脏、可保存。

### v0.5.0 — 日志系统：后台运行观察与 DEBUG

**完成内容**

- 日志捕获层：包装 `console.log/info/warn/error/debug`（保留原始方法，SoulLink 内部日志走原始 console 避免重复捕获）；监听 `window error` 与 `unhandledrejection`；订阅 Tavern 宿主关键事件（appReady / 聊天切换 / 消息收发 / 生成开始结束 / 设置加载等）并以 `debug` 级别记录。
- SoulLink 自身埋点：启动就绪、菜单注册方式、面板开关、视图切换、悬浮球隐藏、API 连接开始 / 模型列表拉取（含传输方式与数量）等，来源标记为 `soulink`。
- 环形缓冲：默认保留 2000 条（可在 500 ~ 10000 间调整，设置持久化），超出自动丢弃最旧；日志仅存内存，不落盘。
- 日志视图 UI：级别筛选 chips（全部 / 调试 / 信息 / 警告 / 错误，带实时计数）、关键词搜索（120ms 防抖）、暂停 / 继续追加（暂停期间新日志计数展示）、自动跟随最新（滚回顶部时出现「回到最新」按钮）、清空、复制纯文本、导出完整 JSON（含元数据）。
- 渲染策略：打开日志视图时整表重建，运行中增量追加；DOM 最多渲染最近 1000 条，超出显示「仅显示最近 N 条 · 共 M 条」提示，缓冲与导出不受影响。
- 安全序列化：日志参数支持 Error / BigInt / 函数 / DOM 节点 / 循环引用，单参数超 800 字符截断，整条消息超 4000 字符截断；消息用 `textContent` 渲染，避免日志内容注入 HTML。
- 首页联动：日志卡片实时显示「已记录 N 条 / N 个错误」，有错误时变红；进入日志视图面板自动加宽（540px）并自动 clamp 回视口。
- 对外 API：`globalThis.SoulLinkLog`（debug / info / warn / error / log / clear / getEntries / setMaxEntries），供其他扩展写入日志。
- 版本号同步更新至 v0.5.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试通过：面板与日志视图创建、console 捕获与级别映射、外部 API 写入、首页状态更新、视图打开渲染、级别筛选、关键词搜索、暂停 / 恢复、环形缓冲上限、清空、宿主事件订阅。
- 待手动验证：真实 Tavern 中观察日志滚动、暂停与导出文件内容。

**过程中发现并修复的 bug**

1. 扩展脚本热重载（重新执行）时，若 console 已被旧实例包装，新实例的缓冲会收不到日志、`SoulLinkLog` API 也会指向旧缓冲 → console 包装改为「一次安装、动态派发」：包装器与窗口监听只装一次，捕获目标（handlers）在每次执行时替换为当前实例；`SoulLinkLog` API 每次执行整体重建。热重载后日志不中断、不重复包装。
2. 其余为纯新增功能，未触碰既有行为；注意：面板默认仍 340px，仅日志视图临时加宽（`.is-log-mode`），切换回首页自动恢复。

### v0.4.0 — 总前端：首页启动器（多视图面板）

**完成内容**

- 面板升级为多视图：打开面板默认进入「首页」，首页以手绘涂鸦风卡片网格展示功能图标；原「API 连接」区块移入独立子视图。
- 首页图标 ①「API 连接」：点击进入原有 API 连接界面（表单、连接、模型拉取逻辑不变），卡片上实时显示连接状态（尚未连接 / 已连接 · N 个模型）。
- 首页图标 ②「日志系统」：仅占位（🚧 施工中 · 敬请期待），预留子视图入口，日志功能后续开发。
- 视图导航：子视图标题栏出现「返回」按钮（←），点击或按 ESC 返回首页，首页再按 ESC 关闭面板；每次打开面板都回到首页。
- 版本号同步更新至 v0.4.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- 待手动验证：点击悬浮球 → 首页两个图标 → API 子视图表单/连接 → 返回/ESC 交互 → 日志占位页。

**过程中发现并修复的 bug**

1. 标题栏（拖拽手柄）内的按钮点击失效：pointerdown 启动拖拽时 `setPointerCapture` 会把后续 click 重定向到标题栏，导致「返回」和「关闭」按钮在真实浏览器中点击无效 → 指针落在按钮上时跳过拖拽初始化（不捕获、不 preventDefault），按钮点击恢复正常。

### v0.3.0 — API 连接（OpenAI 兼容）

**完成内容**

- 面板新增「API 连接」区块：Base URL、API Key（可切换明文/密文显示）、「连接并拉取模型」按钮、模型下拉选择 + 手动填写模型名称。
- 设置持久化：写入宿主 `ctx.extensionSettings`（`SoulLink` 命名空间），宿主上下文不可扩展时自动降级到模块级 WeakMap 存储，读写不中断。
- 模型列表拉取：`GET {base}/models`；跨域时优先走宿主代理（`/api/backends/chat-completions/status`），代理失败（401/403/404/405 或路由不存在）自动回退直连。
- 请求细节：带 `X-CSRF-Token`（从 cookie 取）与宿主请求头，避免宿主代理 POST 撞 CSRF 返回 403；超时用 AbortController（模型列表 20s，默认 30s）。
- 响应兼容：支持 `data.data` / `data.models` / 纯数组 / 嵌套 `response` 字段，模型项支持字符串或 `id`/`name`/`model` 字段。
- 状态反馈：面板状态胶囊（idle/busy/ok/error）+ toastr 通知；错误信息带传输方式（host-proxy / direct / direct-after-proxy-fallback），并提示可手动填模型名绕过。

**验证**

- `node --check` 语法通过。
- 重启 Tavern 后手动验证：填写 Base URL / API Key → 连接并拉取模型 → 选择模型 → 重启后设置仍保留。

**下一步计划**

- [ ] 确定 SoulLink 核心功能（灵魂链接具体做什么），再填充面板内容。
- [ ] 核心功能落地后拆分模块（参考 NPC Tracker 的 scripts/ 结构）。
- [ ] 为 API 连接补充自动化测试（mock fetch 与宿主代理）。

### v0.2.0 — UI 骨架：菜单图标 + 常驻悬浮球 + 占位面板

**完成内容**

- 扩展菜单图标：优先走 TauriTavern 宿主 API（`ST_API.ui.registerExtensionsMenuItem`），失败时手动注入 `#extensionsMenu`，并用 MutationObserver 兜底恢复。
- 常驻悬浮球：56px 紫色渐变圆球，支持拖拽移动（位置记忆）、单击打开面板、长按 650ms 隐藏。
- 占位面板：可拖拽卡片，带关闭按钮、ESC 关闭、版本号与 GitHub 链接；关闭面板后悬浮球重新出现。
- 主题适配：面板样式跟随 SillyTavern 主题变量（`--SmartTheme*`），并带 `color-mix` 降级兜底。

**验证**

- `node --check` 语法通过。
- 自建 DOM mock 冒烟测试 22 项全部通过（覆盖 host 注册成功/失败两条路径、点击/拖拽/长按/ESC/位置持久化）。

**过程中发现并修复的 bug**

1. `bootstrap` 未声明 `async`，`scheduleBootstrapFallback` 里 `bootstrap().catch(...)` 会因 `undefined.catch` 崩溃 → 改为 `async` 并 `await registerMenuItem()`。
2. `registerHostMenuItem` 无条件返回 `true`，host 拒绝注册时手动菜单项被误删 → 改为透传 host 返回值（`result !== false`）。
