# SoulLink 插件结构说明

> 本文档面向「后续维护者」——包括人类开发者与 AI 助手。目标是让你在 30 秒内知道：
> 这个插件由哪些文件组成、每个文件管什么、改某个功能该动哪里、改完怎么让改动生效。
> 阅读顺序建议：先读「架构总览」，再按需查「文件职责」。

---

## 一、架构总览

SoulLink 是一个 SillyTavern / TauriTavern 扩展，核心职责是**用子 agent 维护角色档案、并在发送前做角色推演**。它由「一个宿主入口 + 16 个源码模块」组成。

```
SoulLink/
├── manifest.json          # 宿主加载清单（只认单个 JS 文件）
├── build.js               # 构建脚本：把 js/ 源码 + prompts/ 提示词拼成单个 index.js
├── build.bat              # 一键构建：双击运行 node build.js（Windows）
├── index.js               # 构建产物（宿主实际加载的文件，勿手改）
├── style.css              # 全部样式（悬浮球 / 面板 / 各视图）
├── prompts/               # 默认提示词外挂文件（四个子系统各一个 .txt，改这里）
├── js/                    # 源码目录（改这里，然后跑 node build.js）
│   ├── constants.js       # ① 常量与默认值
│   ├── utils.js           # ② 通用工具 + 设置 + API 请求
│   ├── host.js            # ③ 宿主上下文与事件订阅
│   ├── ui-shell.js        # ④ UI 外壳：悬浮球 / 面板 / 视图切换
│   ├── views-api.js       # ⑤ API 连接面板
│   ├── views-filters.js  # ⑥ 正则过滤面板
│   ├── views-log.js       # ⑦ 日志系统（捕获 + 渲染 + 导出）
│   ├── views-presets.js   # ⑧ 预设编辑器
│   ├── views-register.js  # ⑨ 角色登记
│   ├── views-archive.js   # ⑩ 档案卡片 / 手动编辑
│   ├── views-home.js      # ⑪ 首页状态刷新
│   ├── message-filters.js # ⑫ 消息正则过滤（核心逻辑）
│   ├── worldbook.js       # ⑬ 世界书系统
│   ├── archive-analysis.js# ⑭ 档案分析 / 自动维护 / NPC 推演管线
│   ├── main.js            # ⑮ 入口：bootstrap / 菜单 / 事件看门狗
│   ├── prompts.generated.js # ⑯ 自动生成：默认提示词（勿手改）
│   └── prompts.previous.generated.js # ⑰ 自动生成：历史默认快照（勿手改）
├── 开发日志.md            # 版本迭代记录
├── 重要设计.md            # 关键设计决策与取舍
└── 重要开发经验.md        # 踩坑记录
```

**一句话理解**：`main.js` 是入口，`bootstrap()` 在宿主 `appReady` 后启动一切；`ui-shell.js` 搭起界面骨架；各 `views-*` 文件负责各自面板的交互；`archive-analysis.js` 是业务核心（AI 调用管线）；`utils.js` / `host.js` / `constants.js` 是它们共享的地基。

---

## 二、构建与加载（重要，先看这个）

### 为什么有 build.js？

TauriTavern 的 manifest 只接受**单个 JS 文件**（`"js"` 必须是字符串或单元素数组），不支持多文件数组。所以源码拆在 `js/` 下便于维护，发布时用 `build.js` 拼回一个 `index.js` 供宿主加载。

### 工作流

```
改 prompts/*.txt → 双击 build.bat（或 node build.js）→ 重启酒馆
改 js/ 里的源码 → 跑 node build.js → 重新加载扩展
```

- **改默认提示词**：直接编辑 `prompts/*.txt` → **双击 `build.bat`**（或 `node build.js`）→ 重启酒馆。构建会把 txt 内容拼进 `index.js`，宿主加载的就是新默认。
- **改代码**：永远改 `js/` 下的文件，**不要手改 `index.js`**（它是产物，会被覆盖）。
- **构建**：双击 `build.bat` 或 `node build.js`（纯 Node、零依赖，只需 Node 环境）。
- **自动构建（推荐）**：`node build.js --watch` 常驻监听 `prompts/` 与 `js/`，文件一保存就自动重建
  （含历史默认快照），改完只需在酒馆里重新加载扩展，不用再手动跑构建；Ctrl+C 退出。
- **验证**：构建后建议 `node --check index.js` 确认语法。

### 加载顺序

`build.js` 里的 `FILES` 数组顺序即拼接顺序，必须保持依赖关系：

```
prompts.generated → prompts.previous.generated → constants → utils → host → ui-shell
→ views-api → views-filters → views-log → views-presets
→ views-register → views-archive → views-home
→ message-filters → worldbook → archive-analysis → main
```

**为什么顺序很宽松**：所有文件都是普通脚本（非 ES module），顶层 `const`/`function` 进入全局作用域。真正的调用都发生在 `bootstrap()`（等 `appReady` 之后）里，所以只要「被调用时已定义」即可。但**新增文件时务必加进 `FILES` 数组**，否则不会被打包。


### 默认提示词外挂（prompts/）

四个子系统的默认提示词以纯文本外挂在 `prompts/` 目录，**不再内联在 JS 源码里**：

| 文件 | 对应子系统（DEFAULT_PROMPTS key） |
|------|-----------------------------------|
| `prompts/archiveSystem.txt` | 档案系统（`archiveSystem`） |
| `prompts/archivePreScreen.txt` | 档案预筛（`archivePreScreen`） |
| `prompts/roleplaySystem.txt` | 角色扮演（`roleplaySystem`） |
| `prompts/roleplayPreScreen.txt` | 角色预筛（`roleplayPreScreen`） |

**改默认提示词的工作流**：直接编辑对应的 `.txt` 文件 → **双击 `build.bat`**（或 `node build.js`）→ 重启酒馆。
`build.js` 读取这四个文件生成 `js/prompts.generated.js`（`DEFAULT_PROMPTS` 定义）拼入 `index.js`，宿主加载的就是新默认；同时把覆盖前的旧默认累积快照进 `js/prompts.previous.generated.js`（历史列表）。

**注意**：
- `js/prompts.generated.js`、`js/prompts.previous.generated.js` 与 `index.js` 都是构建产物，**不要手改**，改 `prompts/*.txt` 后重新构建即可。
- **本地迭代零额外操作**：每次构建会自动把旧默认加入历史列表；`getSettings` 检测到保存文本命中任一历史默认（未自定义）就自动升级到当前 `.txt` 内容——改完 txt 跑一次构建再重启即可，连续多次改版构建（未重启）也不会漏迁移。
- 用户已在 UI 里自定义过的提示词（不匹配任何历史/旧版默认）不受影响。
- **发布新版本时**：历史列表只覆盖本地构建链，无法覆盖未在本地构建过的已发布版本；要让旧版本用户自动升级，仍按惯例把该版本默认快照成 `LEGACY_DEFAULT_*_Vn` 常量并加入 `js/utils.js` 的迁移条件（`getSettings`）。

### 顶层立即执行调用（不在函数里，加载即运行）

| 位置 | 调用 | 作用 |
|------|------|------|
| `views-log.js` 末尾 | `initLogCapture()` / `exposeLogApi()` / `initNetworkCapture()` | 安装 console/fetch 包装，暴露 `SoulLinkLog` API |
| `main.js` 末尾 | `onHostEvent(..., 'appReady', bootstrap, ...)` | 等宿主就绪后启动 |
| `main.js` 末尾 | `scheduleBootstrapFallback()` | 兜底重试 bootstrap |

---

## 三、文件职责详解

### ① `js/constants.js`（426 行）— 常量与默认值

**只放数据，不放逻辑。** 所有 ID、key、超时、图标类名、默认设置都集中在这里。

- **DOM ID 常量**：`PANEL_ID`、`API_URL_ID`、`LOG_LIST_ID` 等（`soullink-*` 前缀），UI 代码统一引用，改 ID 只改这里。
- **存储 key**：`SPHERE_POSITION_KEY`、`NPC_DEDUCTION_INJECT_KEY` 等。
- **超时/阈值**：`DEFAULT_API_TIMEOUT_MS`、`NPC_DEDUCTION_TIMEOUT_MS`、`LOG_RENDER_CAP` 等。
- **默认提示词历史快照**：各 `LEGACY_DEFAULT_*`（历史版本，用于自动升级判断）；当前默认 `DEFAULT_PROMPTS` 由 `js/prompts.generated.js` 提供（见「默认提示词外挂」）。
- **默认设置**：`DEFAULT_SETTINGS`。
- **架构常量**：`ARCHIVE_SCALAR_FIELDS`、`ARCHIVE_SECTIONS`（档案的标量字段与列表分节定义）、`PRESET_META`、`HOST_EVENT_TYPE_KEYS` 等。

> 加一个 DOM ID、调超时——都来这里；改提示词默认内容去 `prompts/`（见「默认提示词外挂」）。

### ② `js/utils.js`（416 行）— 通用工具 + 设置 + API 请求

**最底层的地基，被几乎所有文件依赖。** 分三块：

- **设置读写**：`getSettings` / `saveSettings` / `saveSettingsImmediate` / `getHostExtensionSettings`（含 WeakMap 兜底）、`migratePromptText`（旧提示词自动升级）。
- **API 请求**：`fetchText`（带超时/取消）、`fetchModelList`、`chatCompletion`（带自动重试）、`requestChatCompletionOnce`（宿主代理 + 直连回退）、`parseAgentJson`。
- **通用工具**：`cloneValue`、`clampInt`、`compareVersions`、`getApiBase`、`getAuthHeaders`、`isCrossOriginUrl` 等。

> 想加一个「读设置 / 发请求 / 通用小工具」的函数，放这里。

### ③ `js/host.js`（59 行）— 宿主上下文与事件订阅

**SillyTavern / TauriTavern 的适配层。** 所有对宿主 API 的访问都经这里，隔离宿主差异。

- `getContextSafe()`：安全读取宿主上下文（`Luker` / `SillyTavern`）。
- `onHostEvent()`：统一订阅宿主事件，处理事件名映射（`HOST_EVENT_TYPE_KEYS`）与热重载去重。
- `resolveHostEventType()`：把事件名解析成宿主 `event_types` 的值。

> 涉及「宿主事件 / 上下文」的改动来这里。

### ④ `js/ui-shell.js`（817 行）— UI 外壳

**界面骨架与交互底座。** 悬浮球、面板、确认弹层、视图切换。

- **悬浮球**：`createSphere` / `initDraggableSphere`（拖拽、长按隐藏、位置持久化）。
- **面板**：`createPanel`（生成全部 HTML 结构）、`initDraggablePanel`、`openPanel` / `closePanel` / `togglePanel`。
- **视图切换**：`showPanelView`（切换各 `soullink-view`，触发对应视图的渲染）。
- **确认弹层**：`showConfirm`（自绘，规避宿主 `window.confirm` 被 ACL 拦截）。
- **版本检查**：`checkLatestVersion`（联网对比 GitHub 最新版）。

> 改面板布局、悬浮球行为、加一个视图入口——来这里。注意 `createPanel` 里内联了所有视图的 HTML。

### ⑤ `js/views-api.js`（141 行）— API 连接面板

「API 连接」视图：Base URL / API Key / 模型选择。

- `initApiSection`：绑定表单事件（连接、密钥显隐、模型选择、输入持久化）。
- `connectAndLoadModels`：连接并拉取模型列表。
- `applyApiSettingsToForm`：把已存设置回填到表单。

### ⑥ `js/views-filters.js`（335 行）— 正则过滤面板

「正则过滤」视图：管理消息过滤正则（增删改查、导入导出）。

- `initFilterSection`：绑定事件。
- `renderFilterList` / `openFilterEditor` / `saveFilterEditor` / `deleteFilter`。
- `parseRegexLiteral`：解析 `/表达式/标记` 字面量。
- `exportFilters` / `importFilters`：JSON 导入导出。

### ⑦ `js/views-log.js`（888 行）— 日志系统

**最复杂的视图。** 捕获 console / 网络 / 宿主事件，渲染、过滤、导出。

- **捕获**：`initLogCapture`（包装 console + 监听 error/unhandledrejection）、`initNetworkCapture`（包装 fetch）、`initHostEventLogging`。
- **存储**：`pushLogEntry`（含噪音过滤、重复折叠）、`logState`（热重载共享状态）。
- **渲染**：`renderLogList` / `appendLiveLogEntry` / `updateLogStats`。
- **导出**：`buildLogExportText`、完整请求体捕获（`pushFullBodyCapture`）。
- **对外 API**：`exposeLogApi`（暴露 `globalThis.SoulLinkLog`）。

> 注意：`initLogCapture` / `exposeLogApi` / `initNetworkCapture` 三个调用在**本文件末尾**（加载即运行）。

### ⑧ `js/views-presets.js`（137 行）— 预设编辑器

「预设」视图：编辑四个子系统的提示词。

- `initPresetSection`：绑定标签页切换、编辑、保存、恢复默认。
- `savePreset` / `resetPreset` / `renderPresetEditor`。
- 未保存状态用 `presetUnsaved` 记录。

### ⑨ `js/views-register.js`（291 行）— 角色登记

「角色扮演」视图：注册 / 注销角色，前置推演开关。

- `initRegisterSection`：绑定事件。
- `registerCharacter` / `unregisterCharacter`。
- `renderRegisterList`。
- 数据模型（`getCurrentChatKey` / `getRoster` / `createEmptyArchive`）也在这里。

### ⑩ `js/views-archive.js`（484 行）— 档案卡片 / 手动编辑

「档案系统」视图：展示与手动编辑角色档案。

- `renderArchiveList` / `buildArchiveCard` / `buildArchiveDisplayBody`。
- `buildArchiveEditBody` / `saveArchiveEdit` / `rebuildSectionItems`。
- `analyzeAllCharacters`（批量分析入口）、`showArchiveAnalysisError`（失败详情弹层）。

### ⑪ `js/views-home.js`（223 行）— 首页状态刷新

首页各卡片的状态显示（API / 日志 / 预设 / 角色 / 档案 / 世界书 / 上一轮）。

- `refreshHomeStatuses`：统一刷新入口。
- `refreshHomeRegisterStatus` / `refreshHomeArchiveStatus` / `refreshHomeWorldBookStatus` / `refreshHomeRoundBadge`。
- `refreshChatBoundViews`：聊天切换时刷新当前视图。

### ⑫ `js/message-filters.js`（92 行）— 消息正则过滤（核心逻辑）

**四种 AI 调用共用的消息清洗层。** 档案分析 / 档案预筛 / 角色扮演预筛 / 角色推演都经 `getRecentMessages` 取最近消息，过滤只在这一处生效。

- `getRecentMessages`：取最近 N 条消息，按启用的正则剔除匹配内容。
- `compileMessageFilters` / `normalizeMessageFilterList` / `generateMessageFilterId`。

> 想改「哪些消息进入 AI 上下文」——来这里。

### ⑬ `js/worldbook.js`（610 行）— 世界书系统

**世界书触发与排除。** 触发规则完全交给 SillyTavern 引擎，本模块只负责「读取激活结果 → 按排除名单过滤 → 按位置注入」。

- `getStWorldInfoActivation`：调用宿主世界书引擎（dry run）。
- `buildPositionedWorldInfoBlocks`：按 before/after/extra 位置组装。
- `resolveWorldInfoForAnalysis`：档案分析用的世界书解析。
- 世界书视图：`renderWorldBookList` / `toggleWorldBookEntryExclusion` / `clearWorldBookExclusions`。

> 涉及「世界书怎么注入 / 怎么排除」——来这里。

### ⑭ `js/archive-analysis.js`（968 行）— 档案分析 / 自动维护 / NPC 推演管线

**业务核心，最重的一个文件。** 三条 AI 调用管线：

- **档案分析**：`analyzeCharacter`（单角色）、`buildArchiveAnalysisMessages`（四段式请求体）、`applyArchiveDiff` / `applySectionOps`（增量更新档案）。
- **自动档案维护**：`onAutoArchiveGenerationEnded`（generationEnded 触发）、`runAutoArchiveGate`（预筛 + 并发分析）、`buildAutoArchiveGateMessages`。
- **NPC 推演**：`onNpcDeductionMessageSent`（messageSent 阻塞监听）、`runNpcDeductionPipeline`（Gate → 并发推演 → 注入）、`buildNpcDeductionInjectionText`、`clearNpcDeductionInjection`。
- **删楼联动清理**：`purgeArchiveEntriesForDeletedFloors`（楼层溯源，删楼时清理档案）。v1.0.4 起 DOM 观察器直接消费 `MutationRecord.removedNodes`（`.mes[mesid]` → 上次快照聊天副本取签名），连续删两层也能各层触发清理；快照 diff 降为幂等兜底。

> 改「AI 怎么分析 / 怎么推演 / 怎么注入」——来这里。这是理解插件业务逻辑的核心文件。

### ⑮ `js/main.js`（180 行）— 入口

**启动与装配。** 最后加载。

- `bootstrap()`：启动入口（等 `appReady`），依次初始化事件订阅、看门狗、楼层观察、面板、悬浮球、菜单。
- `registerMenuItem` / `ensureManualMenuItem` / `ensureMenuRecovery`：菜单注册（宿主 API + 手动注入双保险）。
- `installHostEventSubscriptions`：统一挂载各宿主事件订阅。
- `startHostEventWatchdog`：事件源自愈看门狗（宿主重建 eventSource 时重挂订阅）。
- 末尾：`onHostEvent(..., 'appReady', bootstrap, ...)` + `scheduleBootstrapFallback()`。

> 想加一个「宿主事件触发的新功能」——在 `installHostEventSubscriptions` 里挂订阅，在 `bootstrap` 里初始化。

---


### ⑯ `js/prompts.generated.js`（自动生成）— 默认提示词

**构建产物，勿手改。** 由 `build.js` 读取 `prompts/*.txt` 生成，定义 `DEFAULT_PROMPTS`（四个子系统的 system 提示词）。
必须排在 `constants.js` 之前拼接（`DEFAULT_SETTINGS` 引用 `DEFAULT_PROMPTS`）。

> 改默认提示词内容——编辑 `prompts/*.txt`，然后跑 `node build.js`。

## 四、数据流速查

### 一次「自动档案维护」的完整流程

```
宿主 generationEnded
  → main.js: onAutoArchiveGenerationEnded（archive-analysis.js）
  → 检查开关 / 末条签名去重 / 运行锁
  → runAutoArchiveGate：Gate 预筛（archivePreScreen 提示词）
      → buildAutoArchiveGateMessages（取最近 4 条消息，经 message-filters 过滤）
      → chatCompletion（utils.js）
      → parseGateCharacterNames（与已注册名单求交集）
  → runArchiveAnalysisBatch：逐角色分析
      → analyzeCharacter
          → buildArchiveAnalysisMessages（含世界书注入，worldbook.js）
          → chatCompletion
          → applyArchiveDiff / applySectionOps（增量更新档案）
  → 更新 UI（views-archive.js / views-home.js）
```

### 一次「发送前角色推演」的完整流程

```
用户点击发送 → 宿主 messageSent（emit 会 await 监听器）
  → main.js: onNpcDeductionMessageSent（archive-analysis.js）
  → 检查开关 / 运行锁 / 签名去重
  → runNpcDeductionPipeline
      → Gate 预筛（roleplayPreScreen 提示词）
      → 入选角色并发推演（roleplaySystem 提示词，各角色独立）
      → 拼接 <NPC_Deduction> 块
      → setExtensionPrompt 注入到最后一条用户消息下方
  → 放行发送；generationEnded / generationStopped 后清空注入
```

---

## 五、维护速查表

| 想做什么 | 改哪里 |
|---------|--------|
| 改默认提示词 | `prompts/*.txt`（改完跑 `node build.js`） |
| 加 DOM ID / 调超时 | `constants.js` |
| 加通用工具 / 改设置读写 / 改 API 请求 | `utils.js` |
| 改宿主事件订阅 / 上下文访问 | `host.js` |
| 改面板布局 / 悬浮球 / 加视图入口 | `ui-shell.js` |
| 改 API 连接面板 | `views-api.js` |
| 改正则过滤面板 | `views-filters.js` |
| 改日志捕获 / 渲染 / 导出 | `views-log.js` |
| 改预设编辑器 | `views-presets.js` |
| 改角色登记 | `views-register.js` |
| 改档案卡片 / 手动编辑 | `views-archive.js` |
| 改首页状态显示 | `views-home.js` |
| 改「哪些消息进 AI 上下文」 | `message-filters.js` |
| 改世界书注入 / 排除 | `worldbook.js` |
| 改 AI 分析 / 推演 / 注入管线 | `archive-analysis.js` |
| 加宿主事件功能 / 改启动流程 | `main.js` |
| 新增一个源码文件 | 建文件 + **加进 `build.js` 的 `FILES`** |

---

## 六、给 AI 维护者的特别提醒

1. **改 `js/`，别改 `index.js`**。`index.js` 是 `node build.js` 的产物，手改会被覆盖。
2. **新增文件必须加进 `build.js` 的 `FILES` 数组**，否则不会被打包，且要放在依赖它的文件之前。
3. **所有文件共享全局作用域**（非 ES module）。顶层 `const`/`function` 是全局的，命名要全局唯一，避免跨文件冲突。
4. **热重载安全**：日志捕获、网络捕获、事件订阅都用了 `globalThis[KEY]` 做去重，脚本重载时不会重复包装。新增「全局副作用」时沿用这个模式。
5. **宿主差异**：SillyTavern 与 TauriTavern 的 API 有差异（事件名、`getContext`、`setExtensionPrompt` 等），统一经 `host.js` / `utils.js` 适配，别在业务代码里直接碰宿主 API。
6. **改完跑一遍**：`node build.js && node --check index.js`，确认语法无误再让用户重新加载。
