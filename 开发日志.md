# 开发日志

> 按日期记录 SoulLink 的开发进度、决策与下一步计划。
> 与「重要开发经验.md」（踩坑与解决方案）和「重要设计.md」（架构与设计决策）配合阅读。
> v0.x 历史版本日志已归档至「开发日志-归档.md」。

## 目录

- [2026-08-10](#2026-08-10)
  - v1.3.2 修复 — 预设「恢复默认」按钮在默认内容下仍可点击
- [2026-08-09](#2026-08-09)
  - v1.3.1 新增 — 思考强度控制
  - v1.3.0 新增 — 档案精编
  - v1.1.9 调整 — 悬浮球统一 40px 圆形、浅色半透明
  - v1.1.8 新增 — 古风典雅主题
  - v1.1.7 新增 — 聊天气泡主题
  - v1.1.6 新增 — 主题切换入口
  - v1.1.5 新增 — API 并发限制
  - v1.1.4 调整 — 首页「日志系统」与「角色扮演」入口对调
  - v1.1.3 优化 — 注入块强化：<NPC_Deduction> 改为「系统导演注」强制指令
  - v1.1.2 优化 — 角色扮演提示词：行动判据收敛为「局面变化」
  - v1.1.1 修复 — 标准 SillyTavern 下角色卡绑定世界书显示不出来
  - v1.1.0 — 默认提示词外挂化 + 一键构建（prompts/*.txt + build.bat）
  - v1.0.18 — 「角色预筛」默认提示词模块化架构重构（任务→输入→推演→判定标准→输出→质量红线）
  - v1.0.17 — 「档案预筛」默认提示词模块化架构重构（任务→输入→判断→输出）
  - v1.0.16 — 「角色扮演」默认提示词模块化架构重构（任务→输入→推演→动机参考→输出→红线→示例）
  - v1.0.15 — 世界书视图新增「折叠本书」按钮
  - v1.0.14 — 角色预筛 Gate 措辞修正（候选集 = 已注册名单，对话只作判断依据）
  - v1.0.13 — 角色扮演默认提示词措辞收尾修正（术语统一 / 去重复 / 示例去总结腔 / 触发点明确）
  - v1.0.12 — 角色预筛 Gate 提速优化（最快返回 + 请求体精简）+ 措辞微调
  - v1.0.11 — 角色预筛 Gate PM 审查迭代（排序判据 / 开放式场景 / 在场硬前提 / 参数对齐 / 原文可见性）
  - v1.0.10 — 角色扮演默认提示词措辞修正（玩家已行动、聚焦反应）
  - v1.0.9 — 档案预筛 Gate 提速优化（最快返回 + 请求体精简 + 输出上限）
  - v1.0.8 — 角色扮演默认提示词「行为倾向推演」重构（PM 审查迭代）+ 迁移链路修复
  - v1.0.8 — 角色扮演预筛 Gate 产品化重构（PM 审查迭代：在场受影响角色必选 + 戏份排序上限）
  - v1.0.8 — 档案预筛 Gate 判断要点对齐下游触发点（PM 审查迭代：在场目击/信息补充必选）
  - v1.0.7 — 档案系统默认提示词新增记忆质量标准（长期扮演一致性）
  - v1.0.6 — 修复：其他插件广播宿主事件误触发 SoulLink API 调用（主生成流程跟踪 + messageSent 载荷校验）
  - v1.0.2 — 修复：档案分析偶发「AI 未返回文本内容」（DeepSeek 并发下返回空内容 JSON）
  - v1.0.1 — 新增：首页底部版本号旁联网检查 GitHub 最新版
  - v1.0.0 — 正式版发布
  - v0.9.16 — 修复：正则编辑界面「取消/保存后关不掉」与「保存产生重复条目」
  - v0.9.15 — 修复：删楼联动清理误伤（智绘姬等扩展原地改写楼层内容被误判为删楼）
  - v0.9.14 — 消息正则过滤：四种调用导入的最新消息按正则剔除噪音
  - v0.9.13 — 修复：删楼联动档案清理未生效（宿主事件机制与初版假设不符）
  - v0.9.12 — 删楼联动档案清理：条目按来源楼层溯源，删楼后自动清除

## 2026-08-10

### v1.3.2 修复（2026-08-10）— 预设「恢复默认」按钮在默认内容下仍可点击

**背景**
- 反馈：预设功能里提示词已是默认内容时，「恢复默认」按钮仍可点击，且状态胶囊显示「已保存的自定义内容」。

**根因**
- 默认提示词（prompts/*.txt）是 CRLF 换行，构建后 DEFAULT_PROMPTS 字符串含 \r\n；而浏览器 textarea 的 value 读取会把换行规范化为 \n（headless Chrome 实测确认）。预设视图用 textarea.value 与 DEFAULT_PROMPTS[key] 直接 === 比较，永远不相等——「默认内容」判定、按钮置灰、首页「默认配置」统计全部失效。

**改动**
- js/views-presets.js 新增 normalizePromptText（\r\n → \n）与 isDefaultPromptText 两个辅助函数，所有与默认提示词的比较（状态胶囊 / 恢复默认按钮置灰 / 首页自定义计数 / 恢复确认条件 / 输入置脏判定）统一先归一化换行再比较；
- 效果：全新安装或已恢复默认时，状态显示「默认内容」且「恢复默认」按钮置灰不可点；编辑自定义后按钮恢复可点；编辑后改回与默认逐字一致时自动回到置灰态。

**验证**
- node build.js 构建通过，node --check index.js 通过；
- 模拟真实浏览器 textarea 换行规范化（CRLF → LF）的冒烟测试 5 个场景全部通过：全新安装默认态（状态「默认内容」+ 按钮置灰）、输入自定义（置脏 + 按钮可点）、改回默认（恢复置灰）、首页「默认配置」、保存自定义后恢复默认闭环。

## 2026-08-09

### v1.3.1 新增（2026-08-09）— 思考强度控制

- API 连接页新增「思考强度」下拉（reasoning_effort，联网核对官方格式）：选项为 默认（不发送）/ 关闭思考（none）/ 低（low）/ 中（medium）/ 高（high）/ 最大（max）——Ollama /v1/chat/completions 与 OpenAI 官方均支持这五个值（Ollama 源码 slices.Contains 校验 high/medium/low/max/none；OpenAI openapi ReasoningEffort 枚举 none/minimal/low/medium/high/xhigh/max）；注意 `think` 参数只用于 Ollama 原生 /api/chat 与 /api/generate，OpenAI 兼容端点不认，故统一发送 `reasoning_effort`。
- 设置持久化到 settings.apiReasoningEffort，所有 AI 调用（分析/精编/预筛/推演）的请求体在非空时附带该字段；「关闭思考」可根治思考型模型把输出预算花在思考上导致正文为空的问题。
- 版本号升至 1.3.1（js/constants.js MODULE_VERSION / manifest.json / README.md）。

### v1.3.0 新增（2026-08-09）— 档案精编

**背景**
- 需求：游戏过程中 AI 会写出各种冗余表述与不规范格式（同一晚拆成多条、长段流水账、称呼不统一、无时间锚点），希望新增「档案精编」功能：点击按钮调用 API，把「档案精编」系统提示词与该角色完整档案一起发给 AI，要求其一规范格式、二适当提炼浓缩，输出整理后的完整档案。

**改动**
- 新增第五份默认提示词 `prompts/archiveRefine.txt`（`archiveRefine`）：任务定位为「只整理已有信息，不新增信息」；输出契约为完整档案 JSON（五个分节都必须给出、fields 原样保留、id 沿用/合并取最早/新建用分节前缀+数字）；精编规则分「规范格式」（一条一件事、时间锚点前置、全名与稳定称呼、禁止无指代代词、标点规范）与「提炼浓缩」（合并重复、删减冗余、保留关键、事件+意义、篇幅目标约原一半或更少、已精炼不强行压缩）；分节分工（memory 按时间线组织、relationships 只写关系状态不重述事件、personality 只留一个 MBTI）；六条红线（不新增、不删关键、不曲解原意、不改变视角、relationships 不写流水账、不硬塞无关事件）；附 memory 与 relationships 的「精编前/精编后」虚构示例示范浓缩程度。
- 档案卡片新增「✨ 精编」按钮（位于「🔮 分析本角色」与「✏️ 编辑」之间）：点击用「档案精编」提示词 + 该角色完整档案调用 AI（不携带剧情与世界书，只整理已有档案），解析返回的完整档案 JSON 后整体替换各分节；进行中按钮变为「⏹ 取消精编」可中断，失败变为「⚠️ 精编失败」可点击查看 AI 回复原文（复用分析失败弹层，标题按操作区分）。
- 精编全程有 toast 反馈：点击即提示「开始精编「角色名」…」，完成提示「「角色名」精编 X 处 / 无变化」，取消提示「已取消「角色名」精编」，失败提示错误信息。
- 档案工具栏在「N 个档案」右侧新增「✨ 精编全部档案」按钮：一键并发精编名单里所有角色（复用逐角色精编管线，每个角色独立 AbortController），进行中变为「⏹ 取消精编全部档案」可中断全部在途请求，完成后 toast 汇总成功/失败/取消/跳过数量；空名单提示「名单里还没有角色」。
- 精编按钮联动与「分析全部角色 / 分析本角色」一致：单个精编开始/结束/取消时同步刷新「精编全部档案」按钮（任一精编在途即显示「⏹ 取消精编全部档案」，点击中断全部在途请求），批量精编时各卡片按钮同步进入「⏹ 取消精编」态。
- toast 消息去掉方括号模块前缀，改为把模块名自然嵌入句中（标题仍为 [SoulLink]）：如「[分析]: 「蕾姆」更新 2 处」改为「「蕾姆」档案更新 2 处」、「[精编]: 「蕾姆」无变化」改为「「蕾姆」精编无变化」，其余模块同理（档案分析 / 档案精编 / 档案预筛 / 角色预筛 / 自动档案维护 / 角色推演 / API / 角色注册 / 正则 / 日志 / 预设），一眼可辨来源模块。
- 精编提示词强化 relationships 分节归属：只写「关系性质 + 关系现状/关键转折」，明确禁止写入具体事件过程（动作/对话/场景细节属于 memory）；从 relationships 删掉的事件细节先确认 memory 已有对应记录、没有则补入，避免精编把事件流水账留在人际关系里；红线与示例同步强化。
- 应用逻辑 `applyArchiveRefine`：按 id 找回旧条目以保留 source 溯源（合并条目沿用最早 id，其来源楼层随 id 保留）；分节缺失时保留原内容（不丢数据）；内容去重、空内容丢弃、重复 id 重新生成；档案为空时直接提示无需精编，不浪费 API 调用。
- 预设系统新增「档案精编」标签页（`PRESET_META` 注册，可编辑/保存/恢复默认，旧存档自动补齐默认文本）；构建脚本 `PROMPT_FILES` 与文档同步更新为五份提示词。
- 版本号升至 1.3.0（js/constants.js MODULE_VERSION / manifest.json / README.md）。
- 默认主题由「手绘涂鸦」改为「古风典雅」（DEFAULT_THEME = 'gufeng'，主题按钮初始文本同步更新）；未设置过主题的存档自动以古风典雅启动，已手动选择过主题的用户保持原选择。
- 精编失败根因修复：deepseek-v4-flash 等模型把全部输出预算花在思考上（finish_reason=length、正文为空）时，Ollama 风格响应把思考内容放在 `reasoning` 键而非 `reasoning_content`——兜底取用与「代理响应可用性」判断均兼容 `reasoning` 键名，避免误判代理损坏而回退直连（直连跨域失败导致「direct-after-proxy-fallback」报错）；同时精编请求输出预算从默认 8192 提升到 16384（ARCHIVE_REFINE_MAX_TOKENS），给「思考 + 完整档案 JSON」留足空间。
- 精编失败态调整：失败后「✨ 精编」按钮恢复可再次点击（不再被「⚠️ 精编失败」占用），失败详情改由状态胶囊（「待分析」那个 UI）承载——胶囊合并分析/精编状态，精编状态优先（busy/error/ok），error 时胶囊变红可点击查看对应失败详情（弹层标题按「分析/精编」区分）；

**验证**
- node build.js 构建通过，node --check index.js 语法通过；index.js 内 archiveRefine 提示词与 prompts/archiveRefine.txt 逐字一致；精编按钮/取消/失败态与预设标签页代码已随构建产物生效。

### v1.1.9 调整（2026-08-09）— 悬浮球统一 40px 圆形、浅色半透明

**背景**
- 需求：三套主题的悬浮球统一做小、统一圆形，颜色改浅，整体半透明；「古风典雅」的方形印章改回圆形。

**改动**
- 三套主题悬浮球统一为 40px 圆形（border-radius: 50%），图标字号统一 15px；
- 手绘涂鸦：56px 不规则贴纸 → 40px 圆形，图标色改为浅紫（#a78fd8），纸底改半透明（rgba(253,246,227,0.88)），描边/阴影减淡；
- 聊天气泡：QQ 蓝渐变改浅蓝半透明（rgba(126,211,255,0.92) → rgba(64,178,255,0.92)），阴影减淡；
- 古风典雅：56px 方形印章 → 40px 圆形，深红实底改为宣纸底半透明（rgba(247,241,227,0.9)）+ 浅朱砂描边与图标（#c2553f）；
- 版本号升至 1.1.9（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- style.css 三处悬浮球覆盖块均无残留旧尺寸/旧色值；三处 border-radius 均为 50%。

### v1.1.8 新增（2026-08-09）— 古风典雅主题

**背景**
- 需求：在主题切换机制上再新增一套「古风典雅」风格的主题，与手绘涂鸦、聊天气泡并列。

**改动**
- THEMES 新增 gufeng「古风典雅」条目，主题菜单现在有三个选项（手绘涂鸦 / 聊天气泡 / 古风典雅），选择后立即生效并持久化（settings.theme）；
- style.css 追加 [data-theme='gufeng'] 覆盖块：宣纸底（#f7f1e3）+ 墨色描边（#3b332a）+ 朱砂印章主色（#a63a2e）+ 鎏金点缀（#b8862f），悬浮球改为朱砂印章式圆角方块（去掉手绘抖动/旋转/虚线描边/不规则圆角），标题栏为墨色横批 + 鎏金底边，字体切换为楷体/宋体衬线（KaiTi / STKaiti / SimSun / FangSong）；
- 覆盖块统一用 :where([data-theme='gufeng']) 保持与基础规则同等的特异性，ok/error/busy 等 [data-state] 状态色仍由原规则生效；额外补了确认弹层「取消」按钮的纸色降级（避免被主按钮朱砂样式覆盖后文字看不清）；
- 版本号升至 1.1.8（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check index.js 通过；index.js 内 THEMES 与 js/ 源码逐字一致；style.css 无残留控制字符，gufeng 覆盖块 133 处选择器均挂在 [data-theme='gufeng'] 下。

### v1.1.7 新增（2026-08-09）— 聊天气泡主题

**背景**
- 需求：在主题切换机制上新增一套「聊天气泡」风格的主题。

**改动**
- THEMES 新增 qq「聊天气泡」条目，主题菜单现在有两个选项（手绘涂鸦 / 聊天气泡），选择后立即生效并持久化（settings.theme）；
- style.css 追加 [data-theme='qq'] 覆盖块：QQ 蓝渐变（#12b7f5 → #0aa5ff）标题栏与主按钮、浅灰底（#f7f8fa）+ 白色圆角卡片、悬浮球改为 QQ 蓝圆形按钮（去掉手绘抖动/旋转/虚线描边/不规则圆角），字体切换为系统无衬线（PingFang SC / Microsoft YaHei）；
- 覆盖块统一用 :where([data-theme='qq']) 保持与基础规则同等的特异性，ok/error/busy 等 [data-state] 状态色仍由原规则生效，不破坏状态语义；
- 版本号升至 1.1.7（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check index.js 通过；index.js 内 THEMES 与 js/ 源码逐字一致；style.css 无残留控制字符，qq 覆盖块 131 处选择器均挂在 [data-theme='qq'] 下。

### v1.1.6 新增（2026-08-09）— 主题切换入口

**背景**
- 需求：面板底部 GitHub 链接左侧加一个主题按钮，显示当前主题名，点击可展开菜单选择其他主题；目前只内置一个主题，先搭好机制。

**改动**
- 面板底部新增主题按钮（🎨 手绘涂鸦）与向上弹出的主题菜单，位于 GitHub 链接左侧；点击按钮展开/收起菜单，点击菜单项立即应用并持久化（settings.theme），点击外部或按 Esc 收起，关闭面板时自动收起；
- 主题机制：面板与悬浮球通过 data-theme 属性切换，style.css 默认变量即「手绘涂鸦」，后续新增主题只需在 THEMES 注册并在 style.css 追加 [data-theme='xxx'] 变量覆盖；
- 常量新增 THEME_ID / THEME_MENU_ID / DEFAULT_THEME / THEMES（当前仅 paper「手绘涂鸦」一项），DEFAULT_SETTINGS 新增 theme 键（默认 paper，旧存档自动补齐）；
- 版本号升至 1.1.6（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check index.js 通过；index.js 内主题按钮/菜单代码与 js/ 源码逐字一致。

### v1.1.5 新增（2026-08-09）— API 并发限制

**背景**
- 需求：程序会并发发起多个 AI 请求（档案分析 / 预筛 / 推演），部分 API 渠道并发能力有限，希望限制同时发送的请求数，多出的请求排队等待前面的完成后再发送，避免被限流或打挂渠道。

**改动**
- 「API 连接」页模型字段下方新增「限制并发」设置，默认开启，上限 3；关闭后不再排队；
- 并发上限输入框可调 1–10，失焦时自动夹取到合法区间，输入即持久化（settings.apiConcurrencyLimit）；
- 设置新增 apiConcurrencyEnabled（默认 true）与 apiConcurrencyLimit（默认 3）两个键；
- archive-analysis.js 新增并发信号量（apiConcurrencyState + acquireApiConcurrencySlot / releaseApiConcurrencySlot）：chatCompletion 发送前先占一个并发名额，整个任务（含自动重试）占用同一名额，结束或取消时立即释放；排队期间点「取消」会立即出队并抛 SoulLinkCancelError，不滞留队列；
- 排队时 toast 会实时显示排队数量：「AI 请求已达并发上限（N），当前 X 个请求正在排队等待」——队列增长/减少时原地更新同一条 toast（拿到 DOM 引用，不重复弹窗），队列清空后自动关闭；宿主 toastr 不返回元素时退化为「每次排队只提示一次」，不刷屏；排队期间日志系统输出 debug 级「API 并发已满，请求排队等待」（含上限与队列长度）；
- 版本号升至 1.1.5（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- 信号量单测覆盖：并发占满后第 N+1 个请求排队、释放后按 FIFO 放行、排队中取消即出队且不泄漏名额、关闭/上限 < 1 不排队、预取消信号立即抛错、监听器清理；
- node --check index.js 通过；index.js 内并发限制代码与 js/ 源码逐字一致。

### v1.1.4 调整（2026-08-09）— 首页「日志系统」与「角色扮演」入口对调

**背景**
- 用户反馈：Home 页「日志系统」占大卡片位置，而「上一轮角色扮演」只是欢迎语右侧的小图标，主次与使用频率不匹配，希望两者对调。

**改动**
- 首页欢迎语右侧小图标改为「日志系统」（soullink-home__mini，点击打开日志视图），原大卡片移除；
- 原小图标（上一轮角色扮演）升级为大卡片，功能名改为「角色扮演」（soullink-home__card--round，黄色面具图标），保留右上角状态徽章，并新增状态行（暂无记录 / 已注入 · N 个角色等，复用 describeRoundOutcome）；
- 面板标题与日志文案同步：角色扮演视图标题由「上一轮角色扮演」改为「角色扮演」；
- 常量与样式重命名：HOME_LOG_CARD_ID→HOME_LOG_ICON_ID、HOME_ROUND_ID→HOME_ROUND_CARD_ID、.soullink-home__round→.soullink-home__mini、.soullink-home__round-badge→.soullink-home__card-badge；移除不再使用的 HOME_LOG_STATUS_ID 及 views-log.js 中对应更新块；
- 版本号升至 1.1.4（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check index.js 通过；index.js 内首页卡片/小图标/面板标题与 js/ 源码逐字一致。

### v1.1.3 优化（2026-08-09）— 注入块强化：<NPC_Deduction> 改为「系统导演注」强制指令

**背景**
- 用户反馈：注入格式还是不够强势，部分模型不搭理推演结果。逐项核对旧写法的问题：
  - 首行「请把这些状态当作…务必遵循」是请求语气，模型可当作建议忽略；
  - 「若独白与既定设定或剧情事实冲突，以设定与剧情为准」给主模型留了整体弃用的合法出口——任何轻微冲突都能被用来跳过本块；
  - 全部规则挤在一行长文里，不醒目；且只要求「内化」，没有要求推演意图必须外显到回复中。

**改动**
- `buildNpcDeductionInjectionText`：首行改为「【系统导演注 · 强制指令】」，并声明本块「不是剧情正文，也不是任何角色的台词、旁白或可引用文本」，语气改为「必须逐条执行，不得忽略、不得淡化，不得以任何理由整体弃用」；
- 新增「执行规则」六条：
  1. 直接驱动：行动与对白必须由独白中的心情、动机与「下一步行动」直接驱动并真实落地，禁止敷衍、无关日常、观望等待；
  2. 必须外显：行动意图要通过动作、神态、语气、话术或选择体现，每个注入角色至少一个意图落地，漏演视为不合格；
  3. 禁止引用：不得复述、转述或让角色念出本段任何原文；
  4. 角色隔离：每个角色的独白只属于 TA 自己，不得张冠李戴；
  5. 冲突裁决：与档案硬设定或已发生剧情冲突时保留情绪与行动意图、改用符合设定与剧情的方式表达——堵住「以设定不符为由跳过」的漏洞，同时保留不破坏既定事实的护栏；
  6. 优先级：在接下来的剧情中，与一般印象或惯性理解冲突时以本段为准；
- 重建 `index.js`（`node build.js`）。

**验证**
- `node --check index.js` 通过；`index.js` 内注入块新文本与 `js/archive-analysis.js` 逐字一致。

### v1.1.2 优化（2026-08-09）— 角色扮演提示词：行动判据收敛为「局面变化」

**背景**
- 用户反馈一：示例一（杨知世）的独白以「等开学典礼一结束，我就找个由头跟在天晓身边……」收尾——行动被推迟到未来事件，不是当前行动；独白只注入当前场景的 SillyTavern 提示词，等开学典礼真正发生时主模型早已读不到这段，等于无效提示词。
- 用户反馈二：测试独白（世子妃回应丈夫的逗弄）中「轻轻戳他一下、靠进他怀里、等他先来哄我」虽是主动行为，但局面没有变化——他逗我、我娇嗔，还是同一套互动，对白和行为性质没有明显改变，是废戏。
- 用户反馈三：提示词中「主动」「被动」字眼过多，AI 误以为核心是「主动做出动作」，而不是「使局面发生变化」——局面不发生变化才是废戏，被动只是其中一种。

**改动**
- `prompts/roleplaySystem.txt`【叙事理论】→【行为理论】重写：A 做动作、B 做反应是基本行为往返；B 的反应不能只是接受、确认或重复；当 B 的行为使当前局面的性质发生明显变化时，才真正形成新节拍；角色必须采取会影响后续剧情的行动，使局面发生变化；
- 【输出】·下一步行动收敛为三条：① 行动必须改变当前剧情状态（对白和行为性质明显改变、对方需要重新接招、场景推进到新阶段；局面没变就是废戏），禁止空泛意图；② 行动必须落在当前场景、此刻就能执行；③ 行为必须围绕当前剧情核心；
- 全篇清除「行动必须主动」「被动接招」等误导性措辞，判据统一为「局面变化」；示例一标签改为「示范信息差与局面变化」；
- 【示例】拆分为【正确示例】（示例一/二）与【错误示例】⚠️（示例三）：世子妃「娇嗔、靠怀里、等他来哄」——他逗我、我娇嗔，还是同一套互动，局面没有变化，是废戏；错误示例区注明禁止模仿；
- 示例一重写：杨知世主动叫住松本、拉到队伍边上直接问话，条件分支保留为同场景备案；
- 【角色沉浸要求】新增第 5 条：角色行为必须围绕当前剧情核心；
- 重建 `index.js`（`node build.js`，已还原 `js/prompts.generated.js` / `js/prompts.previous.generated.js` 保持干净 diff）。

**验证**
- `node --check index.js` 通过；`index.js` 内嵌默认与 `prompts/roleplaySystem.txt` 逐字一致（旧文本仅存于历史快照）。

### v1.1.1 修复（2026-08-09）— 标准 SillyTavern 下角色卡绑定世界书显示不出来

**背景**
- 用户反馈：在标准 SillyTavern 环境里，世界书视图只能加载到已激活的全局世界书，角色卡本身绑定的世界书（角色主世界书）显示不出来；TauriTavern 环境正常。

**根因**
- 标准 SillyTavern 的 `ctx.characterId` 是字符串（`setCharacterId` 统一 `String(value)`），TauriTavern 是数字；`getActiveWorldBookNames` 用 `Number.isInteger(ctx?.characterId)` 判断，字符串 ID 永远不通过，角色主世界书收集不到。
- 标准 SillyTavern 没有全局 `STscript` 函数，`/getcharbook` 兜底分支也失效。

**改动**
- `js/worldbook.js` `getActiveWorldBookNames`：`characterId` 兼容数字与数字字符串（`Number.isInteger` 或 `/^\d+$/`），取角色用 `ctx.characters[Number(characterId)]`；
- `/getcharbook` 兜底：保留 `globalThis.STscript` 分支（TauriTavern），新增 `ctx.executeSlashCommandsWithOptions('/getcharbook')` 分支（标准 ST 的 ctx 暴露此函数），读取 `result?.pipe` 且仅接受 string/number；
- 重建 `index.js`（`node build.js`，本机 CRLF 工作区会重写生成文件，已还原 `js/prompts.generated.js` / `js/prompts.previous.generated.js` 保持干净 diff）。

**验证**
- 标准 SillyTavern 实测：选中「我的青春恋爱物语果然有问题」（`characterId: "10"`，角色卡绑定同名世界书），世界书视图显示「酒馆规则生效 · 1 本书 · 0 条排除」，31 条条目正常显示（含常驻/深度注入/AN 底部/注入前等徽章）；
- `ctx.executeSlashCommandsWithOptions('/getcharbook')` 实测返回 `pipe: "我的青春恋爱物语果然有问题"`；
- `node --check index.js` 通过；TauriTavern 的 `STscript` 分支与数字 `characterId` 路径保留，原环境不受影响。

### v1.1.0 — 默认提示词外挂化 + 一键构建（prompts/*.txt + build.bat）

**背景**
- 迭代默认提示词每次都要动 JS 源码并人工同步迁移快照，改一次忘一次。
- 用户诉求：默认提示词改为四份外挂文本文件，改文件即改默认；并提供一键构建脚本。

**改动（v1.1.0）**
- **默认提示词外挂化**：四份默认提示词提取到 `prompts/`（`archiveSystem.txt` / `archivePreScreen.txt` /
  `roleplaySystem.txt` / `roleplayPreScreen.txt`），不再内联在 JS 源码里；`build.js` 构建时读取生成
  `js/prompts.generated.js`（`DEFAULT_PROMPTS`）并拼入 `index.js`；
- **历史默认自动快照与迁移**：每次构建把覆盖前的旧默认累积快照进 `js/prompts.previous.generated.js`；
  `getSettings` 检测到保存文本命中任一历史默认（未自定义）自动升级到当前 `prompts/*.txt` 内容——
  本地迭代改提示词无需再改迁移代码；
- **一键构建**：新增 `build.bat`（双击运行 `node build.js`，自动检测 Node、成功/失败提示）；
  `build.js --watch` 支持监听 `prompts/` 与 `js/` 自动重建；
- **运行时外挂尝试与废弃**：曾尝试启动时 fetch `prompts/*.txt` 免构建生效，在 TauriTavern 宿主实测不生效
  （无报错但未覆盖），已回退为「改 txt → 构建 → 重启」的确定性工作流（见下一条记录）；
- 版本号升至 1.1.0（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- `node build.js` 重建通过；`node --check index.js` 语法通过；`build.bat` 实测可运行；
- 迁移模拟：用本机真实存档验证四个提示词均正确迁移到当前 txt 内容，幂等不循环，自定义文本不受影响；
- 生成物（`js/prompts.generated.js`、`js/prompts.previous.generated.js`、`index.js`）与 `prompts/*.txt` 逐字一致。

### 运行时外挂方案废弃：改回手动构建 + 一键 build.bat

**背景**
- 运行时外挂（启动时 fetch prompts/*.txt 覆盖默认）在 TauriTavern 宿主里实测不生效：
  改 txt 后重启酒馆默认提示词仍是旧内容，且无报错（脚本地址解析或 fetch 静默失败）。
- 用户决定放弃运行时外挂，改为「改 txt → 手动构建 → 重启」的确定性工作流。

**改动**
- 删除 `js/prompts-runtime.js`；`js/main.js` 移除 `await ensureRuntimePromptOverrides()`；
  `build.js` 移除 `js/prompts-runtime.js` 拼接项，`DEFAULT_PROMPTS` 恢复 `Object.freeze`；
- 新增 `build.bat`：双击即运行 `node build.js`（自动切到脚本目录、检测 Node、成功/失败提示），
  改完 `prompts/*.txt` 双击它再重启酒馆即可。

**验证**
- `node build.js` 重建通过；`node --check index.js` 通过；index.js 无运行时外挂残留；
- 历史默认快照与自动迁移逻辑保持不变：用本机真实存档模拟，四个提示词均正确迁移到当前 txt 内容，
  幂等不循环，自定义文本不受影响。

### 默认提示词运行时外挂：改 txt 重启即生效（无需构建）

**背景**
- 用户反馈：改了 `prompts/archiveSystem.txt` 但重启酒馆后默认提示词没变。
- 根因：宿主只在加载时读一次 `index.js`，改 txt 不重新构建就永远不会生效；且用户已保存的旧默认
  迁移到的是构建内嵌的旧默认，看起来「改了没用」。

**改动**
- 新增 `js/prompts-runtime.js`：顶层捕获 `document.currentScript.src` 得到扩展目录 URL，
  `ensureRuntimePromptOverrides()` 在 `bootstrap()` 初始化面板/订阅之前 fetch
  `prompts/*.txt`（`cache: 'no-store'`）覆盖 `DEFAULT_PROMPTS`（3 秒超时，失败回退内嵌默认）；
- `build.js`：`DEFAULT_PROMPTS` 不再冻结（运行时覆盖需要），`FILES` 在
  `js/prompts.previous.generated.js` 之后、`js/constants.js` 之前插入 `js/prompts-runtime.js`；
- `js/main.js` `bootstrap()`：在 `installHostEventSubscriptions` 之前
  `await ensureRuntimePromptOverrides()`，保证 `getSettings` 迁移读到的是覆盖后的默认。

**验证**
- `node build.js` 重建通过；`node --check index.js` 通过；
- vm 沙箱模拟：fetch 到 txt（4/4）→ `DEFAULT_PROMPTS.archiveSystem` 变为 txt 内容(3501)；
  用本机真实存档模拟 saved(3507) → 自动迁移到 txt(3501)，其余提示词不动，幂等不循环，自定义文本不受影响；
- fetch 失败回退：内嵌默认保持不变，行为不变。

### build.js 增加 --watch 自动构建模式

**背景**
- 外挂化 + 自动迁移落地后，改提示词仍需手动跑 `node build.js`，迭代时容易忘。

**改动**
- `node build.js --watch`：常驻监听 `prompts/` 与 `js/`，文件变更（防抖 200ms）自动重建
  （含历史默认快照与 index.js），产物文件名过滤避免回环；Ctrl+C 退出。

**验证**
- 后台启动 watch → 改动 roleplaySystem.txt → index.js 自动重建且包含新内容；恢复文件后状态无残留。

### 默认提示词迭代零操作化：历史默认自动快照与迁移（PREVIOUS_DEFAULT_PROMPTS）

**背景**
- 外挂化后每次改 `prompts/*.txt` 仍需手工加 `LEGACY_DEFAULT_*_Vn` 快照并改迁移条件，
  否则「已保存过旧默认、未自定义」的用户（包括开发者本机）不会自动升级到新默认——改一次要动一次代码。

**改动**
- `build.js`：每次构建把覆盖前的旧 `DEFAULT_PROMPTS` 累积快照进 `js/prompts.previous.generated.js`
  （`PREVIOUS_DEFAULT_PROMPTS`，每个 key 一个历史文本列表，去重；兼容旧单值格式）；
- `js/utils.js` `getSettings`：新增通用迁移分支——保存文本命中任一历史默认（且不等于当前默认）
  即视为未自定义，自动升级到当前 `prompts/*.txt` 内容；LEGACY 快照分支保持优先、行为不变；
- 迁移日志文案随 key 输出，便于定位。

**验证**
- `node build.js` 重建通过；`node --check index.js` 通过；
- 用本机真实存档模拟：saved(第一次编辑 3603) → 自动迁移到当前 txt(第二次编辑 3599)，其余提示词不动；
- 连续两次「改 txt → build」未重启：历史列表累积为 [3603, 3599, 第三次编辑]，
  saved(第三次编辑) 仍能正确迁移到第四次编辑；
- 自定义文本（不匹配任何历史/LEGACY）不动；已是新默认时幂等不迁移不保存。

**经验（改默认提示词）**
- 本地迭代：改 `prompts/*.txt` → `node build.js` → 重启，无需动任何代码；
- 发布新版本：历史列表只覆盖本地构建链，需把该版本默认快照成 `LEGACY_DEFAULT_*_Vn`
  并加入 `getSettings` 迁移条件，旧版本用户才会自动升级。

### 默认提示词外挂化补充：迁移快照（roleplaySystem V8）

**背景**
- 外挂化后直接改 `prompts/roleplaySystem.txt` 并重启酒馆，默认提示词仍是旧文本。两个原因：
  1. 改完 txt 没跑 `node build.js`——宿主加载的 `index.js` 仍是旧内容；
  2. 即使重新构建，运行时用的是宿主已保存的设置（`settings.json → extension_settings.SoulLink.prompts`），
     `getSettings` 只会在「保存文本与某个 `LEGACY_DEFAULT_*` 快照逐字一致」时迁移到新默认；
     v1.0.18 已发布默认（3706 字）不在快照列表里，因此不会自动升级。

**改动**
- 新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V8` 快照（= v1.0.18 已发布默认），`getSettings` 迁移条件纳入 V8；
- 角色扮演迁移日志文案改为中性表述「已自动升级为新默认（当前 prompts/roleplaySystem.txt 内容）」，不再写死 v1.0.16。

**验证**
- `node build.js` 重建通过；`node --check index.js` 通过；
- 用本机真实保存设置模拟 `getSettings`：`roleplaySystem` 由旧默认迁移为新 txt 内容（逐字一致），
  其余三个提示词不动；二次调用幂等（不再迁移、不会循环）。

**经验（改默认提示词必读）**
- 改 `prompts/*.txt` 后必须跑 `node build.js`；
- 要让「已保存过旧默认、未自定义」的存量用户自动升级到新默认，需把旧默认文本快照成
  `LEGACY_DEFAULT_*_Vn` 常量并加入 `getSettings` 迁移条件；否则新文本只对全新安装与「恢复默认」生效。

### 默认提示词外挂化（prompts/ 目录 + build.js 生成）

**背景**
- 默认提示词此前内联在 `js/constants.js` 的 `DEFAULT_PROMPTS` 模板字符串里，改默认提示词要在大段 JS 中翻找，且容易误伤 `LEGACY_DEFAULT_*` 迁移快照。

**改动**
- 新增 `prompts/` 目录：四个子系统的默认提示词各一个纯文本文件（`archiveSystem` / `archivePreScreen` / `roleplaySystem` / `roleplayPreScreen`）；
- `build.js` 构建时读取 `prompts/*.txt`，生成 `js/prompts.generated.js`（`DEFAULT_PROMPTS` 定义），并排在 `constants.js` 之前拼入 `index.js`；
- `js/constants.js` 移除内联 `DEFAULT_PROMPTS`（仅保留 `LEGACY_DEFAULT_*` 迁移快照）；
- 改默认提示词工作流：编辑 `prompts/*.txt` → `node build.js` → 重新加载扩展。

**验证**
- `node build.js` 重建通过；`node --check index.js` 语法通过；
- 生成的 `DEFAULT_PROMPTS` 与 `prompts/*.txt` 逐字一致（四个 key 全部匹配）；
- `DEFAULT_SETTINGS.prompts` 仍正确引用 `DEFAULT_PROMPTS`；迁移逻辑与「恢复默认」行为不变。

### v1.0.18 — 「角色预筛」默认提示词模块化架构重构（任务→输入→推演→判定标准→输出→质量红线）

**背景**
- 以「一个真正的人能否看懂每个部分在表达什么」为标准走查「角色预筛」默认提示词（v1.0.16 已发布默认），
  发现结构问题：候选集规则（唯一候选集 / 名单外一律不列入）在开场、输出契约、输入说明、判断要点出现 4 次且措辞各异；
  「最快返回 / 回复越短越好」出现 3 次；输出契约「先读这里」前置（与执行顺序颠倒）；
  判断要点层级混乱——「被直接点名包括…」独立条目插在「必须列入 / 应列入」之间、在场硬前提与「不列入」的「不在场」重复表述；
  排序上限与偏差修正（输出治理规则）混在判定规则里；「在场从严 / 戏份从宽」两个偏差维度混在一条里。

**改动（v1.0.18）**
- 《提示词架构.md》新增第十节：角色预筛现状拆解（4 个模块的职责/约束目标/关系）、问题诊断、新架构、新旧模块映射、迁移方案；
- 默认「角色预筛」提示词按新架构重写（1471 字，与旧版 1446 字相当但结构更清晰）：
  - 【任务】：身份 / 职责定义（末条用户消息引发的下一轮剧情中会开口、行动或重要内心反应）/ 产物用途（供后续逐个推演）/ 高频轻量必须最快返回（唯一权威表述，不再重复）；
  - 【输入】前移为第二模块：<Registered_Characters>（唯一候选集，名单外一律不列入，候选集规则收敛到此）/ <Recent_Messages>（唯一判断依据 + 触发点 + 开放式场景引导）；
  - 【推演】：逐角色三步（在场判定 → 戏份判定 → 归类），在场推断规则（未明确去向按最近一次出场）从判断要点移入第 1 步；
  - 【判定标准】：三层分类表独立成模块（必须列入含动作对象 / 应列入 / 不列入），动作对象补充并入「必须列入」；
  - 【输出】：唯一「最高优先级」契约——仅 JSON / 结构 / 名字逐字 / 空数组 / 戏份排序与 6 个上限（排序上限从判断要点移入）；
  - 【质量红线】：两条偏差底线分维度表述（在场判定从严：宁可漏选不可错选不在场；戏份判定从宽：拿不准倾向列入）；
- 迁移：新增 LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V5 快照（= v1.0.16 已发布默认，即 v1.0.14 文本仅「裁判」改「员」），
  getSettings 迁移条件纳入 V5；版本号升至 1.0.18（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js；
- 迁移模拟：V5 快照与 v1.0.16 默认逐字一致；v1.0.16 默认 → v1.0.18 新默认；V1–V4 旧默认 → 新默认；自定义文本不动；新默认零迁移零保存（不循环）；
- 一致性检查：index.js 与 js/constants.js 的 roleplayPreScreen 逐字一致；新默认模块顺序（任务/输入/推演/判定标准/输出/质量红线）与《提示词架构.md》第十节一致；「最高优先级」「唯一候选集」「最快返回」各出现一次。

### v1.0.17 — 「档案预筛」默认提示词模块化架构重构（任务→输入→判断→输出）

**背景**
- 以「一个真正的人能否看懂每个部分在表达什么」为标准走查「档案预筛」默认提示词（v1.0.16 已发布默认，1085 字），
  发现结构问题：候选集规则（名单外一律不选 / 不得列入 / 一律忽略）在开场、输出契约、输入说明三处重复且措辞各异；
  「名单外的角色不得列入」是输入域规则却混进输出格式契约；「值得记录的新信息/新经历/关系背景」是入选标准却写在输入说明里；
  「最快返回…回复越短越好」双写；判断要点三条「必须列入」主语句式重复；
  「值得记录」未点明与下游档案写入触发点（fields/memory/worldview/relationships/family）的对应关系。

**改动（v1.0.17）**
- 默认「档案预筛」提示词按《提示词架构.md》框架重写（985 字，较旧版短 100 字）：
  - 【任务】：身份 / 职责定义（值得写入档案的新信息、新经历，或关系、背景的新内容）/ 唯一产出（JSON 名单，交给档案系统决定本轮更新哪些角色）/ 高频轻量必须最快返回；
  - 【输入】前移为第二模块：<Registered_Characters>（唯一候选集，名单外一律忽略，候选集规则收敛到此）/ <Recent_Messages>（唯一判断依据，不推测消息之外的情况）；
  - 【判断】：三条「必须列入」压缩为编号入选标准 1-3（出场并参与互动 / 在场获知重要信息 / 自身信息被揭示），与下游档案写入点对齐；「不列入」两条独立成组；「拿不准时」改为直接引用条件编号（条件 1/2 倾向列入，漏选会让新信息错过入库）；
  - 【输出】：唯一「最高优先级」契约——仅 JSON / 结构 / 名字逐字 / 空名单；删除「名单外不得列入」与「回复越短越好」重复；
- PRESET_META 中「档案预筛」描述同步为「判断本轮哪些已注册角色获得了值得写入档案的新信息、新经历」；
- 迁移：新增 LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V7 快照（= v1.0.16 默认文本，与 V6 文本不同、各自独立迁移），getSettings 迁移条件纳入 V7；
  版本号升至 1.0.17（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js；
- 迁移模拟：V7 快照与 v1.0.16 默认逐字一致；V1–V7 旧默认各自 → 新默认；自定义文本不动；新默认零迁移零保存（不循环）；
- 一致性检查：index.js 与 js/constants.js 的 archivePreScreen 逐字一致；新默认模块顺序（任务/输入/判断/输出）与《提示词架构.md》第九节一致；「最高优先级」「唯一候选集」「最快返回」各出现一次。

### v1.0.16 — 「角色扮演」默认提示词模块化架构重构（任务→输入→推演→动机参考→输出→红线→示例）

**背景**
- 以「一个真正的人能否看懂每个部分在表达什么」为标准走查「角色扮演」默认提示词，发现结构问题：
  输入说明放在全文最后（与实际执行顺序相反）、输出契约与口吻要求双重「最高优先级」、
  输出契约提前引用后文才定义的三要素/认知状态概念、推演第 3 步引用的 <npc_behavior> 动机库
  被放在示例之后（离引用点最远）、同一禁令（要点清单/分析报告/总结腔）出现 3 次、
  信息盲区的「信息边界」与「表现要求」两类职责混在同一模块。

**改动（v1.0.16）**
- 新增《提示词架构.md》：现状拆解（10 个模块的职责/约束目标/关系）、问题诊断、
  通用框架（任务→输入→推演→参考→输出→红线→示例 + 单一职责/定义先于引用/一个事实只写一次/单一最高优先级）、
  新旧模块映射表、可维护性指南、对其他三份预设提示词的适用性映射；
- 默认「角色扮演」提示词按新架构重写（3701 字，较旧版 3857 字更短）：
  - 【任务】：身份/任务/产物用途/直接给结论（不再重复输出契约内容）；
  - 【输入】前移为第二模块：角色名 / <Character_Profile>（worldview、memory 用法与信息边界）/ <Recent_Messages>（在场判定 + 触发点）；
  - 【推演】：六步内心过程保留，注明「不写入回复」；「行动倾向」步只留思考引导，「对象/方式/时机至少说清两样」收敛到【输出】为唯一权威表述；
  - 【动机参考】：<npc_behavior> 动机库原样保留并补模块身份说明（参考知识，不是输入也不是输出），位置移到推演之后，消除「下方」交叉引用；
  - 【输出】：唯一「最高优先级」契约——JSON 结构 + character 逐字 + 内容三要素 + 两种认知状态 + 口吻 + 只表达自己；三组要求分层列举；
  - 【质量红线】：保留四条，删除与口吻重复的表述（总结腔只在此处，带具体例句）；
  - 【示例】：两个示例原样保留；
- 迁移：新增 LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V7 快照（= v1.0.13 已发布默认），getSettings 迁移条件纳入 V7；
  版本号升至 1.0.16（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js；
- 迁移模拟：V7 快照与 v1.0.13 默认逐字一致；v1.0.13 默认 → v1.0.16 新默认；V1–V6 旧默认 → 新默认；自定义文本不动；新默认零迁移零保存（不循环）；
- 一致性检查：index.js 与 js/constants.js 的 roleplaySystem 逐字一致；新默认模块顺序（任务/输入/推演/动机参考/输出/红线/示例）与《提示词架构.md》一致。

### v1.0.15 — 世界书视图新增「折叠本书」按钮

**背景**
- 世界书视图按书分组展示条目，激活多本书时列表很长，用户希望把暂时不看的书折叠起来，只保留书名，减少滚动与视觉噪音。

**改动（v1.0.15）**
- 每本书的标题栏右侧新增折叠按钮（▾/▸）：点击折叠后隐藏该书的搜索框与全部条目，只显示书名与条目数；再点展开恢复。
- 折叠状态按书名单独记忆（worldBookCollapsedBooks），刷新 / 排除勾选等整体重建列表后不丢；书不再激活时自动清理其折叠状态。
- 折叠时隐藏标题栏虚线分隔线并收紧内边距，视觉上更像一个可点击的书签条；按钮带 aria-expanded 与悬停手绘风格反馈。
- 版本号升至 1.0.15（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js（6406 行）；
- 冒烟测试 22 项全部通过（最小 DOM stub + vm）：折叠按钮渲染、点击折叠/展开、▾/▸ 与 aria-expanded 切换、重渲染后状态保留、书停用后状态清理。

### v1.0.14 — 角色预筛 Gate 措辞修正（候选集 = 已注册名单，对话只作判断依据）

**背景**
- 走查预筛阶段实际发送的内容（system 预筛提示词 + `<Registered_Characters>` 名单块 + `<Recent_Messages>` 剧情块 + 输出契约段）后确认：
  候选集是固定的已注册名单，对话只用于判断名单中的角色谁有戏份；但提示词里「判断每轮实际有谁在场、有谁被点名、有谁该回应」
  等措辞读起来像「从对话中找角色」，可能让模型把注意力放在对话内容上、甚至把名单之外的角色当作候选。

**改动（v1.0.14）**
- 提示词（默认「角色预筛」）：
  - 输入说明明确「<Registered_Characters> 是唯一的候选集：只从这份名单中挑选，名单之外的角色（即使出现在对话里）一律不列入」；
  - 剧情块说明改为「只用于判断名单中的角色：逐个判断 TA 是否在场、是否被点名、是否有戏份，不要从对话中寻找名单之外的角色」；
  - 触发点说明改为「优先判断它点名、涉及或会波及名单中的哪些角色；其余消息用于确认名单中的角色谁还在场」；
  - 判断要点新增首条「以下判断只针对名单中的角色：逐个过一遍 <Registered_Characters>，判断每个角色本轮是否有戏份，而不是从对话里找角色」；
- Gate 引导消息同步：名单段「这是唯一的候选集，只从这份名单中挑选：名单之外的角色（即使出现在对话里）一律不列入」；
  剧情段「请据此逐个判断名单中的角色」；输出契约段「只从名单中列出本轮会开口、行动或有重要内心反应的角色」；
- 版本号升至 1.0.14（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js（6376 行）；
- 迁移模拟：V1–V4 旧默认 → 新默认；新默认零迁移零保存（不循环）；自定义文本不动；
- 一致性检查：index.js 与 js/constants.js 的 roleplayPreScreen 逐字一致；Gate 引导消息 / 输出契约段 / 迁移日志三处同步。

### v1.0.13 — 角色扮演默认提示词措辞收尾修正（术语统一 / 去重复 / 示例去总结腔 / 触发点明确）

**背景**
- 发布前对「角色扮演」默认提示词做整份措辞走查，发现 6 处问题：
  开场「行为倾向」与推演流程「行动倾向」术语不统一；「行动倾向」与「形成假设」重复要求解读玩家行为；
  三要素动作列表同一句内 TA/他混用；「悲喜情绪」作为情境类别混入情绪名词；
  示例一结尾「下一步行动，我打算……」是书面总结腔，与「不写总结腔」质量红线直接矛盾；
  输入说明未点明最后一条消息是玩家言行、本次推演的触发点（与角色预筛 Gate 的「末条用户消息是触发点」不对齐）。

**改动（v1.0.13）**
- 开场：「行为倾向」→「行动倾向」（与推演流程术语统一）；「解读、情绪」→「情绪反应、解读」（与三要素心情→想法→行动顺序对齐）；
- 情境定性：「悲喜情绪」→「悲喜时刻」（情境类别里混入情绪名词）；
- 行动倾向：删掉「先解读 TA 刚才那个行为意味着什么，再」（形成假设已做解读，避免重复）；条件分支句式「他要是……我就……」→「TA 要是……我就……」；
- 三要素-下一步行动：动作列表「跟上他、先按兵不动等他开口」→「跟上 TA、先按兵不动等 TA 开口」（同一句内 TA/他混用，统一为 TA）；
- 示例一：结尾「下一步行动，我打算不动声色地跟着天晓一起走……」改为「等开学典礼一结束，我就找个由头跟在天晓身边，松本要是来找他说那件事，我就在旁边听着，先弄清楚到底是什么事再说」——行动意图自然融入心流，消除与「不写总结腔」红线的矛盾，同时保留条件分支示范；
- 输入说明：<Recent_Messages> 条目追加「其中最后一条通常是玩家刚做出的言行，是本次推演的触发点——独白围绕对它的解读与回应展开」；
- 迁移：新增 LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V6 快照（= v1.0.12 已发布默认），getSettings 迁移条件纳入 V6；版本号升至 1.0.13（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js；
- 迁移模拟：V6 快照与 v1.0.12 默认逐字一致；v1.0.12 默认 → v1.0.13 新默认；V1–V5 旧默认 → 新默认；自定义文本不动；新默认零迁移零保存。

### v1.0.12 — 角色预筛 Gate 提速优化（最快返回 + 请求体精简）+ 措辞微调

**背景**
- 用户要求「重点是快，而且不要输出多余的东西」：角色扮演预筛 Gate 是发送前阻塞链路上的高频轻量调用（每次发送都触发），
  但默认提示词未把「快」作为第一优先级；请求体 6 条消息、JSON 带缩进，输入体积与消息轮次有压缩空间
  （v1.0.9 已对档案预筛 Gate 做过同样优化，角色扮演 Gate 未同步）。

**改动（v1.0.12）**
- 提示词：开场追加「这是高频轻量调用，必须最快返回：不要输出分析过程、不要解释、不要任何多余文字，回复越短越好」；
  输出契约首条追加「回复越短越好」——不增加篇幅，只强化速度语义；
- 请求体（buildNpcDeductionGateMessages）：6 条消息精简为 4 条（名单引导与名单块合并、剧情引导与剧情块合并），
  名单与剧情 JSON 改为紧凑序列化（去掉缩进），减少消息轮次与输入 token；
- 措辞微调（评估整份提示词后）：「在场是硬性前提」的「比漏选更伤」改为「宁可漏选也不可错选不在场角色」，
  「拿不准时倾向列入」改为「拿不准是否有戏份时倾向列入」——消除「漏选」一词两处出现的维度混淆
  （错选不在场 > 漏选有戏份 > 多选浪费），并限定「拿不准」的适用范围与在场硬前提不冲突；
- 版本号升至 1.0.12（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js（6369 行）；
- 迁移模拟：V1–V4 旧默认 → 新默认；新默认零迁移零保存（不循环）；自定义文本不动；
- 一致性检查：index.js 与 js/constants.js 的 roleplayPreScreen 逐字一致；请求体 4 条消息 + JSON 紧凑序列化确认。

### v1.0.11 — 角色预筛 Gate PM 审查迭代（排序判据 / 开放式场景 / 在场硬前提 / 参数对齐 / 原文可见性）

**背景（PM 视角审查）**
- 以产品视角走查「角色预筛 Gate → 并发推演 → <NPC_Deduction> 注入 → 主模型」全链路，评估不同剧情场景下的表现后判定：
  - 「按戏份轻重排序，最多保留 6 个」缺少排序判据——模型自由发挥，可能把「刚开口的」排前面而漏掉「情绪积累要爆发的」；
  - 最后一条用户消息未点名任何人（环境描写 / 沉默 / 开放式动作）时缺乏引导——模型可能退化为「只选明显开口的」或「全选」；
  - 「拿不准时倾向列入」可能被误用于「拿不准是否在场」——不在场角色获得独白会误导主模型（把 TA 拉进场景），比漏选更伤；
  - 角色扮演 Gate 未像自动档案 Gate（v1.0.9）那样限制 maxTokens / 降低 temperature——它在发送前阻塞链路上，更应控制输出；
  - Gate 0 入选 / 漏选时用户不可审计——「上一轮角色扮演」视图不展示 Gate 原始返回。

**改动（v1.0.11）**
- 提示词（默认「角色预筛」）：
  - 输入说明新增开放式场景引导：「若最后一条用户消息没有点名或涉及任何角色（如环境描写、沉默、开放式动作），则以在场角色的情绪积累、未了心事与行动意图为准，选出最可能回应或行动的角色；在场且能感知该事件的角色视为受影响」；
  - 判断要点新增「在场是硬性前提」：「明确不在场（已离开、在别处、被提及在远方）的角色不列入；最近消息未明确其去向时，按最近一次出场推断——最近一次出场在场景中且无离开记录，视为仍在场。不在场角色获得独白会误导主模型，比漏选更伤」；
  - 排序判据明确化：「戏份轻重依次看：与最后一条用户消息的直接关联（被点名/被涉及/直接互动）→ 情绪积累与行动意图的强度 → 在场互动频率」；
- Gate 剧情段引导消息同步补充「若最后一条用户消息未点名任何人，以在场角色的情绪积累与行动意图为准」；
- 调用参数对齐：角色扮演 Gate 单独传 maxTokens: 1024 与 temperature: 0.1（与自动档案 Gate v1.0.9 一致），
  避免模型长篇输出拖慢发送前阻塞链路，同时保持判定确定性；
- 原文可见性：「上一轮角色扮演」视图新增「预筛原文（Gate 返回）」区块（截断 400 字），
  0 入选 / 漏选时可审计模型到底返回了什么；
- 版本号升至 1.0.11（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js（6373 行）；
- 迁移模拟：V1–V4 旧默认 → 新默认；新默认零迁移零保存（不循环）；自定义文本不动；
- 一致性检查：index.js 与 js/constants.js 的 roleplayPreScreen 逐字一致；Gate 引导消息 / 输出契约段 / 迁移日志三处同步。

### v1.0.10 — 角色扮演默认提示词措辞修正（玩家已行动、聚焦反应）

**背景**
- 用户反馈：「对象、方式、时机至少说清两样，允许条件分支」的措辞把「推测玩家下一步」当成了主任务；
  但角色扮演推演发生在玩家发送消息之后（messageSent 阻塞 → 推演 → 注入 → 主模型生成），
  玩家已经做出了行为——独白应聚焦「对玩家已做行为的解读与回应」，而不是推测玩家下一步要干什么；
  条件分支保留为局势有变数时的备案，但必须有一个明确的默认行动。

**改动（v1.0.10）**
- 开场职责定义改为「模拟 TA 此刻的真实内心——对玩家刚才行为的解读、情绪与接下来的行为倾向」；
- 【推演流程】形成假设锚定「重点是玩家刚才的行为意味着什么、TA 想干什么」；行动倾向改为
  「玩家已经行动了，不用再推测 TA 的下一步——此刻要想的是『我怎么回应』」，
  条件分支降级为「局势还有变数时可以带条件分支，但必须有一个明确的默认行动」；
- 【三要素】想法改为「玩家刚才的行为对我来说意味着什么」；下一步行动改为
  「对玩家刚才行为的即时回应——TA 那句话、那个动作，我接不接、怎么接」，
  「对象、方式、时机至少说清两样」保留为括号内备案（用户认可），禁止空泛意图不变；
- 迁移：新增 LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V5 快照（= v1.0.8/v1.0.9 已发布默认），
  getSettings 迁移条件纳入 V5；版本号升至 1.0.10（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js；
- 迁移模拟：V5 快照与 v1.0.8/v1.0.9 默认逐字一致；v1.0.8/v1.0.9 默认 → v1.0.10 新默认；
  V1–V4 旧默认 → 新默认；自定义文本不动；新默认零迁移零保存。

### v1.0.9 — 档案预筛 Gate 提速优化（最快返回 + 请求体精简 + 输出上限）

**背景**
- 用户要求「重点是快，而且不要输出多余的东西」：档案预筛 Gate 是高频轻量调用（每次主生成后触发），
  但默认提示词只写了「不要输出思考过程」，未把「快」作为第一优先级；请求体 6 条消息、JSON 带缩进，
  输入体积与消息轮次有压缩空间；Gate 输出只有一份角色名单，却沿用全局 8192 的 max_tokens 上限。

**改动（v1.0.9）**
- 提示词：开场改为「这是高频轻量调用，必须最快返回：不要分析、不要解释、不要任何多余文字，回复越短越好」；
  输出契约首条追加「回复越短越好」——不增加篇幅，只强化速度语义；
- 请求体（buildAutoArchiveGateMessages）：6 条消息精简为 4 条（名单引导与名单块合并、剧情引导与剧情块合并），
  名单与剧情 JSON 改为紧凑序列化（去掉缩进），减少消息轮次与输入 token；
- 调用参数（runAutoArchiveGate）：Gate 单独传 maxTokens: 1024（原全局 8192）与 temperature: 0.1，
  防止模型长篇输出拖慢高频调用，同时保持判定确定性；
- 迁移：新增 LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V6 快照（= v1.0.8 已发布文本），
  getSettings 迁移条件纳入 V6；版本号升至 1.0.9（js/constants.js MODULE_VERSION / manifest.json）。

**验证**
- node --check 全部 js 源码与构建产物通过；node build.js 重建 index.js（6348 行）；
- 迁移模拟 23 项全过：V1–V6 旧默认 → 新默认；新默认不循环；自定义不动；V6 快照与 v1.0.8 文本逐字一致。

### v1.0.8 — 角色扮演默认提示词「行为倾向推演」重构（PM 审查迭代）+ 迁移链路修复

**背景**
- 以产品视角走查「角色扮演」默认提示词的全链路（子 agent 推演 → <NPC_Deduction> 注入 → 主模型内化）后判定：提示词只描述了「性格常量」（信息局限 / 性欲 / 逐利 / 情感 / 生活 / 嫉妒等长期倾向），缺少「动机 → 此刻情绪 → 具体行动意图」的推导链；「下一步行动」要求偏弱，模型常产出「先看看再说」式空泛意图；风格示例只覆盖日常场景，冲突 / 高压场景下行为倾向不落地；同场多角色时容易把剧情在心里复述一遍。
- 顺带发现三处产品缺陷：① v1.0.7 发布的「角色扮演」默认（含 memory 消费指令）没有对应 legacy 快照（V4 缺失），老用户无法自动升级；② 在途新增的 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V5` 快照内容误填为新默认文本（应为已发布的 v1.0.7 文本），会导致旧用户迁移失效；③ 源码与发布产物不一致——js/constants.js 的「档案系统」默认停留在 v1.0.6（无记忆质量标准）、「角色扮演」默认输入说明被误删尾部（worldview / memory / Recent_Messages 三段），直接重建会让 v1.0.7 的发布内容回退。

**改动（默认「角色扮演」提示词，v1.0.8 新默认）**
- 新增【推演流程】（心里按顺序过一遍，不写出来）：在场判定 → 情境定性 → 动机检索 → 记忆触发 → 形成假设 → 行动倾向——把「性格常量」升级为「情境函数」，<npc_behavior> 动机库由背景板变成推导入口；
- 「下一步行动」升级为「具体的行动倾向」：对象、方式、时机至少说清两样，禁止「先看看再说」「静观其变」类空泛意图，允许条件分支（「他要是……我就……」）；
- 「想法」强调「这件事对我来说意味着什么」而非复述发生了什么；新增【质量红线】：不写剧情复读 / 不写总结腔 / 记忆是原因不是背景板 / 篇幅宁短勿长（通常 3-6 句、一般不超过 200 字，控制多角色并发推演的注入 token 成本）；
- 新增第二个风格示例（冲突威胁场景：客栈老板娘，示范威胁评估与条件行动），与原有日常疑心示例构成「日常 ↔ 高压」双端示范；
- <npc_behavior> 动机库原样保留，仅统一「选择性外向」条目格式（有什么 / 行为倾向）与其他条目一致；
- 输入说明恢复并强化：worldview（不可全知）、memory 消费指令、Recent_Messages 在场判定（先判定 TA 是否在场、能听到看到什么，据此判断获知范围）。
- **注入块强化**（`buildNpcDeductionInjectionText`）：<NPC_Deduction> 首行扮演指令追加两条护栏——「每个角色的独白只属于 TA 自己，不得互相套用或张冠李戴；若独白与该角色既定的设定或剧情事实冲突，以设定与剧情为准」，防止主模型串味或死板照搬。

**迁移链路修复（连带）**
- 新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V4` 快照（= v1.0.7 已发布默认，含 memory 消费指令），getSettings「角色扮演」迁移条件纳入 V4；
- 修正 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V5` 快照内容为 v1.0.7 已发布文本（此前误填为 v1.0.8 新文本，会导致旧用户迁移失效、新用户每次加载重复保存）；
- 把 v1.0.7「档案系统」默认（记忆质量标准）与「角色扮演」默认（memory 消费指令）同步回 js/constants.js 源码，消除源码与发布产物不一致；
- 版本号升至 1.0.8（js/constants.js MODULE_VERSION / manifest.json / README.md）。

**验证**
- `node --check` 全部 js 源码与构建产物通过；`node build.js` 重建 index.js（6289 行）；
- 迁移模拟 17 项全过：V4 / V5 快照与 v1.0.7 发布文本逐字一致；v1.0.7 各份默认 → 新默认；V / V2 / V3 旧默认 → 新默认；自定义文本不动；四份新默认零迁移零保存（不循环）；
- 一致性检查：js/ 内全部 LEGACY_* 引用均有定义。

### v1.0.8 — 角色扮演预筛 Gate 产品化重构（PM 审查迭代：在场受影响角色必选 + 戏份排序上限）

**背景（PM 视角审查）**
- 以产品视角走查「角色预筛 Gate → 并发推演 → <NPC_Deduction> 注入 → 主模型」全链路后判定：Gate 是整条链路的**单点决策器**——它选谁，谁才有内心状态；漏选的角色在主模型侧静默变平（最坏失败模式，用户不可见），多选只多花一次推演调用（成本有界、可降级）。
- 旧默认提示词（v0.9.6）的「判断要点」整体偏向排除（「拿不准时倾向不列入，宁少勿多」），与产品目标「活人感」错位：**在场但未开口的角色**（目击冲突/秘密、被事件波及、情绪被触发）与**带着行动意图/情绪积累、本轮可能爆发**的角色最容易被漏选——而这两类恰恰是「内心状态决定下一轮走向」的核心对象。
- 与同版本「档案预筛」的修复方向（v0.9.8 起「有实际出场并参与互动倾向列入」）不一致：档案预筛已改为召回偏向，角色扮演预筛仍停留在排除偏向。
- 历史故障佐证：v0.9.6 实测「用户直接点名纱雾（动作对象）→ Gate 0 入选」，说明排除偏向 + 定义过窄会直接吞掉整轮推演。

**改动（默认「角色扮演预筛」提示词，v1.0.8 新默认）**
- 职责定义从「哪些角色会开口或有戏份」扩展为「最后一条用户消息引发的下一轮剧情中，哪些角色会开口、行动或产生重要内心反应」——明确本轮 = 末条用户消息触发的下一轮，聚焦触发点；
- 输入说明新增「最后一条用户消息是下一轮剧情的直接触发点：优先判断它点名、涉及或会波及哪些角色；其余消息用于确认场景中还有谁在场、谁带着未了的心事或行动意图」——补上场景连续性判断；
- 判断要点重写为「必须列入 / 应列入 / 不列入」三层：
  - 必须列入：会开口、被直接点名（含动作对象：被抱起/放下/触碰/呼唤/喂食等）、明显有戏份；
  - 应列入（新增）：在场且受到本轮事件直接影响、或很可能产生重要内心反应的角色——目击冲突/秘密/重大消息、被事件波及、情绪被触发、或近期消息中已表现出行动意图与情绪积累、本轮可能爆发或行动；这类角色即使本轮不开口，其内心状态也决定下一轮剧情走向；
  - 不列入：只是被顺带提及、不在场、或在场但纯属背景（不会反应、不会行动）的角色；
- 偏差修正：「拿不准时倾向不列入，宁少勿多」→「拿不准时倾向列入：漏选一个真正有戏份的角色，比多选一个浪费一次推演更伤体验」——与档案预筛的召回偏向对齐，同时用「不列入」清单保留精度排除路径；
- 数量治理：新增「若入选角色较多，按戏份轻重排序，最多保留 6 个戏份最重的角色」——软上限替代旧「宁少勿多」的隐性上限，兼顾并发推演成本与注入块篇幅（每个入选角色 = 1 次推演调用 + 1 段注入独白）；
- Gate 剧情段引导消息与输出契约段同步：「请据此判断哪些角色本轮会开口、被直接点名、明显有戏份，或在场且受到本轮事件直接影响；只是被提及、不在场或纯属背景的角色不要列入」「只列出本轮会开口、行动或有重要内心反应的角色」；
- 旧默认文本（未自定义）自动迁移：新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V4`（与 v0.9.6 默认逐字一致），getSettings 迁移条件纳入 V4。

**验证**
- `node --check` 全部通过；`node build.js` 重建 index.js（6349 行）；
- 迁移模拟：V4 与 v0.9.6 已发布默认逐字一致（含 `\`\`\`json` 转义还原）；v0.9.6 默认 → v1.0.8 新默认；自定义文本不触发迁移；新默认零迁移零保存；
- 一致性检查：index.js 与 js/constants.js 的 roleplayPreScreen 默认逐字一致；Gate 引导消息 / 输出契约段 / 迁移日志三处同步。

### v1.0.8 — 档案预筛 Gate 判断要点对齐下游触发点（PM 审查迭代：在场目击/信息补充必选）

**背景（PM 视角审查）**
- 以产品视角走查「档案预筛 Gate → 档案分析 → 档案更新」链路，发现 Gate 判断要点与下游分析触发点不对齐：
  - 下游「档案系统」的 memory 定义含「在场目击的事实」，fields（年龄/职业/身份）与 worldview（对话中获知的新设定）也是更新触发点；
  - 上游 Gate 判断要点只覆盖「参与互动」，沉默目击者（听到秘密、看到事件）与「只透露自身信息/获知世界观规则」的角色可能被漏选 → 档案静默过期（最坏失败模式，不可见）；
  - 反向：v0.9.8 后「参与互动通常就有新记忆」+「拿不准倾向列入」≈ 场景内全员入选，附和/寒暄角色也触发完整分析（含世界书注入），成本无谓上升。

**改动（默认「档案预筛」提示词，判断要点重写为「必须列入/不列入」清单）**
- 必须列入：有实际出场并参与互动的角色（说话/行动/情绪反应/被点名/动作对象）——日常互动同样算数（保留 v0.9.8 修复）；
- 必须列入：在场目击了重要信息的角色（听到秘密、看到事件发生、旁观关键对话）——即使没有开口，亲历或目击了值得记录的事就应列入（对齐下游 memory 的「在场目击」定义）；
- 必须列入：本轮透露了自身新信息（年龄/职业/身份/性格/家庭/过往经历等）、获知了新的世界观规则，或关系、背景发生变动的角色（对齐下游 fields/worldview/relationships 触发点）；
- 不列入：完全没有出场、只是被提及的角色；以及虽然在场但只是简单附和、寒暄，确实没有任何新信息、新情绪或新互动内容的角色（给模型一条合法的精度排除路径，同时保留召回偏向）；
- 拿不准时，对有实际出场、参与互动或目击了信息的角色倾向列入；对只是被提及的角色不列入。
- Gate 剧情段引导消息同步补充「在场目击了重要信息」。
- 迁移：`LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V5` 快照 = v0.9.8 已发布文本（本版本内已修正），V1-V5 旧默认全部可升级到新默认。

**验证**
- `node --check` 全部通过；`node build.js` 重建 index.js（6349 行）；
- 迁移模拟 21 项全过：V1-V5 旧默认 → 新默认；新默认零迁移零保存（不循环）；自定义文本不动；V5 快照与 v0.9.8 已发布文本逐字一致；新默认含全部新增规则。

------

### v1.0.7 — 档案系统默认提示词新增记忆质量标准（长期扮演一致性）

**背景**
- 档案「记忆」分节此前只有「亲历 / 被告知 / 在场目击的事实」一句描述，没有质量与格式硬性规定：模型容易写出「他帮了她，她很高兴」这类脱离上下文无法读懂的条目，或为同一事件反复 add 重复条目、新旧记忆互相矛盾；长期游玩后档案臃肿、时间线错乱，无法支撑角色扮演的一致性。

**改动**
- 默认「档案系统」提示词新增 memory 硬性质量标准（【档案结构】分节）：
  - 自包含：人物一律用全名（该角色本人用「我」），地点、事件写清楚，禁止「他 / 她 / 那个人 / 那天」等无指代代词与模糊表述；
  - 带时间锚点：写明事件在故事时间线中的位置（初遇当晚 / 三天前 / 在XX事件之后 / 最近），让 AI 能判断先后与新旧；
  - 事件 + 意义：除事实外写明该角色当时的反应、感受或由此产生的态度 / 决定，作为后续扮演情绪与行为一致性的来源；
  - 一条一件事：每条只记一个完整事件，一句话写完，不一条塞多事、不拆碎片；
  - 可长期累积：日常互动合并成习惯性条目（如「与主角保持着每天一起吃晚饭的习惯」），不为每次重复互动单独建条目。
  - 附好 / 坏示例各一条。
- 【判断要点】新增两条：记忆质量优先于数量（只记重要事件 / 关系变化 / 承诺 / 秘密 / 受伤 / 目标 / 情绪转折 / 形成的习惯）；同一事件再次出现用 update 改写原条目、新信息与旧记忆冲突用 update / remove 纠正，不得让档案里同时存在互相矛盾的记忆。
- 【输出契约】add 说明补充「记忆条目必须满足 memory 质量标准」；档案分析请求的输入引导同步补充记忆质量标准一句话。
- 旧默认文本（未自定义）在 `getSettings` 里逐字比对后自动迁移（新增 `LEGACY_DEFAULT_ARCHIVE_SYSTEM_V2`，与 v0.9.5 旧默认一并升级）。
- 版本号升至 1.0.7（index.js / manifest.json / README.md）。

**PM 检验修订（同版本内）**
- 以「产品」视角走查产出物与消费链路（档案分析产出 → 角色扮演子 agent 消费 → 主模型）后补齐 5 处缺口：
  - **时间锚点约束**：分析只看最近 4 条消息，AI 可能引用档案里不存在的「XX事件」作参照 → 明确「时间参照只能来自本轮对话或 current_profile 中已存在的事件」；
  - **称呼映射**：对话里的「哥哥 / 老师 / 老板娘」可能映射不到全名 → 明确「不知道全名时用稳定身份称呼（如「酒馆老板娘」）并保持同一角色始终同一称呼」；
  - **情绪依据**：「事件 + 意义」可能诱导编造角色感受 → 明确「反应 / 感受必须来自对话中的明确表现或合理推断，不得凭空编造」；
  - **分节边界与多角色视角**：同一事实可能同时进 relationships 与 memory、多角色同场时可能复制粘贴相同条目 → 判断要点新增「关系状态记 relationships、具体事件记 memory，不重复」「各角色记录自己视角下的版本，不复制粘贴」；
  - **消费侧闭环**：角色扮演提示词只点名了 worldview、没点名 memory，子 agent 可能不主动引用记忆 → 默认「角色扮演」提示词输入说明新增 memory 消费指令（记忆分节是角色记得的经历与感受，扮演时自然引用，决定记得什么、对谁有旧情旧怨、为何有此态度；不得忘记已记录的事，也不得凭空记得未记录的事），旧默认文本自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V4`）。
  - **空档案初始化**：刚注册角色的档案近乎为空时，AI 可能因「依据模糊时倾向不新增」而漏写世界书设定卡中明确存在的内容 → 输入说明 ① 改为「档案为空或近乎为空时，应据此认真、完整地初始化档案；档案已有内容时用于补全与纠正」；判断要点新增「档案初始化」：把设定卡中明确写出的身份、性格、家庭背景、人际关系、世界观逐项写入对应分节，设定卡中记载的该角色过往经历写入记忆（附第一人称 + 时间锚点示例），初始化同样遵守各分节质量标准、只写设定卡实际存在的内容，此时设定卡就是确凿依据、不受「依据模糊时倾向不新增」约束；memory 定义同步扩展为「该角色亲历/被告知/在场目击的事实（含设定卡中记载的过往经历）」。
- 复杂事件放宽为「尽量一句话写完（复杂事件不超过两句）」。
- **上下文长度问题不在本版本处理**：用户后续自建自动摘要插件，故不设代码级条目上限 / 摘要，提示词也不含「存量治理」类规则（已移除），只保证单条记忆质量。

**验证**
- `node --check` 语法通过；迁移模拟 17 项全过：两份提示词的旧默认（v1.0.6）逐字一致可升级、新默认与自定义不重复触发迁移、新默认含全部新增规则。

### v1.0.6 — 修复：其他插件广播宿主事件误触发 SoulLink API 调用（主生成流程跟踪 + messageSent 载荷校验）

**背景**
- SoulLink 有四种会发起模型 API 调用的场景：① 手动档案分析（用户点按钮）；② 自动档案维护（订阅宿主 generationEnded）；③ 角色推演 Gate 预筛（订阅宿主 messageSent）；④ 单角色推演（同 ③ 的 messageSent 链路）。其中 ②③④ 都挂在宿主全局事件总线上，而 SillyTavern / TauriTavern 的 ventSource 是全局 EventEmitter——**任何扩展都能自行 mit 任意事件**。
- 误触发风险：翻译 / 摘要 / 续写 / QuickReply 等插件完成自己的 API 调用或程序化发送消息后，可能广播 generationEnded / messageSent。SoulLink 收到后若末条消息恰好满足「AI 回复」或「用户消息」形态，就会误发起一轮 Gate + 并发分析 / 推演，产生用户不知情的 API 消耗（每轮最多 1 次 Gate + N 次角色调用）。

**改动**
- **主生成流程跟踪（防 generationEnded 误触发）**：新增 mainGenerationState，订阅 generationStarted 时记录「生成开始时的末条消息签名」，generationStopped / chatChanged / groupSelected（切换/加载聊天）时清空。onAutoArchiveGenerationEnded 增加两道前置校验：
  - 无 generationStarted 配对（startChatSignature 为空）→ 判定为其他插件自行广播，直接跳过；
  - 末条消息签名与生成开始时一致（本轮没有产出新 AI 回复）→ 判定为事件重放 / 插件广播，直接跳过。
  - 校验通过后才消费签名，避免「插件在生成中途广播」把主流程的跟踪吃掉；末条不是 AI 回复时不消费，主流程的 generationEnded 仍可正常处理。
- **messageSent 载荷校验（防 messageSent 误触发）**：onNpcDeductionMessageSent 接收事件参数，当载荷为字符串时与末条用户消息内容比对，不一致即判定为其他插件程序化发送（QuickReply / 自动回复等），跳过推演；载荷非字符串（宿主格式差异）时无法校验，按原有逻辑放行。
- **手动档案分析不受影响**：用户点按钮触发，无事件总线参与，无误触发风险。
- 版本号升至 1.0.6（index.js / manifest.json / README.md）。

**验证**
- 
ode --check 语法通过；构建产物包含全部新逻辑。
- 场景推演（vm 沙箱思路）：主生成正常流程（generationStarted 记录用户消息签名 → AI 回复 → generationEnded 签名变化 → 正常分析）；插件广播 generationEnded（无 generationStarted → 跳过）；插件在生成中途广播（末条仍是用户消息 → 不消费签名 → 主流程 generationEnded 正常处理）；生成被中断（generationStopped 清空 → 后续广播跳过）；切换聊天（chatChanged 清空 → 残留签名不误用）；插件广播 messageSent 携带不同文本（载荷不一致 → 跳过推演）；宿主 messageSent 载荷非字符串（无法校验 → 按原逻辑放行）。
- 待手动验证：真实 Tavern 中开启自动档案维护 / 角色推演，确认正常生成与发送仍触发，且其他插件（如翻译、QuickReply）活动不再引发 SoulLink API 调用。

---### v1.0.5 — 修复：DeepSeek 官方「AI 未返回文本内容」根因（模型把答案写进 reasoning_content，content 留空）

**背景**
- v1.0.3 串行化后仍出现「AI 未返回文本内容（direct-after-proxy-fallback）（已自动重试 2 次）」：代理路径返回空内容 → 回退直连 → 直连也返回 200 + 空 content，串行下连续 3 次全空。既然已无并发，说明与并发无关，是响应解析的问题。
- 拿到用户导出的完整请求/响应体（soullink-fullbody-2026-08-07T14-55-28.json，5 条捕获）后真相大白：**API 其实回复了完整内容，但写在了 `message.reasoning_content` 字段里，`message.content` 为空**。模型为 deepseek-v4-flash（官方带思考能力的新模型），`finish_reason=stop`、`usage.completion_tokens_details.reasoning_tokens` 等于全部 completion tokens——模型把最终答案（档案 JSON）整个输出到了思维链字段。同批 5 条捕获里 2 条如此、3 条正常（content 有值、reasoning_content 为空），属模型侧偶发行为，重试只能碰运气。
- 旧逻辑只读 `content` / `text`：`reasoning_content` 有内容也判「未返回文本内容」；`responseContainsUsableText` 同样不认 reasoning_content，导致代理路径被误判损坏、白白多走一次直连。

**改动**
- `requestChatCompletionOnce`：`content` 为空但 `reasoning_content` 非空时，把 `reasoning_content` 作为回复内容兜底取用（warn 日志记录「AI 回复内容位于 reasoning_content 字段」），不再误报「AI 未返回文本内容」；两者皆空才报错。该兜底位于共享请求层，档案分析、自动档案维护、角色扮演（Gate 预筛 + 单角色推演）全部自动生效，无需各自处理。
- `responseContainsUsableText`：同步把非空 `reasoning_content` 视为可用文本，代理路径不再被误判损坏而多余回退直连。
- 请求体新增 `max_tokens: 8192`（`CHAT_COMPLETION_DEFAULT_MAX_TOKENS`，可用 `options.maxTokens` 覆盖）：顺带规避 deepseek-reasoner 思考阶段耗尽默认 4K 输出预算导致的真·空回复（官方文档记载行为）。
- 空内容错误信息保留 `finish_reason` 诊断与「疑似思考阶段耗尽输出预算」提示，仅当 content 与 reasoning_content 皆空时出现。
- **移除 v1.0.3 的 DeepSeek 官方地址串行机制**（`ARCHIVE_DEEPSEEK_HOST_RE` / `isDeepSeekOfficialBase` / `runArchiveAnalysisBatch` 串行分支）：v1.0.2/v1.0.3 认定的「并发下 200 + 空内容」与本次是同一误判（空 content 实为 reasoning_content 有答案），与并发无关；批量档案更新恢复统一并发（`Promise.allSettled`），取消语义不变。
- 版本号升至 1.0.5（index.js / manifest.json / README.md）。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟 12 项全部通过（复现用户导出的真实响应：content 空 + reasoning_content 有完整 JSON → 返回 reasoning_content 内容且 transport 正确；responseContainsUsableText 对 reasoning_content 判可用；空 choices / 双空仍报「AI 未返回文本内容」；正常响应不受影响；max_tokens 默认 8192、options.maxTokens 覆盖生效）；串行机制移除后 `runArchiveAnalysisBatch` 恢复纯并发，返回结构与 `Promise.allSettled` 一致。

---### v1.0.4 — 修复：同时删除两层（最新 assistant + 最新 user）不触发删楼联动档案清理

**背景**
- 删楼联动清理在「删除一层」与「重生成」时正常生效，但用户同时删除两层（最常见：最新 assistant + 最新 user）时档案不清理。排查宿主（TauriTavern 2.2.0）源码：删楼流程是 `chat` 数组先变 → 聊天表面 DOM 移除 `.mes` → `await` 间隙里 DOM 观察器触发 → 再发 `MESSAGE_DELETED`。纯逻辑复刻（vm 沙箱 8 个场景）全部通过，说明问题出在真实宿主的事件/DOM 时序：连续两次删除之间快照可能被提前重建，或虚拟化聊天表面（bounded chat surface）的移除不在观察窗口内，导致第二次 diff 时第一层的签名已不在快照中、清理落空。

**改动**
- `onFloorTraceDomChanged` 新增**路径 1：直接消费 `MutationRecord.removedNodes`**——被移除的 `.mes` 元素带 `mesid`（消息在 `chat` 里的下标），对照「上次快照时的聊天副本」（新增 `floorTraceLastChat`，在 `rebuildFloorTraceSnapshot` 里同步维护）直接取回被删消息签名并清理，不依赖快照时序；连续删两层时每层移除各自触发，两层都能清到。
- 路径 1 带三重守卫防误伤：① 元素仍连接在 DOM（移动 / 重排）或同一记录里又被加回（move）一律跳过；② 楼层数必须比上次快照少（内容编辑 / 重渲染 / 虚拟化窗口滚动不清理，与 v0.9.15 的楼层数守卫同款）；③ 被删签名必须已不在当前聊天里（重渲染替换元素时消息内容仍在聊天中，不清理）。
- 原有「快照 diff」降为**路径 2 兜底**（幂等），覆盖 removedNodes 拿不到消息的场景。
- 版本号升至 1.0.4（index.js / manifest.json / README.md）。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟 13 项全部通过（新增 removedNodes 场景：删楼模式双删 / 连续双删且中间快照被重建 / 记录批量合并 / 单删回归 / 元素移动不误删；回归：单删 / 双删 / 区间删 / 顺序删 / DOM 兜底 / 幂等）。

---### v1.0.3 — 修复：DeepSeek 官方地址批量档案更新改为串行，规避并发空响应

**背景**
- v1.0.2 已让 DeepSeek 官方地址的批量档案更新「错峰 200ms」发起，但实测无效：每个请求要跑数十秒（60s 超时），错峰只推迟了发起时刻，请求仍完全重叠在途，并发度没有下降。日志显示 3 个请求错峰发出后，60 秒后同时超时中止，回退直连又各跑 60 秒，最终返回 200 但响应体为空，全部失败。
- 附带发现：60s 超时触发的 abort 被错误信息显示成「The user aborted a request」，误导排查（实为超时，非用户取消）。

**改动**
- `runArchiveAnalysisBatch` 重构：当 Base URL 命中 `api.deepseek.com` 时，批量档案更新（「分析全部角色」与自动维护的更新阶段）改为**串行**——逐个 `await`，一个角色分析完（含其内部自动重试）才发起下一个，从根上消除并发；其他地址保持原并发行为（`Promise.allSettled`）。
- 串行中若当前角色被取消（用户点「取消分析全部角色」），`break` 不再发起后续角色，取消语义与并发模式一致。
- 版本号升至 1.0.3（index.js / manifest.json / README.md）。

**验证**
- `node --check` 语法通过。
- 串行路径返回结构与 `Promise.allSettled` 一致（`{status, value|reason}`），调用方汇总逻辑无需改动。

### v1.0.2 — 修复：档案分析偶发「AI 未返回文本内容」（DeepSeek 并发下返回空内容 JSON）

**背景**
- 用户反馈：点「分析全部角色」经常「发 3 个、挂 2 个」，失败详情显示「本次失败发生在 API 请求阶段，未收到 AI 回复内容」；换 ollama.com 等兼容地址则基本不出现。交叉验证扩展日志与宿主 llm-api 后端日志：并发 3 个分析请求全成功那次走的是 ollama.com；切到 api.deepseek.com 后宿主代理返回 200，但响应体是无文本 JSON（能解析、`choices` 为空或 `content` 为空），扩展随即抛「AI 未返回文本内容（host-proxy）」。
- 根因一（上游瞬态 + 扩展不重试）：DeepSeek 在并发压力下会瞬态返回「200 + 无内容 JSON」。旧逻辑 `proxyLooksBroken` 只认「非 2xx / 非 JSON / 401-405」，这种「能解析但等于没回复」的响应直接判死；`chatCompletion` 没有任何重试（st-chatu8 对空回复有 maxRetries 重试，因此其他流程体感更稳）。
- 根因二（日志误导排查）：`readResponseBodyForLog` 先 clone 读截断文本，再对原始 response 二次 clone 读完整体——`fetchText` 已用 `response.text()` 消费原始体后，二次 clone 必抛错，`catch` 把已读到的截断文本一起丢弃，日志把有内容的响应也记成「响应体: (空)」。

**改动**
- `chatCompletion` 重构为「单次请求 + 自动重试」：单次逻辑抽为 `requestChatCompletionOnce`（代理 → 直连回退链不变，新增「无内容 JSON」也视为代理损坏、立即回退直连；传输层网络错误保留「对话请求失败…请检查 API 配置」的友好包裹）；外层按 `CHAT_COMPLETION_MAX_ATTEMPTS=3` 次、`600ms × 次数` 退避自动重试，重试间隙可被「取消」立即中断（`sleepAbortable` 监听 AbortSignal）。
- 可重试判定：`AI 未返回文本内容`、HTTP 429 / 5xx、上游错误信封含 busy / overload / rate limit / try again 等瞬态特征 → 自动重试；401 / 400 / 404 等配置类错误不重试。重试耗尽后错误信息追加「（已自动重试 N 次）」，档案失败详情可区分瞬态故障与真实失败。
- 日志修复：对话接口响应体改为从同一个 clone 完整读完再截断展示（不再二次 clone），日志能真实显示响应体，完整请求体导出也能拿到真实响应体。
- 版本号升至 1.0.2（index.js / manifest.json）。

**验证**
- `node --check` 语法通过。
- vm 沙箱冒烟 19 项全部通过：空 choices 回退直连成功 / 空内容 ×2 后第 3 次成功 / 耗尽报「已自动重试 2 次」且带 retryable 标记 / 401 立即失败不重试（代理 1 次 + 直连 1 次）/ 429 直连兜底 / 503 重试后成功 / busy 错误信封重试后成功 / 重试间隙可取消 / `responseContainsUsableText` 单测 7 项（空 choices、空 content、正常、错误信封、空串、HTML、嵌套 response 信封）。

### v1.0.1 — 新增：首页底部版本号旁联网检查 GitHub 最新版

**改动**
- 面板底部版本号（`v1.0.1`）右侧新增小胶囊提示，联网对比 GitHub 仓库 `main` 分支的 `manifest.json` 版本号：
  - 已是最新版 → 显示「已是最新版」（绿色）；
  - 有更新 → 显示「发现新版本 vX.Y.Z」（粉色）；
  - 联网失败 → 显示「检查失败，点击重试」，点击可强制重新检查。
- 检查源双通道：优先 `raw.githubusercontent.com`，失败自动回退 `api.github.com`（base64 解码 manifest），任一成功即出结果；结果缓存 1 小时，点击胶囊可强制刷新。
- 版本号升至 1.0.1（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；版本比较用例（相等 / 落后 / 领先 / v 前缀 / 多段号）全部通过；真实联网冒烟：本地 v1.0.1 vs 远端 v1.0.0 → 「已是最新版」，本地 v0.9.0 vs 远端 v1.0.0 → 「发现新版本 v1.0.0」。

### v1.0.0 — 正式版发布

**里程碑**
- SoulLink 首个正式版：API 连接、日志系统（含噪音治理与网络捕获）、预设系统、角色扮演、档案系统（含自动维护）、剧情前置 NPC 推演、世界书系统、删楼联动档案清理、消息正则过滤全部进入稳定状态。
- v0.9.12 ~ v0.9.16 完成发布前收尾：删楼联动档案清理（楼层数守卫修复误伤）、消息正则过滤（默认剔除智绘姬 `<image>` 占位块 + 编辑界面关闭 / 重复条目修复）。
- 版本号升至 1.0.0（index.js / manifest.json），作为首个正式版对外发布。

**发布检查**
- `node --check` 语法通过；vm 沙箱冒烟测试（正则解析 / 过滤生效 / 编辑流程）全部通过。

### v0.9.16 — 修复：正则编辑界面「取消/保存后关不掉」与「保存产生重复条目」

**背景**
- 用户反馈：点「编辑」打开正则编辑框后，点「取消」关不掉；点「保存」会出现另一个「智绘姬」条目，编辑框也仍然开着。
- 根因一：`.soullink-filter__editor` 等样式写了 `display: flex`，会覆盖 UA 样式 `[hidden]{display:none}`，导致 `editor.hidden = true` 不生效——编辑框从页面加载起就一直可见，「取消」只是把编辑状态清掉、界面没任何反馈。
- 根因二（重复条目的直接原因）：「取消」虽然关不掉界面，但已经重置了 `filterEditingId`；用户以为还停留在编辑态，再点「保存」就走了「新建」分支，生成一条新的「智绘姬」。

**改动**
- **CSS**：新增面板作用域规则 `.soullink-panel [hidden] { display: none !important; }`，让 `hidden` 属性在面板内始终优先于布局类 display（顺带修复了「上一轮角色扮演」视图里角色独白区块同样会被 `display:flex` 顶掉 hidden 的问题）；作用域限定在 SoulLink 面板内，不影响宿主页面其他元素。
- **JS 加固**：`closeFilterEditor` 关闭时清空名称 / 表达式输入框、复位校验提示、禁用保存按钮——即使极端情况下界面仍可见，残留的表单也不可能再按「新建」保存出重复条目。
- 版本号升至 0.9.16（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟测试 16 项全部通过（打开编辑填充 / 取消后隐藏并清空 / 取消后保存不产生重复 / 保存原地修改不新增 / 新建仍正常）。

### v0.9.15 — 修复：删楼联动清理误伤（智绘姬等扩展原地改写楼层内容被误判为删楼）

**背景**
- v0.9.13 的清理逻辑把「签名从快照中消失」一律视为删楼。智绘姬（st-chatu8）在 insertOriginalText 模式下会把生图提示词以 `<image>...</image>` 块插回最新楼层：先原地改写 `chat[mesId].mes`，再重渲染该楼层 DOM——楼层数不变，但消息签名变了，DOM 观察器触发快照对比后误判为「楼层已删除」，把来源为该楼层的档案条目全部清掉（用户日志：`删楼联动档案清理 楼层已删除，同步清理档案 8 条`，实际只是插入了生图提示词）。

**改动**
- **楼层数守卫**：`diffFloorTraceSnapshot` 新增「楼层数必须减少」才返回消失签名——`floorTraceLastLengths` 按 chatKey 记录上次快照时的楼层数，当前楼层数 ≥ 上次楼层数（内容编辑 / 重渲染 / 新增楼层）一律不算删楼，只重建快照不清理。
- 真实删楼 / 重生成不受影响：宿主（SillyTavern / TauriTavern）的 `deleteMessage` / `deleteLastMessage` 都是先改 `chat` 数组（长度减少）再发 `MESSAGE_DELETED`，事件处理器与 DOM 观察器都能正确触发清理。
- 版本号升至 0.9.15（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟测试 20 项全部通过（新增：智绘姬原地改写不清理 / 改写后删楼不误删其他楼层 / 楼层数未减少 diff 返回空 / 楼层数减少 diff 返回消失签名；回归：真实删楼 / 重生成 / 空聊天重载 / 切换聊天 / 新增楼层 / 跨角色跨分节 / 幂等）。

### v0.9.14 — 消息正则过滤：四种调用导入的最新消息按正则剔除噪音

**背景**
- 档案分析、档案预筛、角色扮演预筛、角色推演这四种调用都会把最近的几条消息（getRecentMessages）作为上下文；智绘姬等扩展会在消息里塞入 `<image>...</image>` 图片占位块，图片内容进入子 agent 上下文既浪费 token 又干扰判断。

**改动**
- **正则过滤系统**：「API 连接」页底部新增「正则过滤」区块，支持新建 / 编辑 / 删除 / 启停，以及 JSON 导入 / 导出（`settings.SoulLink.messageFilters`，随 Tavern 设置持久化）。
- **默认正则**：内置一条启用状态的「智绘姬」过滤——`/<image>[\s\S]*?<\/image>/g`，老存档升级时自动补入（getSettings 迁移）。
- **统一生效点**：过滤只在 getRecentMessages 一处生效，四种调用全部覆盖——每条消息内容按启用的正则顺序剔除匹配部分；整条内容都被匹配（剔除后为空）的消息不再进入上下文。
- **表达式输入**：编辑框同时支持完整字面量（`/表达式/标记`，如 `/<image>[\s\S]*?<\/image>/g`）与纯表达式两种写法，输入时实时校验（不可编译会红字提示并禁用保存）；字面量解析会跳过被 `\` 转义的斜杠。
- **导入格式**：JSON 数组或 `{ filters: [...] }` / `{ items: [...] }` 对象，每条含 name / pattern / flags / enabled；导入一律分配新 id、不做覆盖，无效条目（缺名称 / 缺表达式 / 不可编译）自动丢弃。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟测试通过：默认正则剔除 `<image>` 块、整条被剔除的消息不再进入上下文、禁用正则后内容原样、无效表达式跳过不阻断、导入归一化（缺字段丢弃 / 重复 id 重排）。

### v0.9.13 — 修复：删楼联动档案清理未生效（宿主事件机制与初版假设不符）

**背景**
- v0.9.12 的清理依赖 `chatChanged` 事件对比签名快照 + `messageDeleted` 事件直接取被删消息。实测删楼后档案未被清理，排查宿主源码确认：
  - SillyTavern / TauriTavern 删楼与重生成**都不会触发 `chatChanged`**（该事件只在切换 / 加载聊天时触发）；
  - `MESSAGE_DELETED` 事件的载荷**只是新的聊天长度（数字）**，不包含被删消息本身——旧实现找不到消息对象直接 return，且 DOM 移除也没有任何观察。
- 用户实测日志佐证：删楼瞬间的日志里没有任何 `chatChanged` 事件，档案条目（已带 `source` 标记）原样保留。

**改动**
- **快照对比统一化**：`rebuildFloorTraceSnapshot` / `diffFloorTraceSnapshot` 按 chatKey 维护「消息签名快照」，任何触发点都先对比快照与当前 `ctx.chat`，把「签名已消失」的楼层视为删除。
- **messageDeleted 事件**：不再依赖载荷内容，事件一到即做快照对比（删楼 / 重生成都会触发该事件，幂等可重复执行）。
- **DOM 观察兜底**：新增 `MutationObserver` 观察 `#chat` 的结构变化——既在新增楼层时维持快照新鲜，也在宿主事件缺失时兜底清理；聊天切换 / 重载会先清空 DOM 再重新渲染，通过「chatKey 变化 / 空聊天」守卫跳过，避免把重渲染误判成删楼而误删档案。
- 日志系统新增捕获 `messageDeleted` 事件，便于以后排查宿主事件时序。
- 版本号升至 0.9.13（index.js / manifest.json）。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟测试 20 项全部通过（新增复刻宿主行为的场景：`chat.splice` + `MESSAGE_DELETED(新长度)` 删楼清理 / 重生成清理 / DOM 兜底清理 / 空聊天重载不误删 / 切换聊天不误删 / 幂等）。

### v0.9.12 — 删楼联动档案清理：条目按来源楼层溯源，删楼后自动清除

**背景**
- 玩家删除聊天楼层（删楼 / 重生成 / 编辑楼层内容）后，档案里基于该楼层写入的条目（记忆等）仍会保留，AI 会继续「记得」已被删除的剧情，导致档案与当前剧情脱节。

**改动**
- **楼层溯源**：档案分析（手动「分析本角色 / 分析全部角色」与自动档案维护共用同一入口）写入新条目时，给条目打上 `source` 标记——该次分析时聊天末条消息的签名（role + name + mes）。后续 update 保留原 source；内容去重跳过的新增不重复打标；手动编辑新增的条目不带 source、不会被自动清理。
- **删楼检测**：订阅 `chatChanged` / `groupSelected`，按聊天维护「消息签名快照」（内存 Map，按 chatKey 隔离）。某签名从快照中消失即视为该楼层被删除 / 重生成 / 编辑，随后把来源为该签名的档案条目从所有已注册角色的各分节（性格 / 世界观 / 家庭背景 / 人际关系 / 记忆）中一并清除；首次见到某聊天只建立快照不清理，避免扩展加载时误删历史数据。
- **messageDeleted 兜底**：宿主提供 `messageDeleted` 事件时，直接按被删消息签名清理（与快照互补、幂等可重复执行；聊天里仍有同签名楼层时跳过，避免误删重复楼）。
- **反馈**：发生清理时保存设置、toast 提示（已清理 N 条），日志记录按角色分布的清理明细。
- **边界**：标量字段（姓名 / 年龄等）无法可靠回滚，不做处理；旧数据（无 `source` 字段的条目）不受影响。

**验证**
- `node --check` 语法通过；vm 沙箱冒烟测试 18 项全部通过（新增打标 / update 保留 source / 内容去重 / 跨角色跨分节清理 / 字段不动 / 幂等 / 首次建快照 / 删楼清理 / 切聊天 / messageDeleted 直清 / 同签名跳过）。
