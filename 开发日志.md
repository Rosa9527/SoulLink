# 开发日志

> 按日期记录 SoulLink 的开发进度、决策与下一步计划。
> 与「重要开发经验.md」（踩坑与解决方案）和「重要设计.md」（架构与设计决策）配合阅读。

---

## 2026-08-05

### v0.6.0 — 预设系统：四个子系统提示词管理

**完成内容**

- 首页新增「预设」入口卡片（占整行，粉色主题），实时显示「默认配置 / 已自定义 N 份」。
- 预设视图：四个标签页（档案系统 / 档案预筛 / 角色扮演 / 角色扮演预筛），每页一个编辑器；面板进入预设视图自动加宽到 600px（`.is-preset-mode`）。
- 默认提示词：四份提示词全文内嵌为 `DEFAULT_PROMPTS`（含 \`\`\`json 反引号的转义），作为出厂默认值；设置中 `prompts` 字段随 Tavern 设置持久化，缺字段自动补齐（`getSettings` 深合并，兼容旧存档 / 部分自定义）。
- 编辑体验：字数 / 行数实时统计；状态胶囊区分「默认内容 / 已保存的自定义内容 / 未保存的更改」；「保存」按钮仅在存在未保存修改时可用；未保存内容在标签页间切换不丢失（内存暂存，保存或恢复后清除）。
- 「恢复默认」：一键还原出厂提示词并立即保存；存在自定义或未保存内容时先弹确认框，避免误覆盖；内容已为默认时按钮置灰。
- 交互闭环：打开面板刷新首页预设状态；ESC 从预设视图返回首页再关闭面板；保存 / 恢复均有 toastr 与 SoulLink 日志埋点。

**验证**

- `node --check` 语法通过。
- 提取 `DEFAULT_PROMPTS` 与四份源提示词逐字比对：全部 MATCH（含反引号转义还原）。
- jsdom 冒烟测试 37 项全部通过：面板 / 卡片创建、视图切换与加宽、四个标签渲染、默认内容填充、输入置脏（含清空文本仍视为未保存）、保存写设置、恢复默认（含确认取消路径）、标签切换保留未保存内容、首页状态联动、`getSettings` 深合并（缺字段 / 部分自定义 / null 兜底）、ESC 返回首页。
- 待手动验证：真实 Tavern 中编辑、保存后重启 Tavern 确认提示词仍保留。

**过程中发现并修复的 bug**

1. 恢复默认确认框最初只拦截「未保存的修改」：已保存过自定义内容时点击恢复会直接覆盖，缺少防护 → 改为只要有自定义内容（已保存或未保存）就弹确认，文案区分「已保存的自定义内容 / 未保存的修改」。
2. 面板 HTML 插入时误删了日志视图的收尾 `</section>`，导致日志视图与预设视图结构嵌套错误 → 补回 `</section>`，并用 jsdom 断言视图结构与激活状态。
3. 空文本编辑（全选删除）曾被视为「未保存」以外的干净状态：`presetUnsaved[key]` 空字符串为假值 → dirty 判断改为 `!== undefined`，清空文本依然置脏、可保存。

### v0.5.0 — 日志系统：后台运行观察与 DEBUG

**完成内容**

- 日志捕获层：包装 `console.log/info/warn/error/debug`（保留原始方法，SoulLink 内部日志走原始 console 避免重复捕获）；监听 `window error` 与 `unhandledrejection`；订阅 Tavern 宿主关键事件（appReady / 聊天切换 / 消息收发 / 生成开始结束 / 设置加载等）并以 `debug` 级别记录。
- SoulLink 自身埋点：启动就绪、菜单注册方式、面板开关、视图切换、悬浮球隐藏、API 连接开始 / 模型列表拉取（含传输方式与数量）等，来源标记为 `soulink`。
- 环形缓冲：默认保留 2000 条（可在 500 ~ 10000 间调整，设置持久化），超出自动丢弃最旧；日志仅存内存，不落盘。
- 日志视图 UI：级别筛选 chips（全部 / 调试 / 信息 / 警告 / 错误，带实时计数）、关键词搜索（120ms 防抖）、暂停 / 继续追加（暂停期间新日志计数展示）、自动跟随最新（滚回顶部时出现「回到最新」按钮）、清空、复制纯文本、导出完整 JSON（含元数据）。
- 渲染策略：打开日志视图时整表重建，运行中增量追加；DOM 最多渲染最近 1000 条，超出显示「仅显示最近 N 条 · 共 M 条」提示，缓冲与导出不受影响。
- 安全序列化：日志参数支持 Error / BigInt / 函数 / DOM 节点 / 循环引用，单参数超 800 字符截断，整条消息超 4000 字符截断；消息用 `textContent` 渲染，避免日志内容注入 HTML。
- 首页联动：日志卡片实时显示「已记录 N 条 / N 个错误」，有错误时变红；进入日志视图面板自动加宽（540px）并自动 clamp 回视口。
- 对外 API：`globalThis.SoulLinkLog`（debug / info / warn / error / log / clear / getEntries / setMaxEntries），供其他扩展写入日志。
- 版本号同步更新至 v0.5.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- jsdom 冒烟测试通过：面板与日志视图创建、console 捕获与级别映射、外部 API 写入、首页状态更新、视图打开渲染、级别筛选、关键词搜索、暂停 / 恢复、环形缓冲上限、清空、宿主事件订阅。
- 待手动验证：真实 Tavern 中观察日志滚动、暂停与导出文件内容。

**过程中发现并修复的 bug**

1. 扩展脚本热重载（重新执行）时，若 console 已被旧实例包装，新实例的缓冲会收不到日志、`SoulLinkLog` API 也会指向旧缓冲 → console 包装改为「一次安装、动态派发」：包装器与窗口监听只装一次，捕获目标（handlers）在每次执行时替换为当前实例；`SoulLinkLog` API 每次执行整体重建。热重载后日志不中断、不重复包装。
2. 其余为纯新增功能，未触碰既有行为；注意：面板默认仍 340px，仅日志视图临时加宽（`.is-log-mode`），切换回首页自动恢复。

### v0.4.0 — 总前端：首页启动器（多视图面板）

**完成内容**

- 面板升级为多视图：打开面板默认进入「首页」，首页以手绘涂鸦风卡片网格展示功能图标；原「API 连接」区块移入独立子视图。
- 首页图标 ①「API 连接」：点击进入原有 API 连接界面（表单、连接、模型拉取逻辑不变），卡片上实时显示连接状态（尚未连接 / 已连接 · N 个模型）。
- 首页图标 ②「日志系统」：仅占位（🚧 施工中 · 敬请期待），预留子视图入口，日志功能后续开发。
- 视图导航：子视图标题栏出现「返回」按钮（←），点击或按 ESC 返回首页，首页再按 ESC 关闭面板；每次打开面板都回到首页。
- 版本号同步更新至 v0.4.0（manifest.json / index.js）。

**验证**

- `node --check` 语法通过。
- 待手动验证：点击悬浮球 → 首页两个图标 → API 子视图表单/连接 → 返回/ESC 交互 → 日志占位页。

**过程中发现并修复的 bug**

1. 标题栏（拖拽手柄）内的按钮点击失效：pointerdown 启动拖拽时 `setPointerCapture` 会把后续 click 重定向到标题栏，导致「返回」和「关闭」按钮在真实浏览器中点击无效 → 指针落在按钮上时跳过拖拽初始化（不捕获、不 preventDefault），按钮点击恢复正常。

### v0.2.0 — UI 骨架：菜单图标 + 常驻悬浮球 + 占位面板

**完成内容**

- 扩展菜单图标：优先走 TauriTavern 宿主 API（`ST_API.ui.registerExtensionsMenuItem`），失败时手动注入 `#extensionsMenu`，并用 MutationObserver 兜底恢复。
- 常驻悬浮球：56px 紫色渐变圆球，支持拖拽移动（位置记忆）、单击打开面板、长按 650ms 隐藏。
- 占位面板：可拖拽卡片，带关闭按钮、ESC 关闭、版本号与 GitHub 链接；关闭面板后悬浮球重新出现。
- 主题适配：面板样式跟随 SillyTavern 主题变量（`--SmartTheme*`），并带 `color-mix` 降级兜底。

**验证**

- `node --check` 语法通过。
- 自建 DOM mock 冒烟测试 22 项全部通过（覆盖 host 注册成功/失败两条路径、点击/拖拽/长按/ESC/位置持久化）。

**过程中发现并修复的 bug**

1. `bootstrap` 未声明 `async`，`scheduleBootstrapFallback` 里 `bootstrap().catch(...)` 会因 `undefined.catch` 崩溃 → 改为 `async` 并 `await registerMenuItem()`。
2. `registerHostMenuItem` 无条件返回 `true`，host 拒绝注册时手动菜单项被误删 → 改为透传 host 返回值（`result !== false`）。

### v0.3.0 — API 连接（OpenAI 兼容）

**完成内容**

- 面板新增「API 连接」区块：Base URL、API Key（可切换明文/密文显示）、「连接并拉取模型」按钮、模型下拉选择 + 手动填写模型名称。
- 设置持久化：写入宿主 `ctx.extensionSettings`（`SoulLink` 命名空间），宿主上下文不可扩展时自动降级到模块级 WeakMap 存储，读写不中断。
- 模型列表拉取：`GET {base}/models`；跨域时优先走宿主代理（`/api/backends/chat-completions/status`），代理失败（401/403/404/405 或路由不存在）自动回退直连。
- 请求细节：带 `X-CSRF-Token`（从 cookie 取）与宿主请求头，避免宿主代理 POST 撞 CSRF 返回 403；超时用 AbortController（模型列表 20s，默认 30s）。
- 响应兼容：支持 `data.data` / `data.models` / 纯数组 / 嵌套 `response` 字段，模型项支持字符串或 `id`/`name`/`model` 字段。
- 状态反馈：面板状态胶囊（idle/busy/ok/error）+ toastr 通知；错误信息带传输方式（host-proxy / direct / direct-after-proxy-fallback），并提示可手动填模型名绕过。

**验证**

- `node --check` 语法通过。
- 重启 Tavern 后手动验证：填写 Base URL / API Key → 连接并拉取模型 → 选择模型 → 重启后设置仍保留。

**下一步计划**

- [ ] 确定 SoulLink 核心功能（灵魂链接具体做什么），再填充面板内容。
- [ ] 核心功能落地后拆分模块（参考 NPC Tracker 的 scripts/ 结构）。
- [ ] 为 API 连接补充自动化测试（mock fetch 与宿主代理）。
