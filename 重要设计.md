# 重要设计

> 记录 SoulLink 的架构与关键设计决策，方便后续迭代时保持一致。
> 与「开发日志.md」（进度）和「重要开发经验.md」（踩坑）配合阅读。

---

## 1. 总体结构

当前为单文件骨架（`index.js` + `style.css`），职责划分：

- `index.js`：入口，负责 bootstrap、菜单图标、悬浮球、面板脚手架、首页视图切换与 API 连接。
- `style.css`：悬浮球、面板与 API 表单的样式、动画。
- 后续功能模块化时，按 NPC Tracker 的模式拆到 `scripts/` 子目录，`index.js` 只做引导与 UI 骨架。

## 2. 命名约定

| 类别 | 规则 | 示例 |
| --- | --- | --- |
| DOM id | `soullink-` 前缀 | `soullink-panel`、`soullink-floating-sphere`、`soullink-api-connect` |
| CSS class | `soullink-` 前缀，BEM 风格 | `soullink-panel__dialog`、`soullink-sphere.is-dragging`、`soullink-btn` |
| localStorage key | `SoulLink_` 前缀 | `SoulLink_floating_sphere_position` |
| 设置命名空间 | `ctx.extensionSettings.SoulLink` | `{ apiUrl, apiKey, model, modelOptions }` |
| globalThis 运行时 key | `__soullink_` 前缀 | `__soullink_bootstrapped__` |
| 菜单项 id | `soullink-menu-` 前缀 | `soullink-menu-item`（手动）、`soullink-menu-api`（host） |

## 3. 启动流程（bootstrap）

1. 订阅 `appReady` 事件；同时启动定时兜底重试（最多 60 次 × 500ms），应对 TauriTavern 的 `appReady` 早于扩展脚本注册的情况。
2. `bootstrap` 幂等：`__soullink_bootstrapped__` 全局标记防止重复执行；失败时复位标记允许重试。
3. 顺序：创建面板（含 API 区块初始化）→ 创建悬浮球 → 显示悬浮球 → 注册菜单项。
4. 宿主上下文统一走 `Luker.getContext()` / `SillyTavern.getContext()`，不用全局 `getContext()`。

## 4. 菜单图标：双通道 + 兜底恢复

- **主通道**：`ST_API.ui.registerExtensionsMenuItem({ id, label, icon, onClick })`（TauriTavern 支持），注册前先 await `__TAURITAVERN__.ready` / `__TAURITAVERN_MAIN_READY__`。
- **兜底通道**：手动往 `#extensionsMenu` 注入 `list-group-item` 结构（与 Tavern 原生菜单项一致），支持点击与键盘（Enter/空格）。
- **恢复机制**：MutationObserver 监听 `#extensionsMenu` 出现/重建后重插；host 注册成功后移除手动项，避免菜单出现两条。
- **去重**：`hasMenuEntry()` 按 id（手动/API）与文本双重判断，任何路径重复执行都不会产生重复项。

## 5. 悬浮球交互模型

- 固定定位（`z-index: 10000`），56px 圆形，Pointer Events 实现拖拽。
- **位移阈值 8px**：超过才算拖拽，否则视为点击——避免点击误触发拖拽。
- **长按 650ms**：隐藏悬浮球（防误触的隐藏方式）。
- **位置持久化**：`localStorage` 保存，恢复与 `resize` 时都 clamp 在视口内。
- **入口闭环**：悬浮球隐藏后，仍可通过菜单图标打开面板；关闭面板时悬浮球以 grow 动画重新出现——保证永远有入口找回面板。

## 6. 面板设计

- 固定定位卡片（`z-index: 9999`），默认右上角，可拖拽（标题栏为拖拽手柄），位置仅会话内记忆（dataset）。
- 关闭方式：关闭按钮、ESC 键；关闭后悬浮球重新出现。
- 样式跟随 Tavern 主题变量（`--SmartThemeBlurTintColor` / `--SmartThemeBorderColor` / `--SmartThemeBodyColor`），`color-mix` 前保留纯色降级。
- 内容按「视图」组织（`.soullink-view`）：首页启动器 + 功能子视图；打开面板默认回到首页，子视图标题栏显示「返回」按钮（←），ESC 先返回首页再关闭面板。
- 首页以图标卡片网格（`.soullink-home__card`）组织入口；当前有「API 连接」「日志系统」「预设」三个图标，后续功能以新卡片追加。
- 具体表单按「区块」组织（`.soullink-panel__section`），API 连接表单位于 API 子视图内；面板外壳（拖拽/关闭/主题）保持不变。

## 7. 设置持久化

- 设置存宿主 `ctx.extensionSettings` 的 `SoulLink` 命名空间，随 Tavern 存档/设置一起保存。
- `getSettings(ctx)` 负责默认值合并（`DEFAULT_SETTINGS`），缺字段自动补齐并触发保存。
- **降级兜底**：TauriTavern 的 context 可能是冻结/不可扩展对象，赋值会抛 TypeError → 自动回退到模块级 `WeakMap` 存储（`FALLBACK_SETTINGS_STORE`），保证读写不中断。
- 保存分两种：输入即时保存用 `saveSettings`（debounced），关键操作（如拉取模型后）用 `saveSettingsImmediate`。

## 8. API 连接设计

- **Base URL 归一化**：`getApiBase` 去掉尾部斜杠，并剥离 `/chat/completions`、`/models` 后缀，允许用户粘贴完整接口地址。
- **跨域判定**：`isCrossOriginUrl` 对比 `location.origin`；跨域才走宿主代理，同域直连。
- **宿主代理**：POST `/api/backends/chat-completions/status`，携带 `X-CSRF-Token`（cookie 解析）与宿主请求头（`getRequestHeaders`），避免 403 被误判为上游故障。
- **回退链**：代理失败（401/403/404/405 或 `cannot post` / `not found` / `no route` / `ENOENT`）→ 直连 `GET {base}/models`；错误信息带传输方式，便于排查。
- **超时**：AbortController 实现（模型列表 20s，默认 30s），`fetchText` 统一封装。
- **响应兼容**：`data.data` / `data.models` / 纯数组 / 嵌套 `response` 字段；模型项支持字符串或 `id`/`name`/`model` 字段；空列表报错并提示手动填写。
- **手动兜底**：不支持 `/models` 的渠道可手动填模型名，模型名同样持久化。

## 9. 事件订阅

- 统一走 `ctx.eventSource.on(...)`，用全局 key 记录 handler，重复订阅前先 `removeListener`，避免扩展热重载后重复绑定。
- handler 内 try/catch 包裹，Promise 结果统一 `.catch` 记日志，不让单个事件失败中断后续流程。

## 10. 日志系统（v0.5.0）

- **捕获源**：① `console.log/info/warn/error/debug`（包装前先 bind 保存原始方法，SoulLink 内部经 `logApp` 走原始 console，避免递归与重复；包装器与窗口监听「一次安装、动态派发」到全局 state 的 handlers，扩展热重载时只替换 handlers 指向当前实例，日志不中断、不重复包装）；② `window error` / `unhandledrejection`（未捕获错误，来源 `window` / `promise`）；③ 宿主关键事件（来源 `event`，`debug` 级别）；④ SoulLink 自身埋点（来源 `soulink`）；⑤ 外部扩展经 `SoulLinkLog` API 写入（来源 `external`）。
- **存储**：纯内存环形缓冲，默认 2000 条（设置 `logMaxEntries`，100~20000 可调）；`logAutoScroll` 持久化跟随偏好；日志本身不落盘、不随设置保存。缓冲与暂停/序列状态放在全局共享对象 `__soullinkLogState__`（v0.8.5 起），扩展热重载后新旧实例共用同一份数据，历史不分裂、暂停状态不失联。
- **噪音治理（v0.8.3 / v0.8.9）**：① 全局脱敏——`pushLogEntry` 对所有来源的 message/detail 统一脱敏（`sk-` 密钥、JSON 字段 `apiKey/ZapiKey/password/proxy_password/authorization/token`、`Bearer`、`tauri-invoke-key`），防 console 捕获把密钥带进日志；② 噪音过滤——console 按消息前缀过滤 debug+info 级别（`[WI]` / `[Prompt Template]` / `getRegexedString: Skipping script` / `Event emitted:` / `WI entry` / 元数据保存 / 生成流程内部等，warn/error 永不误伤），network 按 URL 模式过滤 Tavern 内部非模型调用（`ipc.localhost/*`、`/api/chats/save`，模型 API 调用永不误伤）；设置 `logConsoleNoise`，UI 可开关；③ 连续重复折叠——相同 level/source/message/detail 连续出现时合并为一条并显示 `×N`（时间取最后出现时刻），缓冲与导出均生效。
- **条目结构**：`{ id, ts, time(HH:MM:SS.mmm), level, source, message, detail? }`；序列化安全（Error / BigInt / 函数 / DOM / 循环引用），单参数 800 字符、整条 4000 字符截断；`detail` 上限 20000 字符，展开行时展示，复制/导出时缩进附带。
- **渲染**：打开日志视图整表重建（过滤后），运行中增量追加单行；DOM 最多 1000 行（`LOG_RENDER_CAP`），超出显示计数提示；暂停时只入缓冲并累计新增数（徽标 +N），并记录暂停时刻可见边界 `pausedAtId`——暂停状态下任何整表重建只渲染 `id <= pausedAtId` 的快照（过滤/搜索/开关噪音/重开视图都不会泄漏暂停期间的新日志），恢复时一次性整表重建显示全部缓冲。
- **UI**：级别 chips（带计数）+ 来源筛选下拉（网络请求 / SoulLink / 控制台 / 宿主事件 / 外部扩展 / 页面错误 / Promise 拒绝）+ 搜索（防抖 120ms，覆盖 detail）+ 暂停/继续（新日志先缓存 +N，不追加、不泄漏；继续后一次性显示）+ 跟随/回到最新（仅列表在底部附近时自动滚动，上翻不打断阅读，开启时立即滚到底）+ 清空 + 复制（纯文本）+ 导出（JSON，含元数据，暂停期间缓冲也全量导出）；面板进入日志视图加宽到 540px（`.is-log-mode`）。
- **对外 API**：`globalThis.SoulLinkLog`（debug/info/warn/error/log/clear/getEntries/setMaxEntries）。
- **网络捕获（v0.8.1）**：全局 `fetch` 包装「一次安装、动态派发」（与 console 同款热重载策略），透传原始请求不改变行为；请求与响应均记录方法/URL/头/体，响应体经 `response.clone()` 异步读取（不阻塞、不消费原始流），读到 20000 字符上限即取消克隆分支，SSE 长流不会无限缓冲；`Authorization` / `api_key` / `key` / `password` / `proxy_password` / `token` / `cookie` 一律脱敏为 `***`；级别映射：请求与 2xx/3xx 记 `debug`，4xx 记 `warn`，5xx 与网络失败记 `error`。
- **设计取舍**：网络捕获只做「旁路观察」（clone 读取 + 脱敏 + 截断），不拦截、不改写任何请求/响应，避免影响 Tavern 自身网络层。

## 11. 预设系统（v0.6.0）

- **定位**：集中管理四个子系统（档案系统 / 档案预筛 / 角色扮演 / 角色扮演预筛）的子 agent 提示词，是核心功能的前置数据入口。
- **默认提示词**：`DEFAULT_PROMPTS` 冻结对象内嵌于 `index.js`，四个 key：`archiveSystem` / `archivePreScreen` / `roleplaySystem` / `roleplayPreScreen`；源文件含 \`\`\`json 反引号，模板字符串内以 \`\` 转义保存。
- **默认提示词优化（v0.9.5）**：四份提示词统一「输出契约前置 + JSON 格式强化」——回复必须且只能是 JSON 对象、禁止任何多余文字；「角色扮演」风格示例改为 JSON 包裹（示例即格式）；两份预筛提示词要求角色名与名单逐字一致（程序按名字精确匹配）；「档案系统」补充 remove/update 的 id 必须来自 current_profile 已有 id；「尽快返回 / 精简步骤」改为「直接给出结论，不要输出思考过程或多余说明」。旧默认文本（未自定义）在 `getSettings` 里逐字比对后自动迁移。
- **记忆质量标准（v1.0.7）**：「档案系统」默认提示词新增 memory 硬性质量标准——自包含（人物用全名、本人用「我」，禁止无指代代词；称呼映射到全名，无名角色用稳定身份称呼）、带时间锚点（故事时间线位置，参照只能来自对话或档案已有事件）、事件+意义（含角色反应/感受/态度，须有对话依据）、一条一件事（复杂事件不超过两句）、日常互动合并为习惯性条目；判断要点新增「记忆质量优先于数量」「同一事件用 update 合并、冲突用 update/remove 纠正」「关系状态记 relationships、具体事件记 memory，不重复」「各角色记录自己视角的版本」「档案初始化」（档案近乎为空时以世界书设定卡为权威来源认真完整初始化：身份/性格/家庭背景/人际关系/世界观逐项写入对应分节、过往经历写入记忆，设定卡即确凿依据、不受「依据模糊倾向不新增」约束）；memory 定义扩展为含设定卡中记载的过往经历；输出契约 add 说明与档案分析输入引导同步补充记忆质量标准。默认「角色扮演」提示词输入说明同步新增 memory 消费指令（记忆分节是角色记得的经历与感受，扮演时自然引用）。上下文长度治理（条目上限 / 摘要 / 存量合并）不在本版本处理，由后续自动摘要插件负责。旧默认文本（未自定义）在 `getSettings` 里逐字比对后自动迁移（新增 `LEGACY_DEFAULT_ARCHIVE_SYSTEM_V2` 与 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V4`）。
- **角色扮演行为倾向推演重构（v1.0.8）**：「角色扮演」默认提示词由「性格常量描述」升级为「情境函数推导」——新增【推演流程】六步（在场判定 → 情境定性 → 动机检索 → 记忆触发 → 形成假设 → 行动倾向，心里按顺序过、不写出来）；「下一步行动」升级为具体行动倾向（对象 / 方式 / 时机至少说清两样，禁止空泛意图，允许条件分支）；新增【质量红线】（不剧情复读 / 不总结腔 / 记忆是原因不是背景板 / 篇幅通常 3-6 句）；新增冲突场景示例与原有日常示例构成双端示范；<npc_behavior> 动机库保留并统一「选择性外向」格式，作为「动机检索」的推导入口。注入块 <NPC_Deduction> 同步加两条护栏（每个角色的独白只属于 TA 自己、与既定设定冲突时以设定为准）。迁移：新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V4`（= v1.0.7 已发布默认），修正 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V5`（误填为新文本 → 改回 v1.0.7 已发布文本），并把 v1.0.7 的 archive / roleplay 默认内容同步回 js/ 源码（此前源码落后于发布产物，直接重建会回退）。
- **角色扮演预筛 Gate 产品化重构（v1.0.8）**：「角色扮演预筛」默认提示词由「排除偏向」改为「召回偏向 + 精度排除」——职责定义扩展为「最后一条用户消息引发的下一轮剧情中会开口、行动或产生重要内心反应的角色」；判断要点重写为「必须列入（开口/被点名/动作对象）/ 应列入（在场且受本轮事件直接影响、目击冲突秘密、情绪被触发、带行动意图与情绪积累可能爆发）/ 不列入（只是被提及、不在场、纯背景）」三层；偏差修正为「拿不准时倾向列入：漏选比多选更伤体验」（与档案预筛 v0.9.8 的召回偏向对齐）；新增软上限「按戏份轻重排序，最多保留 6 个」治理并发推演成本与注入篇幅；输入说明补充「末条用户消息是下一轮剧情的直接触发点」与场景连续性判断。Gate 剧情段引导消息与输出契约段同步。旧默认文本（未自定义）自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V4`）。
- **角色预筛 Gate PM 审查迭代（v1.0.11）**：排序判据明确化（戏份轻重依次看：与末条用户消息的直接关联 → 情绪积累与行动意图强度 → 在场互动频率）；输入说明新增开放式场景引导（末条未点名任何人时以在场角色的情绪积累与行动意图为准，在场且能感知事件者视为受影响）；判断要点新增「在场是硬性前提」（明确不在场不列入，未明确去向按最近一次出场推断，不在场角色获得独白会误导主模型、比漏选更伤）；Gate 调用参数对齐自动档案 Gate（maxTokens 1024 + temperature 0.1，控制发送前阻塞链路输出）；「上一轮角色扮演」视图新增「预筛原文（Gate 返回）」区块（截断 400 字），0 入选/漏选可审计。
- **角色扮演提示词措辞收尾修正（v1.0.13）**：发布前整份走查修 6 处——开场「行为倾向」→「行动倾向」且「解读、情绪」→「情绪反应、解读」（术语与三要素顺序对齐）；「行动倾向」删与「形成假设」重复的解读步骤；动作列表 TA/他混用统一为 TA；「悲喜情绪」→「悲喜时刻」；示例一结尾「下一步行动，我打算……」总结腔改为自然心流（消除与「不写总结腔」红线矛盾，保留条件分支示范）；输入说明明确「最后一条通常是玩家刚做出的言行，是本次推演的触发点」。旧默认自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V6` = v1.0.12 已发布默认）。
- **角色预筛 Gate 提速优化（v1.0.12）**：提示词开场追加「高频轻量调用，必须最快返回：不要输出分析过程、不要解释、不要任何多余文字，回复越短越好」，输出契约首条追加「回复越短越好」；请求体 6 条消息精简为 4 条（名单/剧情引导与数据块合并），JSON 紧凑序列化（省缩进 token），减少消息轮次与输入体积（对齐 v1.0.9 档案预筛 Gate）；措辞微调：「比漏选更伤」→「宁可漏选也不可错选不在场角色」、「拿不准时倾向列入」→「拿不准是否有戏份时倾向列入」，消除「漏选」两处维度混淆（错选不在场 > 漏选有戏份 > 多选浪费）。
- **角色预筛 Gate 候选集措辞修正（v1.0.14）**：明确「<Registered_Characters> 是唯一的候选集」——只从名单中挑选，名单之外的角色（即使出现在对话里）一律不列入；剧情块只用于判断名单中的角色（逐个判断是否在场/被点名/有戏份），不从对话中寻找名单之外的角色；判断要点新增「逐个过一遍名单，而不是从对话里找角色」；Gate 名单段/剧情段/输出契约段引导消息同步。
- **预筛提示词强化（v0.9.6）**：两份预筛提示词补充「对话里用简称/昵称时也必须输出名单全名」；角色扮演预筛补充「被直接点名包括作为动作对象（被抱起/放下/触碰/呼唤等），必须列入」。
- **预筛判断要点对齐下游（v1.0.8）**：「档案预筛」判断要点重写为「必须列入/不列入」清单——参与互动、在场目击重要信息、透露自身新信息/获知世界观规则/关系背景变动三类必选；只排除「只是被提及」与「在场但纯附和寒暄无新信息」；保留召回偏向（拿不准倾向列入）。对齐下游「档案系统」的 memory（在场目击）、fields（年龄/职业/身份）、worldview（对话中获知新设定）触发点，修复沉默目击者漏选导致的档案静默过期。旧默认自动迁移（`LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V5` = v0.9.8 已发布文本）。
- **角色预筛模块化架构重写（v1.0.18）**：「角色预筛」默认提示词按《提示词架构.md》框架重写为「任务 → 输入 → 推演 → 判定标准 → 输出 → 质量红线」六模块——输出契约不再前置（取消「先读这里」，模块顺序即阅读顺序，与执行顺序一致）；判断要点拆为【推演】（逐角色三步：在场判定 → 戏份判定 → 归类，在场推断规则「未明确去向按最近一次出场」移入第 1 步）与【判定标准】（必须/应/不三层分类表，动作对象补充并入「必须列入」）；排序上限（最多 6 个 + 戏份轻重判据）从判定规则拆出归【输出】；两条偏差底线归【质量红线】并分维度表述（在场判定从严：宁可漏选不可错选不在场；戏份判定从宽：拿不准倾向列入），消除「漏选」维度混淆；候选集约束收敛到【输入】一处、最快返回收敛到【任务】一处（原 4 处/3 处重复）。旧默认自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V5` = v1.0.16 已发布默认）。
- **档案预筛模块化架构重写（v1.0.17）**：「档案预筛」默认提示词按《提示词架构.md》框架重写为「任务 → 输入 → 判断 → 输出」四模块——候选集规则收敛到【输入】（名单外一律忽略，只出现一次）；「值得记录」的定义与下游写入点（fields/memory/worldview/relationships/family）对齐，并在【任务】点明产物用途（交给档案系统决定本轮更新哪些角色）；判断要点压缩为编号入选标准 1-3，「拿不准时」直接引用条件编号（条件 1/2 倾向列入，漏选会让新信息错过入库）；【输出】成为唯一最高优先级契约（删除「名单外不得列入」与「回复越短越好」的重复表述）；「最快返回」只保留在【任务】一处。旧默认文本（未自定义）自动迁移（新增 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V7`）。
- **角色扮演「玩家已行动、聚焦反应」措辞修正（v1.0.10）**：推演发生在玩家发送消息之后，玩家已经做出了行为——默认提示词由「推测玩家下一步」框架改为「对玩家已做行为的解读与回应」框架：开场职责定义、形成假设、想法、行动倾向、下一步行动五处同步锚定「玩家刚才的行为」；条件分支保留为局势有变数时的备案（必须有一个明确的默认行动），「对象、方式、时机至少说清两样」保留为括号内备案。旧默认自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V5` = v1.0.8/v1.0.9 已发布默认）。
- **角色扮演提示词模块化架构重构（v1.0.16）**：「角色扮演」默认提示词按「任务 → 输入 → 推演 → 动机参考 → 输出 → 质量红线 → 示例」新架构重写——输入说明前移为第二模块（材料清单 + 每份材料的用法与信息边界）；推演六步保留、只留思考引导（「对象/方式/时机至少说清两样」收敛到【输出】为唯一权威表述）；<npc_behavior> 动机库升级为【动机参考】模块并移到推演之后（消除「下方」交叉引用）；输出契约成为唯一「最高优先级」（消除与口吻要求的双最高优先级冲突）；三要素 / 认知状态 / 口吻 / 红线各归其位，同一禁令只保留一处；新增《提示词架构.md》记录现状拆解、通用框架与可维护性指南（框架可迁移到其余三份预设提示词）。旧默认自动迁移（新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V7` = v1.0.13 已发布默认）。
- **档案预筛 Gate 提速（v1.0.9）**：默认提示词强化「最快返回」语义（开场「这是高频轻量调用，必须最快返回：不要分析、不要解释、不要任何多余文字，回复越短越好」+ 输出契约首条追加「回复越短越好」）；Gate 请求体由 6 条消息精简为 4 条（引导与数据块合并），名单/剧情 JSON 紧凑序列化省输入 token；Gate 调用单独收紧 maxTokens 1024（原全局 8192）与 temperature 0.1，防长篇输出拖慢高频调用。旧默认自动迁移（新增 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V6` = v1.0.8 已发布文本）。
- **持久化**：写入宿主 `ctx.extensionSettings.SoulLink.prompts`（与 API / 日志设置同一命名空间），`getSettings` 对 prompts 做深合并：顶层缺失、值为 null / 非对象、或个别 key 缺失时自动用默认值补齐并触发保存，兼容旧存档与部分自定义。
- **内存暂存**：`presetUnsaved`（key → 文本）记录未保存修改，标签页切换、视图切换都不丢失；保存 / 恢复默认后清除。dirty 判定用 `!== undefined`（空字符串编辑也算未保存）。
- **状态模型**：每份提示词三态——默认内容（未自定义）、已保存的自定义内容、未保存的更改；状态胶囊 + 标签页圆点 + 保存按钮可用性三者联动；首页卡片汇总「已自定义 N 份」。
- **恢复默认**：还原并立即保存；只要有自定义内容（已保存或未保存）就弹确认框；已是默认内容时按钮置灰。
- **视图宽度**：预设视图与日志视图共用加宽机制（`PANEL_WIDE_MODES` 映射 `is-log-mode` / `is-preset-mode`），进入视图时只保留对应的加宽类。

## 12. 注册系统 + 档案系统（v0.7.0）

- **存储结构**：`ctx.extensionSettings.SoulLink.archives[chatKey][角色名] = 档案`，随 Tavern 设置持久化；`getSettings` 对 `archives` 做缺失兜底（非对象时重置为空对象）。
- **按聊天绑定**：`chatKey` 优先取 `ctx.chatId`，缺失时回退 `characterId` / `groupId`；切换聊天（`chatChanged` / `groupSelected`）后名单与档案各自独立，事件触发时自动刷新激活视图与首页状态。
- **档案结构**：标量字段 `fields`（name / age / gender / occupation）+ 五个列表分节（personality / worldview / family / relationships / memory，条目 `{ id, content }`）；分节采用「增量 diff」维护，与 `archiveSystem` 提示词的输出契约一一对应。
- **注册/注销**：注册即创建空档案（fields.name 预填）；注销带确认框并删除该角色档案数据。名单渲染用 `textContent` 构建 DOM，避免角色名注入 HTML。
- **手动编辑**：编辑态 = 标量输入框 + 分节逐行 textarea；保存时按行重建条目，内容未变的条目沿用原 id（保证 AI 后续 update/remove 引用不失效），新增行自动续号（`i{n}`）。
- **档案分析**：请求体按「提示词 → 剧情 → 世界书 → 输入」四段式组织（v0.8.4）——system 档案提示词 → 剧情段（引导消息 + `<Recent_Messages>` 块，最近 4 条消息 XML 包裹）→ 世界书段（引导消息 + `<World_Info_*>` 块，见第 15 节）→ JSON 输入（character / current_profile / turn_index）；输出按约定 JSON 增量应用。v0.8.6 起世界书按三类处理：① 与角色同名的条目视为该角色「本人设定卡」，是权威设定来源，可直接补全其标量字段/性格/世界观/家庭背景/人际关系；② 对所有人生效的普适世界设定（社会秩序、认主体系、超自然规则等）视为该角色默认知晓的常识，写入其世界观；③ 其他角色的个人条目与私密信息仅作背景参考，只有该角色确实获知的内容才写入档案。
- **并发与取消模型**：单角色分析各自独立异步任务（互不阻塞），每个任务持有自己的 AbortController；按钮状态由「分析中」驱动——角色分析中按钮显示「取消分析角色」，任一角色分析中「分析全部角色」显示为「取消分析全部角色」，全部结束（完成/失败/取消）后自动复原；「分析全部」用 `Promise.allSettled` 并发全部，按 ok / error / cancelled / skipped / busy 汇总 toast；取消通过外部 AbortSignal 贯穿代理与直连传输（`fetchText` 与超时信号合并，优先 `AbortSignal.any`）；注销角色前先中断其在途分析。**v1.0.3 曾做并发降级、v1.0.5 已移除**：v1.0.3 曾对 `api.deepseek.com` 把批量档案更新改为串行，规避「并发下 200 + 空内容」；v1.0.5 定位到真实根因是模型把答案写进 `reasoning_content`（content 留空）而非并发问题，串行机制已移除，恢复统一并发（`Promise.allSettled`），取消语义不变。
- **AI 对话传输**：复用 API 连接的 Base URL / Key / 模型；`POST {base}/chat/completions`；跨域走宿主代理 `/api/backends/chat-completions/generate`（TauriTavern 专用接口，`custom_include_headers` 值需带引号 `Authorization: "Bearer xxx"`，模型/消息/温度直接平铺在请求体），代理返回非 JSON（如 SPA 首页 HTML）、无内容 JSON（200 + choices 为空 / content 与 reasoning_content 皆空）或 401/403/404/405 时回退直连；content 为空但 reasoning_content 有内容时视为可用（v1.0.5，deepseek-v4-flash 等模型偶发把答案写进思维链字段）；代理返回 `{error}` 时直接透传报错；60s 超时；兼容嵌套 `response` 与 `choices[0].text`。**v1.0.2 自动重试**：上游瞬态故障（无内容 JSON、429/5xx、busy/overload 类错误信封）按 `CHAT_COMPLETION_MAX_ATTEMPTS=3` 次、`600ms × 次数` 退避自动重试（重试间隙可取消），耗尽后错误信息标注「已自动重试 N 次」；401/400/404 等配置类错误不重试。
- **响应解析**：`parseAgentJson` 剥 ```json 围栏与前后杂文；diff 应用时标量直接覆盖，分节 add（去重、优先沿用 AI 提供的唯一 id）/ remove（按 id）/ update（按 id 改写）。

## 13. 自动档案维护（v0.8.8，v0.8.10 修复事件订阅）

- **触发时机**：订阅宿主 `generationEnded`，每轮生成结束且确实产出新的 AI 回复后自动启动；不轮询、不猜末条是否写完（与 NPC Tracker 的 `installProfileGenerationTrigger` 同思路）。
- **误触发防护（v1.0.6）**：宿主事件总线是全局的，其他插件可自行广播 `generationEnded`（翻译/摘要/续写等完成自己的 API 调用后）。因此 v1.0.6 起增加主生成流程跟踪：订阅 `generationStarted` 记录「生成开始时的末条消息签名」，`generationStopped` / `chatChanged` / `groupSelected`（切换/加载聊天）时清空；`onAutoArchiveGenerationEnded` 只有「有 `generationStarted` 配对 且 末条消息签名确实变化（本轮产出了新 AI 回复）」才启动，否则判定为插件广播/事件重放直接跳过。签名在「末条是 AI 回复」之后才消费，插件在生成中途广播不会吃掉主流程的跟踪。
- **事件订阅（v0.8.10 修复）**：宿主事件名必须经 `ctx.event_types` 解析——TauriTavern 的 eventSource 需要 `ctx.event_types.GENERATION_ENDED` 的值，裸字符串 `'generationEnded'` 不会触发（v0.8.8 的自动维护因此从未启动）；`onHostEvent` 与 `initHostEventLogging` 统一走 `resolveHostEventType()`，取不到 `event_types` 时回退原字符串。另加事件源看门狗：TauriTavern 在主生成后可能重建 `ctx.eventSource`，每 4s 对比身份、被换后自动重挂订阅（NPC Tracker 同款思路）。
- **Gate 输入最小化**：只送「已注册角色名单 + 最近 4 条消息」（名单块 `<Registered_Characters>` + 剧情块 `<Recent_Messages>`，严格 `ARCHIVE_RECENT_MESSAGE_COUNT=4`），不携带档案、世界书等任何其他上下文；system 提示词复用「档案预筛」（`archivePreScreen`），输出契约 `{ "characters": [...] }`；返回名单与注册名单求交集。
- **Gate 请求体（v0.9.4 分段化）**：按「预筛提示词 → 名单块 → 剧情块 → 输出契约」四段式组织——system 档案预筛提示词 → 名单段（引导消息 + `<Registered_Characters>` 块）→ 剧情段（引导消息 + `<Recent_Messages>` 块，最近 4 条）→ 输出契约段（`{ "characters": [] }` 模板）；名单段强调「名单之外一律视为未注册、不参与本轮预筛」，剧情段强调「只列本轮确实获得新信息/新经历、或关系背景发生变动的角色」；默认提示词「输入说明」同步改为描述两个 XML 块（v0.9.4 及更早旧默认文本自动迁移）。 v1.0.9 起精简为 4 条消息（引导与数据块合并为同一条 user 消息），名单/剧情 JSON 紧凑序列化，Gate 调用单独收紧 maxTokens 1024 与 temperature 0.1。
- **稳定性设计**（规避 NPC Tracker 已知缺陷）：
  - 末条签名去重（`role|name|content`）：同一末条被宿主反复触发（自动续写/事件重放）只处理一次；签名在 Gate 开跑前标记，Gate 失败/为空也算本轮已处理，避免同一末条反复重试轰炸；
  - 运行锁：上一轮 Gate/分析还在飞时新事件直接跳过，杜绝并发 API 风暴；
  - 中断检查：`streamingProcessor.abortController.signal.aborted` 时跳过，不对半截回复发起分析；
  - 名单交集：Gate 返回的名字必须与已注册名单求交集，未知名字一律丢弃（对应 NPC Tracker `parseGateNames` 的归一化逻辑）；
  - 失败即跳过本轮：Gate 调用失败不降级、不重试，本轮视为已处理。
- **复用现有分析**：入选角色直接并发调用现有 `analyzeCharacter`（含世界书注入与增量更新），不重新开发；结果按 ok/error/cancelled/skipped/busy 汇总日志与 toast。
- **开关**：`settings.SoulLink.autoArchiveEnabled`（默认 true），档案系统工具栏「⚡ 自动维护」按钮即时切换并持久化。

## 14. 待定设计

- 是否需要独立 `settings.html` 设置页（当前设置都在面板内）。
- 是否需要服务端/后端能力（当前纯前端）。

## 15. 世界书系统（v0.8.0）

- **定位**：读取 SillyTavern 当前激活的世界书，触发规则 100% 跟随酒馆（绝不自己实现触发规则），并把触发结果按酒馆位置注入档案分析提示词；提供逐条排除能力。
- **权威触发**：调用宿主公开 API `ctx.getWorldInfoPrompt(scanChat, maxContext, true)`（第三参 dry run，不派发 `WORLD_INFO_ACTIVATED` 等副作用事件）。`scanChat` 的构造与酒馆生成时一致：过滤 `is_system` 消息、按 `#world_info_include_names` 复选框（默认开）带 `名字: 内容` 前缀、**最新消息在前**（酒馆引擎按深度 0 = 最新消息扫描）。
- **条目级数据**：TauriTavern fork 的 `getWorldInfoPrompt` 额外返回 `worldInfoActivation.entries`（每个激活条目的 `world` / `uid` / `displayName` / `constant` / `content` / `position`，内容已做宏替换）。原版 ST 无此字段 → 降级为「原样注入酒馆拼好的 worldInfoBefore/After 文本」，条目排除不可用（视图横幅提示 + 复选框禁用）。
- **注入组装**：`buildPositionedWorldInfoBlocks` 按位置分组条目（before 进开头块、after 进结尾块，AN/深度/示例/出口进 `<World_Info_Extra>` 补充块），激活顺序 `unshift`（低 order 在前）；排除名单在此步过滤。世界书块作为独立引导消息置于剧情段之后、JSON 输入之前（v0.8.4），无注入时不发该消息。
- **排除存储**：`settings.SoulLink.worldInfo.excluded = { [书名]: [uid...] }`（v0.7 的 archives 同级命名空间）；身份键 = `书名 + uid`（酒馆自己的稳定身份，`allActivatedEntries` 的 Map key 就是 `${world}.${uid}`），不用显示名——显示名可重复、可被改写，NPC Tracker 的「书名 :: 条目名」匹配需 sanitize 各种脏数据，属于脆弱设计。
- **视图**：世界书视图 = 状态胶囊（引擎模式 · 书数 · 排除数）+ 聊天标签 + 「清除排除」 + 「↻ 刷新」 + 横幅（降级提示）+ 按书分组的条目列表。条目行：排除复选框、显示名（comment 优先 → 首个关键词 → uid）、徽章（常驻 / 位置 / 本次触发 / 已排除）；「本次触发」来自最近一次扫描的激活结果。
- **激活书名单**：激活结果里出现的书（必然激活）∪ 角色主书（`characters[characterId].data.extensions.world`，`/getcharbook` 兜底）∪ 聊天绑定书（`chatMetadata.world_info`）∪ 人设书（`powerUserSettings.persona_description_lorebook`）∪ 全局书多选框（`#world_info`，存在时读取）。条目经 `ctx.loadWorldInfo(name)` 读取。
- **并发防护**：渲染带序号守卫（`worldBookRenderSeq`），排除勾选 / 刷新 / 切聊天触发的并发渲染只允许最新一次写 DOM。
- **与档案系统集成**：`buildArchiveAnalysisMessages` 为异步，`resolveWorldInfoForAnalysis()` 返回 `{ mode, before, after, counts }`；payload 不再携带 `world_info_background`（避免与位置注入重复烧 token），v0.8.4 起 `recent_messages` 同样移出 payload 独立为 `<Recent_Messages>` 块；默认「档案系统」提示词描述各标记块并区分「本人设定卡 / 普适世界设定 / 私密信息」三类（v0.8.7）；每次分析记录注入模式与条目统计到 SoulLink 日志。

## 16. 剧情前置 NPC 推演（v0.9.0）

- **定位**：用户点击发送后、主模型生成前，先推演当前剧情中活跃 NPC 的即时心理与行为倾向，以统一格式注入提示词后再恢复生成——提高角色一致性、主动性与「活人感」。复用预设系统里就绪已久的「角色扮演预筛」「角色扮演」两份提示词。
- **触发与阻塞机制**：订阅宿主 `messageSent`，监听器**返回 Promise**。宿主 EventEmitter（TauriTavern 与 SillyTavern 同源）的 `emit` 会 `await` 每个监听器，`sendMessageAsUser` 又 `await` emit，因此推演期间主模型请求不会发出。**不依赖 `makePause`**（新版 EventEmitter 已无此 API）；监听器必须直接返回 promise，普通 `onHostEvent` 包装（捕获后不返回值）无法阻塞，故 messageSent 走专用 `installNpcDeductionMessageSentHook`。
- **误触发防护（v1.0.6）**：宿主事件总线是全局的，其他插件（QuickReply 程序化发送、自动回复脚本等）可自行广播 `messageSent`。`onNpcDeductionMessageSent` 接收事件载荷：当载荷为字符串时与末条用户消息内容比对，不一致即判定为插件广播直接跳过；载荷非字符串（宿主格式差异）时无法校验，按原有逻辑放行。手动发送（用户点击发送）的载荷与末条消息一致，不受影响。
- **Gate 输入最小化**：只送「已注册角色名单 + 最近 4 条消息」（名单块 `<Registered_Characters>` + 剧情块 `<Recent_Messages>`，严格 `NPC_DEDUCTION_RECENT_COUNT=4`），不携带档案、世界书等任何其他上下文；system 提示词复用「角色扮演预筛」（`roleplayPreScreen`），输出契约 `{ "characters": [...] }`；返回名单与注册名单求交集（精确匹配，去空白后逐字一致；v0.9.6 起 0 入选时日志附模型返回原文，简称/昵称由提示词约束输出名单全名，不做代码级容错）。
- **Gate 请求体（v0.9.3 分段化）**：按「预筛提示词 → 名单块 → 剧情块 → 输出契约」四段式组织——system 角色扮演预筛提示词 → 名单段（引导消息 + `<Registered_Characters>` 块）→ 剧情段（引导消息 + `<Recent_Messages>` 块，最近 4 条）→ 输出契约段（`{ "characters": [] }` 模板）；名单段强调「名单之外一律视为未注册、不参与本轮预筛」，剧情段强调「只列本轮会开口、被点名或明显有戏份的角色」；默认提示词「输入说明」同步改为描述两个 XML 块（v0.9.2 及更早旧默认文本自动迁移）。
- **并发推演**：入选角色并发调用，每个角色独立 API 请求，只提供「该角色档案（`serializeArchiveForPrompt`）+ 最近 4 条消息」，不带其他角色档案、不带世界书、不带完整聊天记录；system 提示词复用「角色扮演」（`roleplaySystem`），输出契约 `{ "character", "monologue" }`，角色名不匹配 / 空独白视为失败。
- **推演请求体（v0.9.2 分段化）**：按「提示词 → 档案块 → 剧情块 → 输出契约」四段式组织——system 角色扮演提示词 → 档案段（引导消息 + `<Character_Profile>` 块，`serializeArchiveForPrompt` 序列化）→ 剧情段（引导消息 + `<Recent_Messages>` 块，最近 4 条）→ 输出契约段（角色名 + `{ character, monologue }` 模板）；引导消息强调「档案是唯一身份依据」「只有亲历/被告知/目击的内容才可知」，与提示词的信息盲区要求呼应；默认提示词「输入说明」同步改为描述两个 XML 块（v0.9.1 旧默认文本自动迁移）。
- **角色扮演提示词（v0.9.1 重写，v0.9.2 输入说明同步，v0.9.5 输出契约强化）**：默认「角色扮演」提示词把「口吻」设为最高优先级（第一人称心流、像 TA 心里自言自语，禁止分析报告 / 要点列表 / 书面总结腔），硬性要求三要素（心情 / 想法 / 下一步行动，融入独白不列点）与两种认知状态（信息盲区：只用档案 + 亲历/被告知/目击的信息、宁可误判不可全知；认知框架：身份/立场/经历决定解读方式），并内置风格示例；v0.9.5 起输出契约前置并绝对化（回复必须且只能是 `{ "character", "monologue" }` JSON 对象），风格示例改为 JSON 包裹（示例即格式，避免模型模仿散文示例直接输出正文）；旧默认文本（未自定义）在 `getSettings` 里逐字比对后自动迁移。
- **注入位置**：`setExtensionPrompt(key, text, extension_prompt_types.IN_CHAT, 0, false, extension_prompt_roles.SYSTEM)`——IN_CHAT + depth 0 在 OpenAI 路径（`populationInjectionPrompts` 对最新优先 messages 在 index 0 前插）与文本补全路径（`doChatInject` 同语义）均落在**最后一条用户消息正下方**；SYSTEM role 避免被当作 user/assistant 轮次。内容为 `<NPC_Deduction>` 统一格式块，首行为「【系统导演注 · 强制指令】本块不是剧情正文，也不是任何角色的台词、旁白或可引用文本；它是系统在发送前依据角色档案与当前剧情推演出的各角色在接下来剧情中的真实内心状态与行动意图。以下规则必须逐条执行，不得忽略、不得淡化，不得以任何理由整体弃用。」，后附六条执行规则（直接驱动 / 必须外显 / 禁止引用 / 角色隔离 / 冲突裁决保留意图改换表达 / 本轮优先级）——v0.9.1 起由「仅作参考」弱提示改为强指令，v1.1.3 升级为不可弃用的强制导演注。
- **注入清理**：`generationEnded` / `generationStopped`（`HOST_EVENT_TYPE_KEYS` 需含 `generationStopped`，TauriTavern 事件名经 `ctx.event_types` 解析）后 `setExtensionPrompt(key, '', ...)` 置空；每轮开始前也先清旧注入——防止 swipes / 重生成 / 后续轮次复用过期推演块（`GENERATION_STARTED` 在提示词组装**之前**触发，不能在此清理）。
- **稳定性设计**（延续自动档案维护的设计纪律，针对「发送前阻塞」加固）：
  - 运行锁 + 签名去重：上一轮推演还在飞时新发送直接放行（本轮内容已覆盖）；签名在轮次结束后清空，允许后续相同内容的发送重新推演；
  - 失败即降级：Gate 失败 / 无角色入选 / 全部推演失败 / 宿主不支持注入 → 不注入直接放行；单角色失败不影响其余；
  - 总超时：`NPC_DEDUCTION_TIMEOUT_MS`（45s）硬截止，共享 AbortController 中止所有在途请求并放行发送，绝不无限阻塞；
  - 能力检查：只要求宿主提供 `setExtensionPrompt`；`extension_prompt_types` / `extension_prompt_roles` 缺失时（TauriTavern 2.x 的 `getContext()` 不提供，标准 SillyTavern 提供）按已知数值常量兜底（IN_CHAT=1、SYSTEM=0）。
- **开关**：`settings.SoulLink.npDeductionEnabled`（默认 true；无注册角色 / API 未配置时零开销直接放行），首页「角色推演」视图工具栏「🎭 前置推演」按钮即时切换并持久化；视图展示最近一轮汇总（时间 / 耗时 / Gate 入选 / 成功 / 失败 / 注入角色）。

## 17. 消息正则过滤（v0.9.14）

- **定位**：四种调用（档案分析 / 档案预筛 / 角色扮演预筛 / 角色推演）都经 getRecentMessages 取「最近几条消息」作为上下文；过滤只在这一处生效，四路全部覆盖，不单独改任何调用点。
- **数据模型**：`settings.SoulLink.messageFilters = [{ id, name, pattern, flags, enabled }]`，随 Tavern 设置持久化；getSettings 迁移保证老存档自动获得默认「智绘姬」过滤（`/<image>[\s\S]*?<\/image>/g`，enabled）；用户清空列表后不再回填默认（与「未初始化」区分）。
- **过滤语义**：按列表顺序对每条消息内容逐条 replace(regex, '')，随后 trim；整条内容都被剔除（原本非空）的消息不再进入上下文，避免向子 agent 发送空内容占位。禁用全部过滤时行为与旧版完全一致（不 trim）。
- **健壮性**：单个表达式编译失败只跳过该条并告警，不影响其余过滤与调用流程；导入的条目归一化校验（名称 / 表达式 / 可编译），一律分配新 id 防止覆盖现有条目。
- **UI**：「API 连接」页底部独立区块——状态胶囊（共 N 条 · 启用 M）、新建 / 导入 / 导出按钮、编辑表单（名称 + 表达式，实时校验，支持 `/表达式/标记` 字面量与纯表达式两种写法，字面量解析跳过转义斜杠）、列表行（启停开关 / 名称 / 表达式 / 编辑 / 删除带确认）。

