# 重要开发经验

> 本文件用于记录 SoulLink 开发过程中积累的重要经验、踩过的坑和解决方案。
> 每一条按日期追加，保持简洁，只记录真正有价值、以后可能再遇到的问题。

---

## 2026-08-05

### SillyTavern 第三方扩展如何被识别

- 扩展放在 `data/extensions/third-party/<名字>/` 目录下。
- 必须包含 `manifest.json`，Tavern 启动扫描时才认得出这是扩展。
- `manifest.json` 关键字段：
  - `display_name`：列表里显示的名字
  - `version`：版本号
  - `author`：作者名
  - `js`：入口 JS 文件（如 `index.js`）
  - `css`：样式文件（如 `style.css`）
  - `homePage`：仓库地址
- **注意**：列表里版本号后面会带分支名和 commit 哈希，如 `0.1.0 (main-eb0d221)`。
- **关键坑**：manifest 里声明的 `js` / `css` 文件必须真实存在，否则扩展可能加载报错或无法识别。
- 修改 manifest 或新增文件后，需要**重启 Tavern**（或刷新扩展列表）才会生效。

### git 与 GitHub 仓库关联的要点

- 首次推送空仓库前需要：`git init` → `git add` → `git commit` → `git branch -M main` → `git push -u origin main`。
- 如果没配置用户身份，commit 会失败，需要先 `git config user.name` / `git config user.email`（本仓库内配置即可，不污染全局）。
- 使用带 token 的远程地址可以免密推送，但 token 会明文存在 `.git/config` 里，注意安全。

---

### 扩展菜单图标与常驻悬浮球（2026-08-05 追加）

- 菜单图标有两条路：
  - 优先走宿主 API：`ST_API.ui.registerExtensionsMenuItem({ id, label, icon, onClick })`（TauriTavern 支持，需先 await `__TAURITAVERN__.ready` / `__TAURITAVERN_MAIN_READY__`）。
  - 兜底手动注入：往 `#extensionsMenu` 追加 `<div class="list-group-item flex-container flexGap5 interactable">`，里面放 `<div class="fa-solid fa-xxx extensionsMenuExtensionButton"></div><span>名字</span>`，点击/回车都响应。
  - 用 MutationObserver 监听 `#extensionsMenu` 出现后重插（TauriTavern 菜单可能后建）；host 注册成功后要移除手动项，避免菜单出现两条。
- 悬浮球要点：
  - `position: fixed` + `z-index: 10000`，56px 圆形；Pointer Events 拖拽，位移超过 8px 才算拖，否则算点击。
  - 位置存 `localStorage`（key：`SoulLink_floating_sphere_position`），恢复时 clamp 在视口内，`resize` 时也 clamp。
  - 长按 650ms 隐藏悬浮球；关闭面板时再以 grow 动画出现——保证永远有入口找回面板。
- 宿主上下文不是全局 `getContext()`，而是 `Luker.getContext()` 或 `SillyTavern.getContext()`（NPC Tracker 的 host.js 就是这么取的）。
- 事件订阅用 `ctx.eventSource.on(...)`，重复订阅前先 `removeListener`（NPC Tracker 的 `replaceHostEventSubscription` 同款思路）。
- `appReady` 可能先于扩展脚本跑完，所以要加定时兜底重试 bootstrap（最多 60 次 × 500ms）。


### OpenAI 兼容 API 连接（2026-08-05 追加）

- 设置存宿主 context：`ctx.extensionSettings[SoulLink]`（apiUrl / apiKey / model / modelOptions）。
  - 部分宿主的 context 是冻结/不可扩展对象，直接赋值会抛 TypeError，要回退到模块级 `WeakMap` 存储（NPC Tracker 同款思路）。
  - 常规改动走 `ctx.saveSettingsDebounced()`；「连接」这类显式操作后要立即落盘，用 `ctx.saveSettings()`（没有就退回 debounced），避免用户刷新时防抖定时器被丢弃。
- Base URL 归一化：去尾部斜杠，并剥掉用户误填的 `/chat/completions` 或 `/models` 后缀，再拼 `/models`。
- 跨域（浏览器/WebView 里 origin 不同）优先走宿主反向代理：POST `/api/backends/chat-completions/status`，body 里带 `chat_completion_source: 'custom'`、`custom_url`、`proxy_password`、`custom_include_headers: 'Authorization: Bearer xxx'`。
  - 宿主代理请求必须带上 `ctx.getRequestHeaders()` 里的 session 头（含 X-CSRF-Token），否则撞 CSRF 防护返回 403，会被误判成上游 API 故障。
  - 代理 401/403/404/405 或 "cannot post / not found / no route / ENOENT" 时回退直连（带 Authorization 头）。
- /models 响应形态差异大：兼容 `data.data`（标准 OpenAI）、`data.models`、纯数组，以及包在 `response` 字段里的嵌套响应；模型项可能是字符串或 `{ id }` 对象。
- 模型列表只是探测请求，超时给 20s 即可；请求用 AbortController 兜超时。
- UI 上保留「模型下拉 + 手动输入模型名」双通道：部分渠道不支持 /models，下拉拉不到也能手动填了直接用。


### 日志系统（2026-08-05 追加）

- 包装 console 前先把原始方法 `bind(console)` 存下来（`console.log` 直接取再调用可能 Illegal invocation）；自己内部记日志一律走原始方法，否则会递归进包装器造成重复。
- 日志渲染必须用 `textContent`，日志内容可能来自用户输入 / 报错信息，直接 innerHTML 会注入。
- 序列化日志参数要兜底：Error 在 JSON.stringify 下是 `{}`；BigInt 会抛 TypeError；对象可能循环引用；DOM 节点会序列化成一坨属性 → 统一用 replacer + WeakSet + 截断（单参数 800 / 整条 4000 字符）。
- 日志视图别每条日志都整表重建：打开视图时重建一次，运行中增量 append 单行，超渲染上限（1000 行）从头部删；计数用 rAF 节流更新。
- 自动跟随最新 = 「滚到底部附近（<24px）时新日志 scrollTop=scrollHeight」，用户上翻后自然停止跟随，再提供一个「回到最新」按钮即可，不用做复杂的暂停逻辑。
- 环形缓冲 `splice(0, len - cap)` 即可；上限做成设置（默认 2000），改小后立即裁剪。
- 用 jsdom（`pretendToBeVisual: true` 才有 requestAnimationFrame）做冒烟测试很值：真实 DOM 事件（click / input）可以直接驱动面板交互，验证筛选、搜索、暂停、清空全链路。

### 注册系统与档案系统（2026-08-05 追加）

- 档案数据按聊天绑定：`settings.archives[chatId][角色名]`，切换聊天用 `chatChanged` / `groupSelected` 事件刷新视图；`chatId` 缺失时回退 `characterId` / `groupId`，保证 key 稳定。
- 用户输入（角色名）一律用 `textContent` 构建 DOM，禁止 innerHTML 拼接；名单、卡片、分节条目同理。
- 手动编辑分节时「按行重建、内容不变沿用原 id」：这样 AI 后续的 update/remove 引用不会因手动改动而失效；新增行用 `i{n}` 续号。
- AI 输出解析要剥 ```json 围栏与前后杂文再 JSON.parse；add 条目若带 id 且不与现有 id 冲突，沿用该 id（AI 可能引用它），冲突时自动续号。
- 子 agent 并发：每个角色一个独立 async 任务即可，天然并发；「分析全部」用 Promise.allSettled 汇总成功/失败，不要串行 await。
- 跨域对话走宿主代理 `/api/backends/chat-completions`（body 直接附带 model / messages / temperature），404/无路由回退直连；`buildHostProxyConfig` 加可选第三参 extraBody，模型列表与对话复用同一个函数。
- jsdom 冒烟测试注意：`ctx.worldInfo` 必须用页面 realm 的 Map 构造（`window.Map`），否则扩展代码里 `instanceof Map` 跨 realm 判定失败、世界书被跳过。

### TauriTavern 宿主代理接口（2026-08-06 追加）

- TauriTavern 的宿主代理接口与 SillyTavern 不同：对话生成是 `POST /api/backends/chat-completions/generate`，模型列表探测是 `POST /api/backends/chat-completions/status`（没有 SillyTavern 的裸 `/api/backends/chat-completions` 路由，POST 过去会返回 SPA 首页 HTML、状态码 200）。
- 请求体格式参考 st-chatu8：`chat_completion_source: 'custom'` + `custom_url` + `custom_include_headers: 'Authorization: "Bearer key"'`（值要带引号）+ 平铺的 `model / messages / temperature / top_p / max_tokens / stream`。
- 代理响应要防「200 + HTML」：解析前先判断响应体是否以 `{` / `[` 开头（`looksLikeJson`），非 JSON 一律视为代理不可用并回退直连；`{error}` 字段则直接透传报错，不要误当成功。
- 排查同类问题的方法：在宿主环境里找一个能正常工作的同类扩展（如 st-chatu8），直接读它的源码确认接口路径与请求体，比自己猜接口快得多。

### 分析取消与按钮联动（2026-08-06 追加）

- 每个「分析本角色」任务持有一个 AbortController，点击「取消分析角色」即 `controller.abort()`；`fetchText` 加第三参 signal，与超时信号合并（优先 `AbortSignal.any([timeout, external])`，旧环境降级为「外部 abort 转发到超时控制器」）。
- 用户取消与超时/失败要区分：取消后 `options.signal.aborted === true`，据此抛 `SoulLinkCancelError`（name 标记），分析函数 catch 里不覆盖「已取消」状态、不 toast 失败。
- 按钮文案不要用独立布尔标志维护，直接用「是否有角色处于 busy 状态」驱动（`isAnyAnalysisBusy()`）：任一分析中 → 全部按钮显示「取消分析全部角色」，全部结束自动复原，天然满足「取消全部 → 全部复原 → 全部按钮复原」的联动。
- 取消后卡片会被重新渲染（`renderArchiveCard` 原位 replaceWith），测试里点击按钮后必须重新 query 按钮/卡片节点，旧引用是已脱离 DOM 的节点，读到的文案是旧的。
- 结果汇总用「每个分析返回结果字符串（ok/error/cancelled/skipped/busy）+ Promise.allSettled」而非抛错计数，取消与失败才能分开统计。

### 世界书系统（2026-08-06 追加）

- **触发规则必须用宿主公开 API**：`ctx.getWorldInfoPrompt(chat, maxContext, isDryRun)` 就是酒馆自己的世界书引擎（扫描深度 / 递归 / 概率 / 预算 / 粘性冷却全部内置）。chat 传**字符串数组、最新消息在前**（酒馆按深度 0 = 最新扫描），要带 `名字: 内容` 前缀需与酒馆设置 `world_info_include_names`（默认开，DOM 里 `#world_info_include_names` 可读实时值）一致。第三参传 `true`（dry run）就不会派发 `WORLD_INFO_ACTIVATED` 副作用事件。
- **TauriTavern 的 `getWorldInfoPrompt` 返回里多一个 `worldInfoActivation.entries`**（每个激活条目的 world / uid / displayName / constant / content / position，position 是字符串名），这是「按条目排除」的数据基础；原版 ST 只返回 worldInfoBefore/After 文本。字段名不是 `activatedEntries`——隔壁 NPC Tracker 读 `result.activatedEntries` 恒为空，导致它的「ST 权威路径」从未生效、永远降级到自己的关键词过滤。
- **条目稳定身份 = 书名 + uid**（酒馆内部 Map key 就是 `${world}.${uid}`）；排除名单按它存，不要按显示名匹配（显示名会重复 / 被 ACU skill 化改写，NPC Tracker 还得专门写 sanitize）。
- **不要 import 酒馆内部模块**（`world-info.js` 等）：相对路径在扩展目录里很容易算错（NPC Tracker 的 `../../../../world-info.js` 实际指向不存在的路径，只能靠抓全局变量/父窗口兜底），跨宿主脆弱；只用 ctx 公开 API（`getWorldInfoPrompt` / `loadWorldInfo` / `getWorldInfoNames` / `chatMetadata` / `powerUserSettings`）。
- **激活书名单**没有单一 API：用「激活结果里出现的书 ∪ 角色卡 `data.extensions.world` ∪ `/getcharbook` ∪ `chatMetadata.world_info` ∪ `powerUserSettings.persona_description_lorebook` ∪ `#world_info` 多选框（存在时）」合并，别学 NPC Tracker 列十几条候选路径再做个 debug 面板。
- **异步渲染防串扰**：排除勾选 / 刷新 / 切聊天会并发触发多次渲染，给渲染加序号守卫（`++seq`，每次 await 后检查 `seq !== worldBookRenderSeq` 就 return），否则交错写 DOM 会重复列表。
- **dry run 无副作用 + 内容已宏替换**：激活条目的 content 是酒馆引擎 substituteParams 之后的内容，直接可用；唯一不做的处理是酒馆最后的 world-info 正则后处理（getRegexedString），影响极小、可接受，文档里注明即可。

### 日志系统：网络请求捕获（2026-08-06 追加）

- **旁路观察 fetch**：包装器 `await state.original(...args)` 后原样返回，行为零改动；响应体用 `response.clone()` 异步读，绝不读原始 response（body 只能消费一次）。克隆分支读到上限（20000 字符）就 `reader.cancel()`，SSE 长流不会无限缓冲；tee 机制下取消克隆分支不影响原始分支。
- **请求体也要读**：`fetch(url, { body })` 的 body 是字符串直接读；`fetch(new Request(...))` 的 body 是流，必须 `input.clone()` 再读（原 Request 的 body 会被 fetch 消费，直接读会抛「body already used」）。
- **脱敏要覆盖两处**：请求头（`Authorization` 等）和请求体 JSON 字段（`api_key` / `password` / `proxy_password` 等），正则 `("(?:api[_-]?key|key|password|proxy_password|authorization)"\s*:\s*")[^"]*(")` 一把梭；日志可复制导出，密钥绝不能进缓冲。
- **请求条目要在 await 原始 fetch 之前派发**，否则请求日志和响应日志的时间差会误导排查（漏派 request 事件是真实踩过的坑）。
- **Headers 遍历用 `new Headers(x).forEach((value, key) => ...)`**：key 会被规范化为小写，测试断言别写死原始大小写。
- **级别映射**：请求与 2xx/3xx 记 debug（量大但可筛），4xx 记 warn，5xx 与网络失败记 error——一眼看出哪些调用出了问题。
- **detail 字段**：条目加 `detail`（上限 20000）比硬塞进 message 好——message 保持一行可扫，detail 展开才看；搜索与导出都要带上 detail，否则「搜得到但导不出」。

### 日志系统：噪音治理（2026-08-06 追加）

- **脱敏必须在 pushLogEntry 里做，不能只做在 network 层**：Tavern 自己的 console debug 会 dump 宏变量全表（`[Prompt Template] all variables: {...}`），里面躺着 `ZapiKey`、`apiKey` 明文密钥——console 捕获等于把密钥带进日志。统一在入口对所有来源的 message/detail 做正则脱敏（`sk-[A-Za-z0-9_-]{12,}`、JSON 字段 `apiKey|ZapiKey|password|proxy_password|authorization|token`、`Bearer xxx`、`tauri-invoke-key`）。
- **噪音过滤要看来源+级别**：只对 `source === 'console' && level === 'debug'` 的前缀黑名单生效（`[WI]`、`[Prompt Template]`），warn/error 和 soulink/network 永不误伤；做成可开关（设置 + UI 按钮），用户要排查世界书时能临时关掉。
- **重复折叠用「连续相同」而不是全局去重**：同一内容连续出现（如一次分析里 WI 扫描 ×3）折叠成 `×N` 并更新时间；全局去重会丢失「这次真的又发生了一次」的时间信息，连续折叠两全其美。折叠判定要包含 detail，否则「同一 message 不同详情」会被误合并。
- **折叠条目的实时更新**：折叠时不能 append 新行，要原位更新最后一行（时间 + ×N 徽标）；用 `dataset.id` 匹配最后一行，避免更新错行。
- 真实日志量化：556 条里 518 条（93%）是 `[WI]` 扫描、一次分析重复扫 3 次——先量化再动手，过滤名单只加真正刷屏的前缀，别把有用的也误杀。
- **噪音过滤别只盯 debug 级别**：真实环境里 `[WI]` / `[Prompt Template]` 常以 `console.info` 输出（`[WI] Hypothetically adding 9 entries`、`[Prompt Template] processing #8 messages`），v0.8.3 只过滤 debug 导致全部漏网——用户看到「噪音还是一堆」。量化真实日志后把过滤扩展到 debug+info（warn/error 永不误伤），并补齐名单：正则脚本跳过（`getRegexedString: Skipping script`）、事件总线（`Event emitted:`，宿主事件已由 event 来源记录，console 版纯冗余）、世界书概率（`WI entry`）、元数据保存、生成流程内部。
- **网络日志也有噪音**：Tavern 的 token 计数 / 聊天提交 IPC（`ipc.localhost/*`）和 `/api/chats/save` 会淹没真正的模型调用；按 URL 模式过滤（`/ipc\.localhost/`、`/\/api\/chats\//`），模型 API（`/api/backends/chat-completions/*`）与 SoulLink 自身请求永不误伤。注意 URL 在 message 里带 `POST ` / `200 POST ` 前缀，正则别用 `^` 锚定。

### 日志系统：暂停/跟随与热重载（2026-08-06 追加）

- **暂停不能只拦「实时追加」**：过滤 chips、搜索、开关噪音、重开视图都会走 `renderLogList` 整表重建，暂停期间缓冲的新日志会被「重建」进列表——用户看到的就是「点了暂停还有新日志」。正确做法是暂停时记录可见边界 `pausedAtId`，重建只渲染 `id <= pausedAtId` 的快照，恢复时再一次性显示全部缓冲。
- **模块级 let 状态在热重载后会「各执一份」**：按钮监听器是旧实例的闭包，切换的是旧 `logPaused`；热重载后新版 `pushLogEntry` 检查的是新 `logPaused`——两个变量互不相干，暂停彻底失联。把缓冲与暂停/序列状态迁到 `globalThis.__soullinkLogState__` 共享对象，新旧实例读写同一份数据，按钮与捕获管道永远同步。
- **跟随 ≠ 强制滚动**：自动跟随 = 仅当列表在底部附近（<24px）时 `scrollTop = scrollHeight`，用户上翻阅读历史时新日志照常追加但不打扰；关闭/开启用 is-active + title 说清楚语义，开启时立即滚到底，「回到最新」按钮兜底。
