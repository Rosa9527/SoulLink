# 提示词架构：以「角色扮演」为样本的重构设计

> 本文档拆解当前（v1.0.13 起发布的）「角色扮演」默认提示词的功能模块与问题，给出重构后的通用提示词框架，
> 并落地为新的「角色扮演」默认提示词。判断标准：**一个真正的人能否看懂每个部分在表达什么**；同时结构必须服务于 AI 的稳定执行。

## 一、现状拆解

当前「角色扮演」默认提示词共 3857 字，按出现顺序可拆成 10 个功能模块：

| # | 模块 | 职责 | 约束目标 | 与其他模块的关系 |
|---|------|------|----------|------------------|
| 0 | 开场职责段（无标题） | 身份（角色扮演引擎）、任务（扮演指定角色）、产物用途（交给主模型）、子 agent 行为（直接给结论） | 建立任务锚点：独白围绕「玩家刚才的行为」 | 任务描述与「输入说明」中的触发点描述分处两端 |
| 1 | 输出契约（最高优先级，先读这里） | JSON 硬约束（格式、结构、角色名逐字、只表达自己） | 保证输出可被程序解析 | 提前引用第 4、5 模块才定义的三要素/认知状态/口吻概念；与「口吻要求」争夺「最高优先级」 |
| 2 | 口吻要求（最高优先级） | 表达方式：第一人称心流、口语化、连贯 | 防止分析报告腔、要点列表 | 与模块 1「禁止要点清单/总结」、模块 6「不总结腔」重复 |
| 3 | 推演流程 | 六步内心过程：在场判定→情境定性→动机检索→记忆触发→形成假设→行动倾向 | 把「性格常量」升级为「情境函数」推导链 | 第 3 步引用「下方 <npc_behavior> 动机库」，但动机库实际位于示例之后（模块 8），离引用点最远 |
| 4 | 三要素 | monologue 内容清单：心情、想法、下一步行动 | 保证独白信息完整 | 「下一步行动」与推演第 6 步「行动倾向」同一规则两处表述；「想法」与模块 5「认知框架」边界模糊 |
| 5 | 两种认知状态 | 信息盲区 + 认知框架 | 保证信息差与视角一致性 | 信息盲区的前半段（只能获知档案+亲历/目击）属于**输入使用规则**，后半段（表现困惑/猜测）属于**输出内容要求**——一个模块混了两类职责 |
| 6 | 质量红线 | 不剧情复读 / 不总结腔 / 记忆是原因 / 篇幅 | 宁缺毋滥的底线 | 「不总结腔」与模块 2 重复；「不剧情复读」与推演第 1 步「不在心里复述」语义重叠 |
| 7 | 格式示例 | 两个 JSON 示例（日常疑心 / 冲突威胁） | 示范结构与口吻 | 与模块 1 的结构呼应，位置合理 |
| 8 | <npc_behavior> 动机库 | 7 类动机 + 4 层冲突的参考知识 | 供推演第 3 步选用 | 无标题、无使用说明（何时用、要不要输出）；位置在示例之后，与引用它的推演流程严重脱节 |
| 9 | 输入说明（最后） | 材料清单：角色名、<Character_Profile>、<Recent_Messages> | 说明每份材料怎么用、信息边界 | 位置在全文最后，与阅读顺序相反；worldview/memory 消费指令混入（属使用规则而非材料清单）；触发点说明埋在末尾 |

## 二、问题诊断

### 2.1 结构问题

1. **阅读顺序与执行顺序颠倒**。模型实际执行是：读输入 → 判断在场 → 推演 → 按格式输出。
   但当前顺序是：任务 → 输出契约 → 口吻 → 推演 → 三要素 → 认知状态 → 红线 → 示例 → 动机库 → 输入说明。
   「输入说明」放在最后，模型在不知道材料长什么样时先读了四条输出约束。
2. **双重「最高优先级」**。「输出契约」与「口吻要求」都标注最高优先级，语义冲突——当口吻要求与格式契约冲突时模型无法判断谁优先。
3. **定义先于引用被破坏**。输出契约的 monologue 要求里提前出现「自然融入心情、想法、下一步行动，并体现信息盲区与认知框架」，
   这些概念直到模块 4、5 才定义，形成向前引用。
4. **动机库错位**。被推演第 3 步引用的参考资料放在全文倒数第二段，且无模块身份说明。

### 2.2 功能表达问题

1. **职责重叠**：推演第 6 步（行动倾向）与三要素「下一步行动」是同一规则的两种表述（一处有「对象/方式/时机至少说清两样」、一处没有）；
   认知状态的信息边界与输入说明的信息边界重复声明。
2. **边界模糊**：模块 5 同时承担「输入使用规则」与「输出内容要求」两类职责，修改信息盲区规则时必须同时改动两个模块。

### 2.3 措辞问题

1. 同一禁令出现多次：「禁止要点清单/分析报告/书面总结腔」在输出契约、口吻要求、质量红线出现 3 次；
   「直接给出结论，不要输出思考过程」在开场与预筛风格句各出现 1 次。
2. 「玩家刚才的行为」这一任务锚点分散在开场、形成假设、想法、行动倾向、下一步行动共 5 处，表述各异（解读/回应/即时回应），
   语义一致但难以确认哪处是权威表述。

### 2.4 逻辑顺序问题

- 优先级未形成单一链条：格式契约、口吻、红线各自为政；模型需要自行推断「格式 > 内容 > 红线」的执行优先级。

### 2.5 可维护性问题

- 模块职责不单一，导致「一处事实、多处维护」：改「总结腔」要动 2-3 处，改「信息盲区」要动 2 处，
  新增一条质量规则时难以判断该放进哪个模块。

## 三、新架构设计

### 3.1 设计原则

1. **单一职责**：每个模块只回答一个问题——管什么、何时生效、怎么执行。
2. **阅读流 = 执行流**：模块顺序对齐模型实际执行管线：任务 → 材料 → 思考 → 产出 → 质量 → 示范。
3. **定义先于引用**：任何概念只在其定义模块之后被引用，全文无向前引用。
4. **一个事实只写一次**：同一规则只出现在一个模块，其余位置只引用模块名。
5. **单一最高优先级**：只有输出契约带「最高优先级」，其余模块按层级自然服从。

### 3.2 通用框架

| # | 模块 | 单一职责 | 约束目标 | 触发条件 | 优先级 |
|---|------|----------|----------|----------|--------|
| 1 | 【任务】 | 身份、任务、产物与消费者、行为模式 | 建立任务锚点 | 每次调用 | 任务定义 |
| 2 | 【输入】 | 材料清单 + 每份材料的用法与信息边界 | 保证模型正确消费输入 | 每次调用 | 事实来源 |
| 3 | 【推演】 | 内心处理顺序（明确标注不写入回复） | 保证推导链完整 | 需要推演/判断的调用 | 思考过程 |
| 4 | 【参考】（可选） | 推演可调用的知识库 | 让动机/规则有据可依 | 被推演步骤引用时 | 参考资料 |
| 5 | 【输出】 | 交付物格式契约 + 内容要求 + 写法要求 | 保证可解析、可内化 | 每次调用 | **最高优先级** |
| 6 | 【质量红线】 | 必须避免的行为（宁缺毋滥） | 兜底质量底线 | 每次调用 | 底线约束 |
| 7 | 【示例】 | 完整示范（格式 + 内容 + 口吻） | 降低理解成本 | 每次调用 | 辅助理解 |

模块间关系：

- 【任务】定义「做什么」；【输入】提供「用什么做」；【推演】规定「怎么想」；【输出】规定「写成什么样」；
  【质量红线】规定「不许写成什么样」；【示例】给出「长什么样」。
- 【参考】只服务于【推演】中的具体步骤，模块内首句声明其身份（参考知识，不是输入、不是输出）。
- 【输出】是唯一「最高优先级」：当任何其他模块的内容与格式契约冲突时，以格式契约为准。

### 3.3 为什么是这个顺序

模型读提示词时按顺序建立工作记忆：先知道自己是谁、要干什么（任务），再认识材料（输入），
然后进行思考（推演，可查参考），最后把思考结果按契约格式化（输出），并接受底线约束（红线），
最后通过示例校准对「长什么样」的预期（示例）。这个顺序同时满足：

- **人**：像读一份说明书，每段都能回答「这段在管什么」。
- **AI**：执行时无需跳读或回找，前文定义的概念在后文直接可用，交叉引用降为零。

### 3.4 优先级模型

```
【输出】格式契约（最高优先级，防程序解析失败）
  ↓
【输出】内容三要素 + 认知状态 + 口吻（monologue 质量下限）
  ↓
【质量红线】（宁缺毋滥的底线，篇幅为软约束）
  ↓
【推演】+【参考】（内心过程，不直接进入回复）
```

## 四、新「角色扮演」默认提示词全文

完整文本已落地为 js/constants.js `DEFAULT_PROMPTS.roleplaySystem` 的新值（3709 字），
模块骨架如下（全文以代码为准）：

```
【任务】……你是「角色扮演」子 agent……唯一产出是一个 JSON 对象……直接给出结论……
【输入】……角色名 / <Character_Profile>（worldview、memory 用法与边界）/ <Recent_Messages>（在场判定 + 触发点）……
【推演】……六步内心过程（不写入回复）……
【动机参考】……<npc_behavior> 动机库（参考知识，不是输入、不是输出）……
【输出】……最高优先级契约：JSON 结构 / character 逐字 / 内容三要素 / 两种认知状态 / 口吻 / 只表达自己……
【质量红线】……不剧情复读 / 不总结腔 / 记忆是原因不是背景板 / 篇幅……
【示例】……示例一（日常疑心）/ 示例二（冲突威胁）……
```

## 五、新旧模块映射

| 旧模块 | 新去向 | 处理 |
|--------|--------|------|
| 开场职责段 | 【任务】 | 保留职责、产物用途；补「唯一产出是 JSON 对象」的早期锚定 |
| 输出契约 | 【输出】 | 拆为「格式契约」（最高优先级）+「内容/认知/口吻」三组要求；删除对未定义概念的提前引用 |
| 口吻要求 | 【输出】·口吻 | 与输出契约合并为同一模块，取消第二个「最高优先级」 |
| 推演流程 | 【推演】 | 六步保留；「行动倾向」步只留思考引导，「对象/方式/时机」约束收敛到【输出】·下一步行动（唯一权威表述） |
| 三要素 | 【输出】·内容 | 保留，措辞与推演第 5、6 步去重 |
| 两种认知状态 | 【输入】+【输出】·认知 | 信息边界（只能获知档案+亲历/目击）归【输入】；表现要求（困惑/猜测/误判/求证）归【输出】 |
| 质量红线 | 【质量红线】 | 保留四条；删除与【输出】重复的「禁止总结腔」表述，红线只留具体禁令与示例 |
| 格式示例 | 【示例】 | 保留两个示例原文 |
| <npc_behavior> | 【动机参考】 | 内容原样保留；补模块身份说明（参考知识）；位置移到【推演】之后，消除「下方」交叉引用 |
| 输入说明 | 【输入】 | 移到全文第二模块；材料清单与使用规则分层；触发点与角色名说明并入 |

## 六、可维护性指南

- **新增输出要求**：加进【输出】对应分组（内容/认知/口吻），不要加进红线或推演。
- **新增质量底线**：加进【质量红线】，一条一句，格式「禁止 X，改为 Y」。
- **调整思考链**：只改【推演】步骤；若新步骤需要参考知识，同步在【参考】补条目并让步骤引用模块名。
- **调整信息边界**：只改【输入】对应材料条目；【输出】的认知状态只负责「如何表现」。
- **删除规则**：先全文检索是否在其他模块有语义重复（历史问题是重复，不是缺失）。

## 七、迁移方案

- 新增 `LEGACY_DEFAULT_ROLEPLAY_SYSTEM_V7` 快照（= v1.0.13 已发布默认，即旧全文）。
- `getSettings` 的 roleplaySystem 迁移条件纳入 V7：未自定义（与任一旧版默认逐字一致）的用户自动升级到新默认。
- 自定义过的文本不迁移（与既有迁移策略一致）。
- 新默认文本不在 `isStalePromptText` / `migratePromptText` 的定点替换范围内（那些针对 archiveSystem 的世界书措辞）。

## 八、框架对其他三份预设提示词的适用性

同一框架可直接映射（「档案预筛」已随 v1.0.17 落地、「角色预筛」已随 v1.0.18 落地，见后续章节；「档案系统」留待后续迭代）：

| 预设 | 【任务】 | 【输入】 | 【推演】 | 【参考】 | 【输出】 | 【红线】 | 【示例】 |
|------|----------|----------|----------|----------|----------|----------|----------|
| 档案系统 | 维护档案 | 近期消息/世界书/current_profile/character | 五步决策（初始化→标量→新增→更新→删除） | 无 | JSON diff 契约 + 质量标准 | 依据模糊不新增等 | 无（契约即格式） |
| 档案预筛 | 预筛信息变化角色（v1.0.17 已落地） | 名单块/剧情块 | 逐角色判断（入选标准 1-3 + 不列入） | 无 | JSON 名单契约（唯一最高优先级） | 召回偏向（条件 1/2 拿不准倾向列入）/ 最快返回 | 无 |
| 角色预筛 | 预筛有戏份角色（v1.0.18 已落地） | 名单块/剧情块/触发点 | 逐角色三步（在场判定→戏份判定→归类） | 无 | JSON 名单契约（唯一最高优先级） | 在场从严/戏份从宽两条偏差底线 | 无 |

两份预筛提示词原结构（输出契约前置 + 输入说明 + 判断要点）已相对简单，迁移后的
主要收益是「输入说明前移」「候选集规则收敛到【输入】」「判断要点编号化/分层化」「去掉重复的『最快返回』表述」；
「档案系统」结构最复杂（含档案结构、决策规则、质量标准），迁移收益最大，留待后续迭代。
## 九、「档案预筛」落地（v1.0.17）

### 9.1 现状拆解（v1.0.16 已发布默认，1085 字，按出现顺序）

| # | 模块 | 职责 | 约束目标 | 与其他模块的关系 |
|---|------|------|----------|------------------|
| 0 | 开场职责段（无标题） | 身份（档案预筛员）、任务（哪些角色信息/记忆会变化）、唯一产出（JSON 名单）、候选对象声明、最快返回 | 建立任务锚点 | 候选对象声明与【输入说明】重复；「只输出 JSON」与输出契约重复；最快返回与输出契约首条重复 |
| 1 | 输出契约（最高优先级，先读这里） | JSON 硬约束：仅 JSON、结构、名字逐字、名单外排除、空名单 | 输出可解析、名字可精确匹配 | 「名单外的角色不得列入」是候选集规则（输入域），混入输出格式契约 |
| 2 | 输入说明 | 材料清单：<Registered_Characters>（唯一候选集）、<Recent_Messages>（判断依据） | 材料性质与用法边界 | 「值得记录的新信息/新经历/关系背景」是判断标准却写在材料说明里，职责错位 |
| 3 | 判断要点 | 三类必须列入 + 两类不列入 + 拿不准时偏差 | 入选/排除标准与召回偏向 | 三条「必须列入」句式重复；「只依据消息中实际出现的内容」是输入使用规则，应归【输入】 |

### 9.2 问题诊断

1. **同一规则三处重复**：候选集规则（名单外一律不选 / 不得列入 / 一律忽略）出现在开场、输出契约、输入说明，措辞各异。
2. **输入规则混入输出契约**：「名单外的角色不得列入」约束的是「从哪选」，不是「输出成什么样」，放在输出契约导致格式契约不纯。
3. **判断标准混入输入说明**：「值得记录的新信息/新经历/关系背景」是入选标准，却在输入说明里先出现，模型读材料时先学到判断标准、读判断要点时又重复一遍。
4. **「最快返回」双写**：开场「必须最快返回…回复越短越好」与输出契约首条「…；回复越短越好」重复。
5. **判断要点主语重复**：三条「必须列入：…的名单内角色」句式冗余，压缩为编号入选标准后，「拿不准时」可直接引用编号（条件 1/2 倾向列入），消除偏差规则里的长句复述。
6. **「值得记录」无定义、无下游锚点**：v1.0.8 起三类入选标准本是与「档案系统」写入触发点（fields / memory / worldview / relationships / family）对齐设计的，提示词里却没有点明产物用途，后续改下游触发点时易失同步。

### 9.3 新架构

```
【任务】 身份 / 职责定义（值得写入档案的新信息、新经历，或关系、背景的新内容）/ 唯一产出 / 高频轻量必须最快返回
  ↓
【输入】 <Registered_Characters>（唯一候选集，名单外一律忽略）/ <Recent_Messages>（唯一判断依据，不推测消息之外的情况）
  ↓
【判断】 入选标准（编号 1-3，与下游档案写入点对应）/ 不列入（2 条）/ 拿不准时（条件 1/2 倾向列入）
  ↓
【输出】 唯一最高优先级：仅 JSON / 结构 / 名字逐字 / 空名单
```

- 四个模块各管一件事：任务管「做什么」，输入管「用什么、从哪选」，判断管「怎么定」，输出管「写成什么样」。
- 候选集规则只出现在【输入】；【输出】只保留格式与逐字契约，不再重复排除规则。
- 【输出】是唯一「最高优先级」；最快返回只出现在【任务】，不再双写。
- 判断条件编号化，「拿不准时」直接引用编号，偏差方向与入选标准构成同一决策体系。

### 9.4 新旧模块映射

| 旧模块 | 新去向 | 处理 |
|--------|--------|------|
| 开场职责段 | 【任务】 | 保留身份/任务/唯一产出/最快返回；补「交给档案系统决定本轮更新哪些角色」的下游锚点；删除候选声明（归【输入】）与「只输出 JSON」重复 |
| 输出契约 | 【输出】 | 保留格式/结构/逐字/空名单四条；删除「名单外不得列入」（归【输入】）与「回复越短越好」重复；标题去掉「先读这里」，保留「（最高优先级）」 |
| 输入说明 | 【输入】 | 前移为第二模块；材料清单与使用规则分层（候选集 / 判断依据）；新增「不推测消息之外的情况」；「值得记录」的定义收敛到【任务】与【判断】 |
| 判断要点 | 【判断】 | 三条「必须列入」压缩为编号入选标准 1-3；「不列入」两条独立成组；「拿不准时」改为引用条件编号；「只依据消息中实际出现的内容」归【输入】 |

### 9.5 迁移方案

- 新增 `LEGACY_DEFAULT_ARCHIVE_PRESCREEN_V7` 快照（= v1.0.16 默认文本；与 V6 文本不同，二者各自独立迁移）。
- `getSettings` 的 archivePreScreen 迁移条件纳入 V7：未自定义（与任一旧版默认逐字一致）的用户自动升级到新默认。
- 自定义过的文本不迁移（与既有迁移策略一致）。
- 新默认文本不在 `isStalePromptText` / `migratePromptText` 的定点替换范围内。
## 十、「角色预筛」落地（v1.0.18）

### 10.1 现状拆解（v1.0.16 已发布默认，按出现顺序）

| # | 模块 | 职责 | 约束目标 | 与其他模块的关系 |
|---|------|------|----------|------------------|
| 0 | 开场职责段（无标题） | 身份（预筛员）、任务（末条用户消息引发的下一轮剧情中谁会开口/行动/重要内心反应）、唯一产出（JSON 名单）、高频轻量最快返回、子 agent 直接给结论 | 建立任务锚点 + 速度要求 | 「只输出 JSON 名单」与输出契约重复；「最快返回/回复越短越好」与输出契约首条重复；「直接给结论/不要思考过程」与「不要输出分析过程」重复 |
| 1 | 输出契约（最高优先级，先读这里） | JSON 硬约束：仅 JSON、结构、名字逐字、空数组 | 输出可解析、名字可精确匹配 | 「先读这里」与执行顺序颠倒；候选集约束与开场/输入说明/判断要点重复 4 次 |
| 2 | 输入说明 | 材料清单：<Registered_Characters>（唯一候选集）、<Recent_Messages>（判断依据）、触发点（末条用户消息）、开放式场景引导 | 材料性质与用法边界 | 候选集规则重复；「逐个判断名单中的角色」与判断要点首条重复 |
| 3 | 判断要点 | 三层判定（必须/应/不）+ 动作对象补充 + 在场硬前提 + 戏份排序上限 + 偏差修正 | 入选/排除标准、输出治理与偏差方向 | 「被直接点名」拆成两条；「不在场」在「不列入」与「在场硬前提」重复；排序上限与偏差修正是输出治理规则，混在判定规则里 |

### 10.2 问题诊断

1. **同一规则多处重复**：候选集规则（唯一候选集 / 名单外一律不列入）出现在开场、输出契约、输入说明、判断要点 4 处；「最快返回 / 回复越短越好」3 处；「逐个判断名单中的角色」2 处。
2. **阅读顺序与执行顺序颠倒**：输出契约标注「先读这里」前置，但模型实际执行是 读输入 → 逐角色判断 → 按格式输出。
3. **判断要点层级混乱**：「被直接点名包括…」作为独立条目插在「必须列入」与「应列入」之间，打断三层并列结构；「在场硬前提」与「不列入」中的「不在场」重复表述。
4. **三类职责混排**：判断要点同时承载「判定规则」（三层）、「输出治理」（排序上限）、「偏差修正」（拿不准倾向列入）——改输出上限要在判定规则里找。
5. **在场判定规则与偏差方向混在一条**：「在场是硬性前提」一条里既有推断规则（未明确去向按最近一次出场），又有偏差方向（宁可漏选不可错选不在场）；「在场从严 / 戏份从宽」两个维度没有分列，模型需自行拆解。

### 10.3 新架构

```
【任务】 身份 / 职责定义（末条用户消息引发的下一轮剧情中会开口、行动或重要内心反应）/ 产物用途（供后续逐个推演）/ 高频轻量必须最快返回
  ↓
【输入】 <Registered_Characters>（唯一候选集，名单外一律不列入）/ <Recent_Messages>（唯一判断依据 + 触发点 + 开放式场景引导）
  ↓
【推演】 逐角色三步：在场判定（未明确去向按最近一次出场推断）→ 戏份判定 → 归类
  ↓
【判定标准】 三层分类表：必须列入（含动作对象）/ 应列入 / 不列入
  ↓
【输出】 唯一最高优先级：仅 JSON / 结构 / 名字逐字 / 空数组 / 戏份排序与 6 个上限
  ↓
【质量红线】 两条偏差底线分维度：在场判定从严（宁可漏选不可错选不在场）/ 戏份判定从宽（拿不准倾向列入）
```

- 六个模块各管一件事：任务管「做什么」，输入管「用什么、从哪选」，推演管「按什么顺序想」，判定标准管「怎么定」，输出管「写成什么样」，红线管「偏差往哪偏」。
- 候选集规则只出现在【输入】；最快返回只出现在【任务】；【输出】只保留格式契约与名单治理（排序 / 上限）。
- 【输出】是唯一「最高优先级」；「先读这里」取消——模块顺序即阅读顺序，与执行顺序一致。
- 在场推断规则归【推演】第 1 步，偏差方向归【质量红线】并分维度表述，消除「漏选」维度混淆。

### 10.4 新旧模块映射

| 旧模块 | 新去向 | 处理 |
|--------|--------|------|
| 开场职责段 | 【任务】 | 保留身份/任务/唯一产出/最快返回；补「供后续逐个推演」的产物用途；删除「只输出 JSON 名单」与「直接给结论/不要思考过程」重复（后者并入最快返回一句） |
| 输出契约 | 【输出】 | 保留格式/结构/逐字/空数组四条；新增排序与 6 个上限（从判断要点移入）；标题去掉「先读这里」，保留「最高优先级」；删除「回复越短越好」重复 |
| 输入说明 | 【输入】 | 前移为第二模块；材料清单与使用规则分层（候选集 / 判断依据 / 触发点 / 开放式场景）；候选集规则只保留此处 |
| 判断要点 | 【推演】+【判定标准】+【质量红线】 | 三层判定独立为【判定标准】（动作对象补充并入「必须列入」）；「逐个过名单」改为【推演】三步过程；在场推断规则归【推演】第 1 步；排序上限归【输出】；两条偏差归【质量红线】并分维度 |

### 10.5 迁移方案

- 新增 `LEGACY_DEFAULT_ROLEPLAY_PRESCREEN_V5` 快照（= v1.0.16 已发布默认，即 v1.0.14 文本仅「裁判」改「员」）。
- `getSettings` 的 roleplayPreScreen 迁移条件纳入 V5：未自定义（与任一旧版默认逐字一致）的用户自动升级到新默认。
- 自定义过的文本不迁移（与既有迁移策略一致）。
- 新默认文本不在 `isStalePromptText` / `migratePromptText` 的定点替换范围内。
